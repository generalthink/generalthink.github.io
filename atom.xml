<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Zhu.Yang</title>
  
  <subtitle>朱阳的个人博客</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2019-12-20T03:26:24.675Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>Zhu.Yang</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Kubernetes常用命令</title>
    <link href="http://yoursite.com/2019/12/19/k8s-common-command/"/>
    <id>http://yoursite.com/2019/12/19/k8s-common-command/</id>
    <published>2019-12-19T09:58:24.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<h4 id="获取所有的namespace"><a href="#获取所有的namespace" class="headerlink" title="获取所有的namespace"></a>获取所有的namespace</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get namespace</span><br></pre></td></tr></table></figure><blockquote><p>默认使用的是 ~/.kube/config这个配置文件，如果文件不再这个目录下可以通过 <--kubeconfig configfilepath="">指定<br><a href="https://kubernetes.io/docs/tasks/access-application-cluster/configure-access-multiple-clusters/#set-the-kubeconfig-environment-variable" target="_blank" rel="noopener">https://kubernetes.io/docs/tasks/access-application-cluster/configure-access-multiple-clusters/#set-the-kubeconfig-environment-variable</a></--kubeconfig></p></blockquote><a id="more"></a><h4 id="生成config文件"><a href="#生成config文件" class="headerlink" title="生成config文件"></a>生成config文件</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">kubectl config --kubeconfig=config set-cluster &lt;cluste name&gt; --server=&lt;kubernetes server address&gt;</span><br><span class="line"></span><br><span class="line">kubectl config --kubeconfig=config set-credentials &lt;username&gt; --token=&lt;user token&gt;</span><br><span class="line"></span><br><span class="line">kubectl config --kubeconfig=config set-context &lt;context name&gt; --cluster=&lt;cluster name&gt; --user=&lt;username&gt;</span><br><span class="line"></span><br><span class="line">kubectl config --kubeconfig=config use-context &lt;current context&gt;</span><br></pre></td></tr></table></figure><p>通过上面的命令生成config文件，通过这个文件就可以使用kubectl访问kubernetes集群</p><h4 id="生成证书"><a href="#生成证书" class="headerlink" title="生成证书"></a>生成证书</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl create secret tls &lt;cert name&gt; --cert &lt;hostname.crt&gt; --key &lt;hostname.key&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><p>生成证书之后就可以在ingress配置spec.tls.hosts.secretName中配置命令中指定的<cert name=""></cert></p><h4 id="生成secret"><a href="#生成secret" class="headerlink" title="生成secret"></a>生成secret</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"># &lt;secret file&gt;这里常用的就是property文件，内容是key=value</span><br><span class="line">kubectl create secret generic &lt;secret name&gt; --from-env-file=&lt;secret file&gt; -n &lt;namespace&gt;</span><br><span class="line"></span><br><span class="line"># 删除secret</span><br><span class="line">kubectl delete secret &lt;secret name&gt; -n &lt;namespace&gt;</span><br><span class="line"></span><br><span class="line"># 查看有哪些secret:</span><br><span class="line">kubectl get secrets -n &lt;namespace&gt;</span><br><span class="line"></span><br><span class="line"># 查看secret详细信息</span><br><span class="line">kubectl describe secrets &lt;secret name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><p>secret数据字段存储的是源字符串使用base64编码的。你可以使用下面的命令对其进行base64编码和解码</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 编码</span><br><span class="line">echo -n &apos;1f2d1e2e67df&apos; | base64</span><br><span class="line"></span><br><span class="line"># 解码</span><br><span class="line">echo &apos;MWYyZDFlMmU2N2Rm&apos; | base64 --decode</span><br></pre></td></tr></table></figure><h4 id="查看有哪些pod"><a href="#查看有哪些pod" class="headerlink" title="查看有哪些pod"></a>查看有哪些pod</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods -n &lt;namespace&gt; -o wide</span><br></pre></td></tr></table></figure><h4 id="查看deployment"><a href="#查看deployment" class="headerlink" title="查看deployment"></a>查看deployment</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl describe deploy/&lt;deployment name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><h4 id="查看某个pod日志"><a href="#查看某个pod日志" class="headerlink" title="查看某个pod日志"></a>查看某个pod日志</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl logs &lt;pod name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><h4 id="进入到容器内部"><a href="#进入到容器内部" class="headerlink" title="进入到容器内部"></a>进入到容器内部</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">winpty kubectl exec &lt;pod name&gt; -n &lt;namespace&gt; -it  -- bin/bash</span><br></pre></td></tr></table></figure><p>windows下执行才需要加上winpty,mac是不需要的。 如果bin/bash没有，可以指定bin/sh。</p><p>如果你的pod中部署了多个container,你想进入到某一个中，可以使用下面命令</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">winpty kubectl exec &lt;pod name&gt; -c &lt;container name&gt; -n &lt;name space&gt; -it -- bin/sh</span><br></pre></td></tr></table></figure><h4 id="水平扩展-收缩"><a href="#水平扩展-收缩" class="headerlink" title="水平扩展/收缩"></a>水平扩展/收缩</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl scale deployment &lt;deployment name&gt; --replicas=4</span><br></pre></td></tr></table></figure><h4 id="查看滚动更新结果"><a href="#查看滚动更新结果" class="headerlink" title="查看滚动更新结果"></a>查看滚动更新结果</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 查看deployment</span><br><span class="line">kubectl describe deployment &lt;deployment name&gt; -n &lt;namespace&gt;</span><br><span class="line"></span><br><span class="line"># 或者查看rs</span><br><span class="line">kubectl get rs -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><h4 id="查看历史版本"><a href="#查看历史版本" class="headerlink" title="查看历史版本"></a>查看历史版本</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl rollout history deployment/&lt;deployment name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><h4 id="查看某个历史版本的细节"><a href="#查看某个历史版本的细节" class="headerlink" title="查看某个历史版本的细节"></a>查看某个历史版本的细节</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"># 这里的versionNum是通过rollout history查看到的对应的数字</span><br><span class="line">kubectl rollout history deployment/&lt;deployment name&gt; --revision=&lt;versionNum&gt;</span><br></pre></td></tr></table></figure><h4 id="回滚到某个特定版本"><a href="#回滚到某个特定版本" class="headerlink" title="回滚到某个特定版本"></a>回滚到某个特定版本</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl rollout undo deployment/smcp-service --to-revision=&lt;versionNum&gt;</span><br></pre></td></tr></table></figure><h4 id="删除rs"><a href="#删除rs" class="headerlink" title="删除rs"></a>删除rs</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl delete rs -l app=&lt;label name&gt; -n &lt;namespace&gt;</span><br></pre></td></tr></table></figure><p>每次更新都会产生一个新的replica set,如果要删除那些旧的，可以使用这个命令，并通过-l参数指定label来进行删除</p><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>其实kubectl语法都是存在特定格式的</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl [command] [TYPE] [NAME] [flags]</span><br></pre></td></tr></table></figure><p>写命令忘记的时候，可以使用kubectl -h来查看支持的命令，关于这些命令可以参考 <a href="https://kubernetes.io/docs/reference/kubectl/overview/" target="_blank" rel="noopener">官方文档</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;获取所有的namespace&quot;&gt;&lt;a href=&quot;#获取所有的namespace&quot; class=&quot;headerlink&quot; title=&quot;获取所有的namespace&quot;&gt;&lt;/a&gt;获取所有的namespace&lt;/h4&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;kubectl get namespace&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;blockquote&gt;
&lt;p&gt;默认使用的是 ~/.kube/config这个配置文件，如果文件不再这个目录下可以通过 &lt;--kubeconfig configfilepath=&quot;&quot;&gt;指定&lt;br&gt;&lt;a href=&quot;https://kubernetes.io/docs/tasks/access-application-cluster/configure-access-multiple-clusters/#set-the-kubeconfig-environment-variable&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;https://kubernetes.io/docs/tasks/access-application-cluster/configure-access-multiple-clusters/#set-the-kubeconfig-environment-variable&lt;/a&gt;&lt;/--kubeconfig&gt;&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
    
      <category term="Kubernetes" scheme="http://yoursite.com/tags/Kubernetes/"/>
    
      <category term="K8S" scheme="http://yoursite.com/tags/K8S/"/>
    
  </entry>
  
  <entry>
    <title>github集成drone CI的那些事儿</title>
    <link href="http://yoursite.com/2019/12/10/drone-with-ci/"/>
    <id>http://yoursite.com/2019/12/10/drone-with-ci/</id>
    <published>2019-12-10T02:56:00.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>基于Drone来做CI/CD,个人感觉真的很棒,相对于业界老大哥jekins,我更喜欢drone,相比较而言，我觉得它主要有以下优势</p><ol><li>插件不需要额外管理</li><li>基于yaml文件，易编写,配置可以进行版本管理</li><li>可以根据不同的条件进行构建</li><li>更人性化的UI界面</li></ol><p>对于我们后端的java项目而言，我们CI要做什么呢?</p><p><img src="/images/drone/ci.png" alt="CI流程"></p><a id="more"></a><blockquote><p>一般提交代码流程如下</p><ol><li>Clone 项目到本地，创建一个分支来完成新功能的开发, git checkout -b feature/sync-status。在这个分支修改一些代码</li><li>git add .，书写符合规范的 Commit 并提交代码， git commit -m “sync article status”</li><li>将代码推送到代码库的对应分支， git push origin feature/sync-status</li><li>如果功能已经开发完毕，可以向 Develop(或者Master) 分支发起一个 Pull Request，并让项目的负责人 Code Review</li><li>Review 通过后，项目负责人将分支合并入主干分支</li></ol></blockquote><p>从上图中可以看到当我们提交代码时,会执行整个CI流程，需要注意的有以下2点</p><ol><li>执行build或者unit test的时候，如果失败，会发送消息到Slack,这个时候开发人员就能注意到这个问题，当然也可以使用发送邮件或者微信的方式</li><li>执行SonarQube check的时候,如果存在问题会将结果回写到github中,开发人员就会去看这个问题</li></ol><p>先看下SonarQube回写到github中的的检查结果是怎样的</p><p><img src="/images/drone/sonar-qube-check.png" alt="SonarQube Feed Back"></p><p>当sonar qube检测完成之后,会将检查结果通过oauth的方式发送给github,所以你需要在github中创建Personal access token(这个要记下来)</p><blockquote><p>当你激活你的代码仓库时，Drone会自动将Webhooks添加到版本控制系统中,例如GitHub,而无需手动配置</p></blockquote><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">kind:</span> <span class="string">pipeline</span></span><br><span class="line"><span class="attr">name:</span> <span class="string">default</span></span><br><span class="line"></span><br><span class="line"><span class="attr">steps:</span></span><br><span class="line"><span class="comment"># build for push and pull_request</span></span><br><span class="line"><span class="attr">- name:</span> <span class="string">build-pr</span></span><br><span class="line"><span class="attr">  image:</span> <span class="attr">maven:latest</span></span><br><span class="line"><span class="attr">  pull:</span> <span class="string">if-not-exists</span></span><br><span class="line"><span class="attr">  commands:</span></span><br><span class="line"><span class="bullet">  -</span> <span class="string">mvn</span> <span class="string">clean</span> <span class="string">org.jacoco:jacoco-maven-plugin:prepare-agent</span> <span class="string">install</span> <span class="bullet">-Dmaven.test.skip=true</span> <span class="bullet">-s</span> <span class="string">settings.xml</span></span><br><span class="line"><span class="attr">  when:</span></span><br><span class="line"><span class="attr">    branch:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">feature/*</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">issue/*</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">develop</span></span><br><span class="line"><span class="attr">    event:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">push</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">pull_request</span></span><br><span class="line"></span><br><span class="line"><span class="attr">- name:</span> <span class="string">unittest</span></span><br><span class="line"><span class="attr">  image:</span> <span class="attr">maven:latest</span></span><br><span class="line"><span class="attr">  pull:</span> <span class="string">if-not-exists</span></span><br><span class="line"><span class="attr">  commands:</span></span><br><span class="line"><span class="bullet">  -</span> <span class="string">mvn</span> <span class="string">test</span> <span class="bullet">-s</span> <span class="string">settings.xml</span></span><br><span class="line"><span class="attr">  when:</span></span><br><span class="line"><span class="attr">    branch:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">develop</span></span><br><span class="line"><span class="attr">    event:</span></span><br><span class="line"><span class="attr">      include:</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">pull_request</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">push</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 这里我们使用命令来深度定制我们的扫描，而不是使用drone sonar plugin</span></span><br><span class="line"><span class="attr">- name:</span> <span class="string">sonar-scan</span></span><br><span class="line"><span class="attr">  image:</span> <span class="string">newtmitch/sonar-scanner:4.0.0-alpine</span></span><br><span class="line"><span class="attr">  environment:</span></span><br><span class="line"><span class="attr">    SONAR_TOKEN:</span></span><br><span class="line"><span class="attr">      from_secret:</span> <span class="string">sonar_token</span></span><br><span class="line"><span class="attr">    GITHUB_ACCESS_TOKEN_FOR_SONARQUBE:</span></span><br><span class="line"><span class="attr">      from_secret:</span> <span class="string">github_access_token_for_sonarqube</span></span><br><span class="line"><span class="attr">  commands:</span></span><br><span class="line"><span class="bullet">  -</span> <span class="string">&gt;</span></span><br><span class="line"><span class="string">    sonar-scanner</span></span><br><span class="line"><span class="string">    -Dsonar.host.url=https://sonarqube.company-beta.com/</span></span><br><span class="line"><span class="string">    -Dsonar.login=$$SONAR_TOKEN</span></span><br><span class="line"><span class="string">    -Dsonar.projectKey=smcp-service-BE</span></span><br><span class="line"><span class="string">    -Dsonar.projectName=smcp-service-BE</span></span><br><span class="line"><span class="string">    -Dsonar.projectVersion=$&#123;DRONE_BUILD_NUMBER&#125;</span></span><br><span class="line"><span class="string">    -Dsonar.sources=src/main/java</span></span><br><span class="line"><span class="string">    -Dsonar.tests=src/test/java</span></span><br><span class="line"><span class="string">    -Dsonar.language=java</span></span><br><span class="line"><span class="string">    -Dsonar.java.coveragePlugin=jacoco</span></span><br><span class="line"><span class="string">    -Dsonar.modules=smcp-api,smcp-web</span></span><br><span class="line"><span class="string">    -Dsonar.java.binaries=target</span></span><br><span class="line"><span class="string">    -Dsonar.projectBaseDir=.</span></span><br><span class="line"><span class="string">    -Dsonar.analysis.mode=preview</span></span><br><span class="line"><span class="string">    -Dsonar.github.repository=Today_Group/SMCP-Service</span></span><br><span class="line"><span class="string">    -Dsonar.github.oauth=$$GITHUB_ACCESS_TOKEN_FOR_SONARQUBE</span></span><br><span class="line"><span class="string">    -Dsonar.github.pullRequest=$&#123;DRONE_PULL_REQUEST&#125;</span></span><br><span class="line"><span class="string">    -Dsonar.github.disableInlineComments=false</span></span><br><span class="line"><span class="string"></span><span class="attr">  when:</span></span><br><span class="line"><span class="attr">    event:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">pull_request</span></span><br><span class="line"><span class="attr">    branch:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">develop</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># post sonarscan result back to git PR (not in preview mode)</span></span><br><span class="line"><span class="attr">- name:</span> <span class="string">sonar-scan-feedback</span></span><br><span class="line"><span class="attr">  image:</span> <span class="string">newtmitch/sonar-scanner:4.0.0-alpine</span></span><br><span class="line"><span class="attr">  environment:</span></span><br><span class="line"><span class="attr">    SONAR_TOKEN:</span></span><br><span class="line"><span class="attr">      from_secret:</span> <span class="string">sonar_token</span></span><br><span class="line"><span class="attr">    GITHUB_ACCESS_TOKEN_FOR_SONARQUBE:</span></span><br><span class="line"><span class="attr">      from_secret:</span> <span class="string">github_access_token_for_sonarqube</span></span><br><span class="line"><span class="attr">  commands:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">&gt;</span></span><br><span class="line"><span class="string">      sonar-scanner</span></span><br><span class="line"><span class="string">      -Dsonar.host.url=https://sonarqube.company-beta.com/</span></span><br><span class="line"><span class="string">      -Dsonar.login=$$SONAR_TOKEN</span></span><br><span class="line"><span class="string">      -Dsonar.projectKey=smcp-service-BE</span></span><br><span class="line"><span class="string">      -Dsonar.projectName=smcp-service-BE</span></span><br><span class="line"><span class="string">      -Dsonar.projectVersion=$&#123;DRONE_BUILD_NUMBER&#125;</span></span><br><span class="line"><span class="string">      -Dsonar.sources=src/main/java</span></span><br><span class="line"><span class="string">      -Dsonar.tests=src/test/java</span></span><br><span class="line"><span class="string">      -Dsonar.language=java</span></span><br><span class="line"><span class="string">      -Dsonar.java.coveragePlugin=jacoco</span></span><br><span class="line"><span class="string">      -Dsonar.modules=smcp-api,smcp-web</span></span><br><span class="line"><span class="string">      -Dsonar.java.binaries=target</span></span><br><span class="line"><span class="string">      -Dsonar.projectBaseDir=.</span></span><br><span class="line"><span class="string">      -Dsonar.analysis.gitRepo=Today_Group/SMCP-Service</span></span><br><span class="line"><span class="string">      -Dsonar.analysis.pullRequest=$&#123;DRONE_PULL_REQUEST&#125;</span></span><br><span class="line"><span class="string"></span><span class="attr">  when:</span></span><br><span class="line"><span class="attr">    event:</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">pull_request</span></span><br><span class="line"><span class="attr">    branch:</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">develop</span></span><br></pre></td></tr></table></figure><p>上面drone的配置就是整个CI的基本流程了，需要注意的有以下几点</p><ol><li>只有当分支名称以feature/,issue/,develop开头的才会触发上面的执行步骤,对于unit test而言,只有develop分支才生效(可以根据需要自行定制)</li><li>sonar配置中的sonar.projectKey,sonar.projectName一定要和你在sonar服务器(sonar.host.url指定的地址)中创建project时的名称一样</li><li>sonar_token的值是在sonar服务器上创建的，然后将这个值设置在了drone的secrets中(drone中点击某一个仓库,进入Settings可以进行设置)</li><li>github token和sonar_token是同样的方式，都需要在drone中预设置(好处就是你不会暴露你的密码在文件中，这样更加安全)</li><li>由于所用Java项目是个多模块项目,所以可以在sonar.modules中指定多个模块名称</li><li>sonar scan feedback的内容到pr不要指定preview mode</li><li>build的时候使用了jacoco(分析单元测试覆盖率),所以需要在java项目中pom.xml引入这个plugin</li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.jacoco<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jacoco-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;jacoco.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">executions</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">execution</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>prepare-agent<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">goals</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">goal</span>&gt;</span>prepare-agent<span class="tag">&lt;/<span class="name">goal</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">goals</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">execution</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">execution</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">id</span>&gt;</span>default-report<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">phase</span>&gt;</span>test<span class="tag">&lt;/<span class="name">phase</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">goals</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">goal</span>&gt;</span>report<span class="tag">&lt;/<span class="name">goal</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">goals</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">dataFile</span>&gt;</span>target/jacoco.exec<span class="tag">&lt;/<span class="name">dataFile</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">outputDirectory</span>&gt;</span>target/jacoco<span class="tag">&lt;/<span class="name">outputDirectory</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">execution</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">executions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br></pre></td></tr></table></figure><p><strong>其他可能遇到的问题：</strong></p><ol><li><p>ci执行完成之后，如何发送邮件或者消息到微信群</p><p>答: drone提供了关于<a href="http://plugins.drone.io/drillster/drone-email/" target="_blank" rel="noopener">邮件</a>和<a href="http://plugins.drone.io/clem109/drone-wechat/" target="_blank" rel="noopener">微信的插件</a></p></li><li><p>sonarqube能否集成阿里巴巴的p3c或者自定义checkstyle</p><p>答: 没有p3c的插件，但是可以通过PMD来进行集成</p><blockquote><p>集成p3c: <a href="https://www.jianshu.com/p/a3a58ac368be" target="_blank" rel="noopener">https://www.jianshu.com/p/a3a58ac368be</a><br>自定义checkstyle: <a href="https://www.jianshu.com/p/a3a58ac368be" target="_blank" rel="noopener">https://www.jianshu.com/p/a3a58ac368be</a></p></blockquote></li><li><p>我想根据build的信息(是否成功,时间等)自己做统计怎么办?</p><p>答: drone提供了webhooke的plugin,你只需要编写自己统计的程序就可以了,可以根据模板设置需要发送的信息</p></li><li><p>没有我想要的插件,怎么办?</p><p>答: 可以自己写一个插件,官网有bash/go的示例,用你熟悉的语言也是可以的</p></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;基于Drone来做CI/CD,个人感觉真的很棒,相对于业界老大哥jekins,我更喜欢drone,相比较而言，我觉得它主要有以下优势&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;插件不需要额外管理&lt;/li&gt;
&lt;li&gt;基于yaml文件，易编写,配置可以进行版本管理&lt;/li&gt;
&lt;li&gt;可以根据不同的条件进行构建&lt;/li&gt;
&lt;li&gt;更人性化的UI界面&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;对于我们后端的java项目而言，我们CI要做什么呢?&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/drone/ci.png&quot; alt=&quot;CI流程&quot;&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="drone" scheme="http://yoursite.com/tags/drone/"/>
    
      <category term="CI" scheme="http://yoursite.com/tags/CI/"/>
    
  </entry>
  
  <entry>
    <title>k8s rolling update遇到的一个问题</title>
    <link href="http://yoursite.com/2019/12/02/k8s-rolling-update-get-error/"/>
    <id>http://yoursite.com/2019/12/02/k8s-rolling-update-get-error/</id>
    <published>2019-12-02T10:02:24.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p><strong>在使用K8S rolling update的时候,我同时使用JMeter不间断call API，就有少部分请求抛出了这个错误</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;timestamp&quot;:&quot;2019-12-02T07:33:47.120+0000&quot;,</span><br><span class="line">  &quot;status&quot;:500,</span><br><span class="line">  &quot;error&quot;:&quot;Internal Server Error&quot;,</span><br><span class="line">  &quot;message&quot;:&quot;No message available&quot;,</span><br><span class="line">  &quot;path&quot;:&quot;/api/anon/article/TW1Tq/feed&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><a id="more"></a><p>上面的异常是SpringBoot的BasicErrorController抛出的带有错误，HTTP状态和异常消息的详细信息的JSON响应</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.boot.autoconfigure.web.servlet.error;</span><br><span class="line"></span><br><span class="line"><span class="comment">//...</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"$&#123;server.error.path:$&#123;error.path:/error&#125;&#125;"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BasicErrorController</span> <span class="keyword">extends</span> <span class="title">AbstractErrorController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//...</span></span><br><span class="line">  <span class="meta">@RequestMapping</span></span><br><span class="line">  <span class="keyword">public</span> ResponseEntity&lt;Map&lt;String, Object&gt;&gt; error(HttpServletRequest request) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 方法内部组装了timestamp,error,message,path等信息</span></span><br><span class="line">    Map&lt;String, Object&gt; body = getErrorAttributes(request,</span><br><span class="line">        isIncludeStackTrace(request, MediaType.ALL));</span><br><span class="line">    HttpStatus status = getStatus(request);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> ResponseEntity&lt;&gt;(body, status);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个错误显然是因为服务更新中，容器服务被直接停止，部分请求仍被分发到终止的容器，导致服务出现500错误，这部分错误请求数据占用比较少。由于是部分请求会产生服务器错误的情况，考虑使用优雅的终止方式，将错误请求降到最低，直至滚动更新不影响用户。</p><blockquote><p>kubernetes终止pod过程如下</p><ol><li>kubernetes停止将新的连接路由到需要终止的pod。已建立的连接将保持不变并保持打开状态。</li><li>假设容器将开始停止，Kubernetes将TERM信号发送到Pod中每个容器的根进程。它发送的这个信号无法配置。</li><li>等待pod的TerminalGracePeriodSeconds中指定的时间段(默认为30秒),如果此时容器仍在运行，它将发送KILL终止容器</li></ol></blockquote><p>由于在更新过程中,我们使用jmeter一直发起请求，所以有部分请求仍会发送到需要delete的pod,很明显在30s内它处理不完这么多的请求,而当过了30s之后，发送KILL命令就导致有些请求失败了。</p><p>那么能不能当我们收到通知的时候就不接收新的请求了呢？然后再把正在处理的请求处理完成之后在关闭我们的应用。</p><p>由于我们使用的内嵌的tomcat,所以只需要实现对应的接口就行了。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.apache.catalina.connector.Connector;</span><br><span class="line"><span class="keyword">import</span> org.apache.tomcat.util.threads.ThreadPoolExecutor;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.web.embedded.tomcat.TomcatConnectorCustomizer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.ApplicationListener;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.event.ContextClosedEvent;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GracefulShutdown</span> <span class="keyword">implements</span> <span class="title">TomcatConnectorCustomizer</span>,</span></span><br><span class="line"><span class="class">        <span class="title">ApplicationListener</span>&lt;<span class="title">ContextClosedEvent</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TIMEOUT = <span class="number">30</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">volatile</span> Connector connector;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(Connector connector)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">this</span>.connector = connector;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onApplicationEvent</span><span class="params">(ContextClosedEvent event)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.connector.pause();</span><br><span class="line">    Executor executor = <span class="keyword">this</span>.connector.getProtocolHandler().getExecutor();</span><br><span class="line">    <span class="keyword">if</span> (executor <span class="keyword">instanceof</span> ThreadPoolExecutor) &#123;</span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line"></span><br><span class="line">        ThreadPoolExecutor threadPoolExecutor = (ThreadPoolExecutor) executor;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 将状态设置为shutdown,不再接收新的请求，正在跑的任务会执行完</span></span><br><span class="line">        threadPoolExecutor.shutdown();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!threadPoolExecutor.awaitTermination(TIMEOUT, TimeUnit.SECONDS)) &#123;</span><br><span class="line"></span><br><span class="line">          log.warn(<span class="string">"Tomcat thread pool did not shut down gracefully within "</span></span><br><span class="line">                  + TIMEOUT + <span class="string">" seconds. Proceeding with forceful shutdown"</span>);</span><br><span class="line"></span><br><span class="line">          <span class="comment">// 如果在规定的时间30s之内还未处理完成，那么直接关闭</span></span><br><span class="line">          threadPoolExecutor.shutdownNow();</span><br><span class="line"></span><br><span class="line">          <span class="keyword">if</span> (!threadPoolExecutor.awaitTermination(TIMEOUT, TimeUnit.SECONDS)) &#123;</span><br><span class="line">              log.error(<span class="string">"Tomcat thread pool did not terminate"</span>);</span><br><span class="line"></span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125; <span class="keyword">catch</span> (InterruptedException ex) &#123;</span><br><span class="line">          Thread.currentThread().interrupt();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ShutdownConfiguration</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Bean</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> GracefulShutdown <span class="title">gracefulShutdown</span><span class="params">()</span> </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> GracefulShutdown();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Bean</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> ConfigurableServletWebServerFactory <span class="title">webServerFactory</span><span class="params">(<span class="keyword">final</span> GracefulShutdown gracefulShutdown)</span> </span>&#123;</span><br><span class="line">      TomcatServletWebServerFactory factory = <span class="keyword">new</span> TomcatServletWebServerFactory();</span><br><span class="line">      factory.addConnectorCustomizers(gracefulShutdown);</span><br><span class="line">      <span class="keyword">return</span> factory;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过上面的设置我们就能实现所说的效果。同时即使在滚动更新的时候，一直有请求不断的call api，也不会出现报错的问题了。</p><blockquote><p>这里不断call api,类似于一个高并发请求(现在不说点高并发都不好意思说自己是写代码的了,狗头)</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;在使用K8S rolling update的时候,我同时使用JMeter不间断call API，就有少部分请求抛出了这个错误&lt;/strong&gt;&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;timestamp&amp;quot;:&amp;quot;2019-12-02T07:33:47.120+0000&amp;quot;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;status&amp;quot;:500,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;error&amp;quot;:&amp;quot;Internal Server Error&amp;quot;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;message&amp;quot;:&amp;quot;No message available&amp;quot;,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;path&amp;quot;:&amp;quot;/api/anon/article/TW1Tq/feed&amp;quot;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="K8S" scheme="http://yoursite.com/tags/K8S/"/>
    
      <category term="Rolling Update" scheme="http://yoursite.com/tags/Rolling-Update/"/>
    
  </entry>
  
  <entry>
    <title>K8S中只挂载文件,不要挂载目录</title>
    <link href="http://yoursite.com/2019/11/19/only-mount-file-in-k8s/"/>
    <id>http://yoursite.com/2019/11/19/only-mount-file-in-k8s/</id>
    <published>2019-11-19T07:04:19.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>在kubernetes中部署前端项目(使用nginx作为服务器)的时候,遇到了一个报错,报错信息如下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">2019/11/19 02:16:31 [emerg] 1#1: open() &quot;/etc/nginx/mime.types&quot; failed (2: No such file or directory) in /etc/nginx/nginx.conf:14</span><br><span class="line">nginx: [emerg] open() &quot;/etc/nginx/mime.types&quot; failed (2: No such file or directory) in /etc/nginx/nginx.conf:14</span><br></pre></td></tr></table></figure><p>提示的意思是没有找到mine.types文件,也就是说容器内/etc/nginx下不存在这个文件。但是这个文件不是nginx提供的吗?</p><a id="more"></a><p>我赶忙看了下我是如何部署这个容器的。</p><p>dockerfile:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">FROM nginx:1.17.3</span><br><span class="line">COPY dist /usr/share/nginx/html</span><br></pre></td></tr></table></figure><p>deployment.yml:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1beta2</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">smcp-web</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">nginx-test</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  selector:</span></span><br><span class="line"><span class="attr">    matchLabels:</span></span><br><span class="line"><span class="attr">      app:</span> <span class="string">smcp-web</span></span><br><span class="line"><span class="attr">  replicas:</span> <span class="number">1</span></span><br><span class="line"><span class="attr">  template:</span></span><br><span class="line"><span class="attr">    metadata:</span></span><br><span class="line"><span class="attr">      labels:</span></span><br><span class="line"><span class="attr">        app:</span> <span class="string">smcp-web</span></span><br><span class="line"><span class="attr">    spec:</span></span><br><span class="line"><span class="attr">      containers:</span></span><br><span class="line"><span class="attr">      - name:</span> <span class="string">smcp-web</span></span><br><span class="line"><span class="attr">        image:</span> <span class="string">docker-registry.xxx.com/fe/fe-nginx:no-nginx-conf</span></span><br><span class="line"><span class="attr">        ports:</span></span><br><span class="line"><span class="attr">        - containerPort:</span> <span class="number">80</span></span><br><span class="line"><span class="attr">        volumeMounts:</span></span><br><span class="line"><span class="attr">        - name:</span> <span class="string">nginx-conf</span></span><br><span class="line"><span class="attr">          mountPath:</span> <span class="string">/etc/nginx</span></span><br><span class="line"><span class="attr">      volumes:</span></span><br><span class="line"><span class="attr">      - name:</span> <span class="string">nginx-conf</span></span><br><span class="line"><span class="attr">        configMap:</span></span><br><span class="line"><span class="attr">          name:</span> <span class="string">nginx-conf</span></span><br><span class="line"><span class="attr">          items:</span></span><br><span class="line"><span class="attr">            - key:</span> <span class="string">nginx.conf</span></span><br><span class="line"><span class="attr">              path:</span> <span class="string">nginx.conf</span></span><br></pre></td></tr></table></figure><p>这样就会将在configMap中声明的nginx.conf对应内容挂载到/etc/nginx(mountPath)/nginx.conf文件中。</p><blockquote><p>kubectl create configmap nginx-conf –from-file=nginx.conf=./path/to/nginx.conf -n nginx-test</p></blockquote><p>一想到这里我大概知道了原因，<strong>是因为我们挂载的是整个/etc/nginx目录,但是我们这个目录里面只加了nginx.conf,而容器中/etc/nginx目录下面还有很多其他的文件，<br>我们直接挂载了整个目录进去，其他的文件会随着目录的挂载而消失，自然读取配置就出了问题。</strong></p><p>现在问题已经知道了，如何解决呢?</p><p>要是可以只挂载这个文件就好了，查阅了下文档，发现subPath完全可以满足我们的需求。</p><blockquote><p>subPath的目的是为了在单一Pod中多次使用同一个volume而设计的</p></blockquote><p>所以将spec部分修改如下:</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  containers:</span></span><br><span class="line"><span class="attr">  - name:</span> <span class="string">smcp-web</span></span><br><span class="line"><span class="attr">    image:</span> <span class="string">docker-registry.xxx.com/fe/fe-nginx:no-nginx-conf</span></span><br><span class="line"><span class="attr">    ports:</span></span><br><span class="line"><span class="attr">    - containerPort:</span> <span class="number">80</span></span><br><span class="line"><span class="attr">    volumeMounts:</span></span><br><span class="line"><span class="attr">    - name:</span> <span class="string">nginx-conf</span></span><br><span class="line"><span class="attr">      mountPath:</span> <span class="string">/etc/nginx/nginx.conf</span></span><br><span class="line"><span class="attr">      subPath:</span> <span class="string">nginx.conf</span></span><br><span class="line"><span class="attr">  volumes:</span></span><br><span class="line"><span class="attr">  - name:</span> <span class="string">nginx-conf</span></span><br><span class="line"><span class="attr">    configMap:</span></span><br><span class="line"><span class="attr">      name:</span> <span class="string">nginx-conf</span></span><br></pre></td></tr></table></figure><p>这样我们就做到了将nginx.conf文件挂载到/etc/nginx目录下，同时不影响原有目录中的内容。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在kubernetes中部署前端项目(使用nginx作为服务器)的时候,遇到了一个报错,报错信息如下&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;2019/11/19 02:16:31 [emerg] 1#1: open() &amp;quot;/etc/nginx/mime.types&amp;quot; failed (2: No such file or directory) in /etc/nginx/nginx.conf:14&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;nginx: [emerg] open() &amp;quot;/etc/nginx/mime.types&amp;quot; failed (2: No such file or directory) in /etc/nginx/nginx.conf:14&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;提示的意思是没有找到mine.types文件,也就是说容器内/etc/nginx下不存在这个文件。但是这个文件不是nginx提供的吗?&lt;/p&gt;
    
    </summary>
    
    
      <category term="Kubernetes" scheme="http://yoursite.com/tags/Kubernetes/"/>
    
      <category term="K8S" scheme="http://yoursite.com/tags/K8S/"/>
    
  </entry>
  
  <entry>
    <title>K8S集群中安装Nginx Ingress Controller</title>
    <link href="http://yoursite.com/2019/11/08/install-nginx-ingress-controller/"/>
    <id>http://yoursite.com/2019/11/08/install-nginx-ingress-controller/</id>
    <published>2019-11-08T06:45:10.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>Kubernetes Ingress是为了代理不同后端Service而设置的负载均衡服务,我们自己的每个服务可以根据自己的需求定制自己的ingress rule.</p><p>实际的使用中我们要选择一个具体的ingress controller，部署在k8s集群中，然后Ingress Controller会根据我们定义的Ingress对象,提供对应的代理能力。</p><p>比如我在们开发一个cms系统,是前后端分离的，也是分开部署的，我希望它们有这样的规则</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">http://www.mycms.com/api   ---&gt; 后端</span><br><span class="line"></span><br><span class="line">http://www.mycms.com/   ---&gt;  前端</span><br></pre></td></tr></table></figure><a id="more"></a><p>ingress很简单，下面这样就可以</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">extensions/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Ingress</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">cms-ingress</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">today-partner-beta</span></span><br><span class="line"><span class="attr">  annotations:</span></span><br><span class="line">    <span class="string">nginx.ingress.kubernetes.io/proxy-body-size:</span> <span class="string">"20M"</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  tls:</span></span><br><span class="line"><span class="attr">  - hosts:</span></span><br><span class="line"><span class="bullet">    -</span> <span class="string">mycms.com</span></span><br><span class="line"><span class="attr">    secretName:</span> <span class="string">cms-secret</span></span><br><span class="line"><span class="attr">rules:</span></span><br><span class="line"><span class="attr">- host:</span> <span class="string">mycms.com</span></span><br><span class="line"><span class="attr">  http:</span></span><br><span class="line"><span class="attr">    paths:</span></span><br><span class="line"><span class="attr">    - path:</span> <span class="string">/api</span></span><br><span class="line"><span class="attr">    backend:</span></span><br><span class="line"><span class="attr">      serviceName:</span> <span class="string">cms-service</span></span><br><span class="line"><span class="attr">      servicePort:</span> <span class="number">9000</span></span><br><span class="line"><span class="attr">    - path:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">    backend:</span></span><br><span class="line"><span class="attr">      serviceName:</span> <span class="string">cms-html</span></span><br><span class="line"><span class="attr">      servicePort:</span> <span class="number">80</span></span><br></pre></td></tr></table></figure><p>当我们创建了ingress之后，可以使用下面的命令查看<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ kubectl get ingress cms-ingress -n today-partner-beta</span><br><span class="line">NAME           HOSTS       ADDRESS          PORTS     AGE</span><br><span class="line">cms-ingress   mycms.com   172.19.88.130   80, 443     4d</span><br></pre></td></tr></table></figure></p><p>上面ingress rule这样就会形成下面的映射<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">mycms.com -&gt; 172.19.88.130 -&gt; /api  cms-service:9000</span><br><span class="line">                              /     cms-html:80</span><br></pre></td></tr></table></figure></p><p>当然在这个ingress生效之前，我们首先要在集群中安装ingress controller,它会收集所有的ingress rule,然后在遇到请求的时候进行转发</p><p>这里我们选择的是<a href="https://github.com/kubernetes/ingress-nginx/blob/master/docs/deploy/index.md" target="_blank" rel="noopener">nginx ingress controller</a>,安装步骤如下</p><ol><li>执行下面的命令</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/master/deploy/static/mandatory.yaml</span><br></pre></td></tr></table></figure><p>如果你使用的是1.14之前的Kubernetes版本，则需要在mandatory.yaml的第217行将kubernetes.io/os更改为beta.kubernetes.io/os，</p><ol start="2"><li>使用nodeport的方式暴露nginx-controller</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/master/deploy/static/provider/baremetal/service-nodeport.yaml</span><br></pre></td></tr></table></figure><ol start="3"><li>验证安装是否成功</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl get pods --all-namespaces -l app.kubernetes.io/name=ingress-nginx --watch</span><br></pre></td></tr></table></figure><p>如果你看到ingress controller的状态是running，就代表安装成功，现在你可以安装自己的ingress了。</p><ol start="4"><li>查看nginx ingress controller暴露的端口</li></ol><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">$ kubectl get svc -n ingress-nginx</span><br><span class="line">NAME            TYPE       CLUSTER-IP      EXTERNAL-IP   PORT(S)                      AGE</span><br><span class="line">ingress-nginx   NodePort   172.19.88.130   &lt;none&gt;        80:32257/TCP,443:30839/TCP   6d</span><br></pre></td></tr></table></figure><p>可以看到这里对外暴露出来的端口是32257和30839两个。</p><blockquote><p>这两个端口是在你配置LoadBalance的时候要用的,80–&gt;32257,443–&gt;30839</p></blockquote><p>我们的nginx-ingress-controller部署好之后，它会去拉取集群中的所有ingress-rule,当请求通过worker node转发到ingress controller之后，它会根据具体的规则将请求转发到对应的Service.</p><p><strong>尤其需要注意的是我们k8s集群的网络是私有的，如果你想要访问集群内的服务，只能通过worker node ip才能访问对应的服务。</strong></p><p>比如我的k8s集群中有3个worker node,它们的ip分别是下面这样</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">10.231.123.142</span><br><span class="line">10.231.123.143</span><br><span class="line">10.231.123.147</span><br></pre></td></tr></table></figure><p>一般我们会添加一个LoadBalancer,映射这三个workerer node,这个时候会分配给你一个VIP,然后在DNS中设置域名到VIP的映射</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mycms.com   10.128.161.21</span><br></pre></td></tr></table></figure><blockquote><p>DNS也是可以实现负载均衡的，只是DNS的实现和专业的LoadBanlance相比还是有很大区别的</p></blockquote><p><img src="/images/k8s/lb-http.png" alt="Load Balancer"><br><img src="/images/k8s/lb-https.png" alt="Load Balancer"></p><p>这样当我们访问<a href="https://mycms.com/api的时候,请求通过LB转发到worker" target="_blank" rel="noopener">https://mycms.com/api的时候,请求通过LB转发到worker</a> node,然后通过ingress controller转发到具体的service了。</p><p>如果你是在windows上，你可以配置hosts文件，mycms.com指向worknode ip即可。</p><p><strong> 413 Request Entity Too Large</strong></p><p>当文件上传的时候你遇到这个错误，就证明你需要适当调整你的ingress配置了</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nginx.ingress.kubernetes.io/proxy-body-size : &quot;20M&quot;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Kubernetes Ingress是为了代理不同后端Service而设置的负载均衡服务,我们自己的每个服务可以根据自己的需求定制自己的ingress rule.&lt;/p&gt;
&lt;p&gt;实际的使用中我们要选择一个具体的ingress controller，部署在k8s集群中，然后Ingress Controller会根据我们定义的Ingress对象,提供对应的代理能力。&lt;/p&gt;
&lt;p&gt;比如我在们开发一个cms系统,是前后端分离的，也是分开部署的，我希望它们有这样的规则&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;http://www.mycms.com/api   ---&amp;gt; 后端&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;http://www.mycms.com/   ---&amp;gt;  前端&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="nginx-ingress" scheme="http://yoursite.com/tags/nginx-ingress/"/>
    
      <category term="ingress" scheme="http://yoursite.com/tags/ingress/"/>
    
      <category term="kubernetes" scheme="http://yoursite.com/tags/kubernetes/"/>
    
      <category term="k8s" scheme="http://yoursite.com/tags/k8s/"/>
    
  </entry>
  
  <entry>
    <title>MongoDB 复制集那点事儿</title>
    <link href="http://yoursite.com/2019/10/30/mongodb-replica-set/"/>
    <id>http://yoursite.com/2019/10/30/mongodb-replica-set/</id>
    <published>2019-10-30T03:16:52.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<h3 id="何以高可用"><a href="#何以高可用" class="headerlink" title="何以高可用?"></a>何以高可用?</h3><p>我们以前用Mysql的时候，经常是一台服务器走天下，如果只是用于学习，是没有问题的，但是在生产环境中，这样的风险是很大的，如果服务器因为网络原因或者崩溃了，就会导致数据库一段时间了不可用，这样的体验很不好。</p><p>那么应该怎么办呢？既然一台机器不行，我就多上几台机器总可以了吧，比如我上个两台，让他们互为主备，相互同步数据。想到这里我就只想说一个字，稳。</p><blockquote><p>其实redis,mongodb,kafka等分布式应用基本上都是这样的思想</p></blockquote><p>MongoDB也差不多是这样的思想。它通过复制集来解决这个问题,MongoDB复制集由一组Mongod进程组成，包含一个Primary节点和多个Secondary节点，Mongodb Driver(客户端)的所有数据都写入Primary，Secondary从Primary同步写入的数据，以保持复制集内所有成员存储相同的数据集，提供数据的高可用。</p><p>要想成为primary节点，你必须保证大多数节点都同意才行,大多数的节点就是副本中一半以上的成员。</p><a id="more"></a><table><thead><tr><th>成员总数</th><th>大多数</th><th>容忍失败数</th></tr></thead><tbody><tr><td>1</td><td>1</td><td>0</td></tr><tr><td>2</td><td>2</td><td>0</td></tr><tr><td>3</td><td>2</td><td>1</td></tr><tr><td>4</td><td>3</td><td>1</td></tr><tr><td>5</td><td>3</td><td>2</td></tr><tr><td>6</td><td>4</td><td>2</td></tr><tr><td>7</td><td>4</td><td>3</td></tr></tbody></table><p>为什么要要求大多数呢？其实是为了避免出现两个primary节点。比如一个五个节点的复制集，其中3个成员不可用，剩下的2个仍然正常工作。这两个工作的节点由于不能满足复制集大多数的要求（这个例子中要求要有3个节点才是大多数)，所以他们无法选择主节点，即使其中有一个节点是primary节点，当它注意到它无法获取大多数节点的支持时，它就会退位，成为备份节点。</p><p>如果让这两个节点可以选出primary节点，问题是另外3个节点可能不是真正挂了，而只是网络不可达而已。另外3个节点就一定可以选择出primary节点，这样就存在了两个primary节点了。<br>所以要求大多数就可以避免产生两个primary节点的问题。</p><blockquote><p>如果MongoDB副本集可以拥有多个primary节点,那么就会面临写入冲突的问题，在支持多线程写入的系统中解决冲突的方式有手动解决和让操作系统任选一个这两种方式，但是这两种方式都不易实现，无法保证写入的数据不被其他节点修改，因此mongodb只支持单一的primary节点，这样使得开发更容易。</p></blockquote><p>当一个备份节点无法与主节点连通时，它会联系并请求其他副本集成员将自己选举为主节点，其他成员会做几项理性的检查：自身是否能够与主节点连通？希望被选举为主节点的备份节点的数据是否最新？有没有其他更高优先级的成员可以被选举为主节点？<br>如果竞选节点成员能够得到大多数投票，就会成为主节点。但是一旦大多数成员中只有一个否决了本次选举，选举就会取消。</p><blockquote><p>在日志中可以看到得票数为比较大的负数的情况，因为一张否决票相当于10000张赞成票。如果有2张赞成票，2张否决票，那么选举结果就是-19998，依此类推。</p></blockquote><h3 id="配置选项"><a href="#配置选项" class="headerlink" title="配置选项"></a>配置选项</h3><p>我们一般在部署的时候，副本集节点个数至少是3个(因为它允许1个失败),这也就意味着数据要被复制三份。<br><img src="/images/mongodb/1-primary-2-secondary.png" alt=""></p><h4 id="仲裁者-arbiter"><a href="#仲裁者-arbiter" class="headerlink" title="仲裁者(arbiter)"></a>仲裁者(arbiter)</h4><p>很多人的应用程序使用量比较小，不想保存三份数据，只想要保存两份就行了，保存第三份纯粹是浪费。对于这种部署MongoDB也是支持的。它有一种特殊的成员叫做仲裁者(arbiter)，它唯一的作用就是参与选举，它既不保存数据也不为客户端提供服务，只是为了帮助只有两个成员的副本集满足大多数这个条件而已。<br><img src="/images/mongodb/arbiter.png" alt="arbiter"></p><blockquote><p>仲裁者其实也是有缺点的。如果真有一个节点挂了(数据无法恢复),另一个成员称为主节点。为了数据安全，就需要一个新的备份节点，并且将主节点的数据备份到备份节点。复制数据会对服务器造成很大的压力，会拖慢应用程序。相反如果有三个数据成员即使其中一个挂了，仍有一个主节点和一个备份节点，不影响正常运作。这个时候还可以用剩下的那个备份节点来初始化一个新的备份节点服务器，而不依赖于主节点。所以如果可能尽可能在副本集中使用奇数个数据成员，而不要使用仲裁者。</p></blockquote><h4 id="优先级-priority"><a href="#优先级-priority" class="headerlink" title="优先级(priority)"></a>优先级(priority)</h4><p>如果想让一个节点有更大的机会成为primary的话这需要设置优先级，比如我添加一个优先级为2的成员(默认为1)</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rs.add(&#123;&quot;_id&quot;:4, &quot;host&quot;: &quot;10.17.28.190:27017&quot;, &quot;priority&quot; : 2&#125;);</span><br></pre></td></tr></table></figure><p>假设其他都是默认优先级，只要10.17.28.190拥有最新数据，那么当前primary节点就会自动退位，10.17.28.190会被选举为新的主节点。如果它的数据不够新，那么当前主节点就会保持不变。</p><p>如果设置priority为0，表示不会被选为primary节点。</p><h4 id="投票权-vote"><a href="#投票权-vote" class="headerlink" title="投票权(vote)"></a>投票权(vote)</h4><p>由于复制集成员最多50个,而参与Primary成员投票的最多7个,所以其他成员的vote必须设置为0(priority也必须为0)。<br>尽管无投票权的成员不会在选举中投票，但这些成员拥有副本集数据的副本，并且可以接受来自客户端应用程序的读取操作。</p><h4 id="隐藏成员-hidden"><a href="#隐藏成员-hidden" class="headerlink" title="隐藏成员(hidden)"></a>隐藏成员(hidden)</h4><p>客户端不会像隐藏成员发送请求，隐藏成员也不会作为复制源(尽管当其他复制源不可用时隐藏成员)。因此很多人将不够强大的服务器或者备份服务器隐藏起来。通过设置hidden:true可以设置隐藏，只有优先级为0的才能被隐藏。<br>可使用Hidden节点做一些数据备份、离线计算的任务，不会影响复制集的服务</p><h4 id="延迟备份节点-slaveDelay"><a href="#延迟备份节点-slaveDelay" class="headerlink" title="延迟备份节点(slaveDelay)"></a>延迟备份节点(slaveDelay)</h4><p>数据可能会因为人为错误而遭到毁灭性的破坏，为了防止这类问题，可以使用slaveDelay设置一个延迟的备份节点。</p><p>延迟备份节点的数据回比主节点延迟指定的时间(单位是秒),slaveDelay要求优先级是0，如果应用会将读请求路由到备份节点，应该将延迟备份节点隐藏掉，以免读请求被路由到延迟备份节点。</p><p>因Delayed节点的数据比Primary落后一段时间，当错误或者无效的数据写入Primary时，可通过Delayed节点的数据来恢复到之前的时间点。</p><h4 id="修改副本集配置"><a href="#修改副本集配置" class="headerlink" title="修改副本集配置"></a>修改副本集配置</h4><p>比如我有个副本集叫做rs0,我想修改增加或者删除成员，修改成员的配置(vote,hidden,priority等)可以通过reconfig命令</p><blockquote><p><a href="https://docs.mongodb.com/manual/reference/method/rs.reconfig/" target="_blank" rel="noopener">https://docs.mongodb.com/manual/reference/method/rs.reconfig/</a></p></blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">cfg = rs.conf();</span><br><span class="line">cfg.members[1].priority = 2;</span><br><span class="line">rs.reconfig(cfg);</span><br></pre></td></tr></table></figure><h3 id="同步"><a href="#同步" class="headerlink" title="同步"></a>同步</h3><p>Primary与Secondary之间通过oplog来同步数据，Primary上的写操作完成后，会向特殊的local.oplog.rs特殊集合写入一条oplog，Secondary不断的从Primary取新的oplog并应用。</p><p>因oplog的数据会不断增加，local.oplog.rs被设置成为一个capped集合，当容量达到配置上限时，会将最旧的数据删除掉。由于复制操作的过程是先复制数据在写入oplog，oplog必须具有幂等性，即重复应用也会得到相同的结果。</p><p>我向test库的coll集合插入了一条数据之后(<code>db.coll.insert({count:1})</code>)，调用db.isMaster()命令可以看到当前节点的最后一次写入时间戳</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">&gt; db.isMaster()</span><br><span class="line">&#123;</span><br><span class="line">  &quot;ismaster&quot; : true,</span><br><span class="line">  &quot;secondary&quot; : false,</span><br><span class="line">  &quot;lastWrite&quot; : &#123;</span><br><span class="line">    &quot;opTime&quot; : &#123;</span><br><span class="line">            &quot;ts&quot; : Timestamp(1572509087, 2),</span><br><span class="line">            &quot;t&quot; : NumberLong(1)</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;lastWriteDate&quot; : ISODate(&quot;2019-10-31T08:04:47Z&quot;),</span><br><span class="line">    &quot;majorityOpTime&quot; : &#123;</span><br><span class="line">            &quot;ts&quot; : Timestamp(1572509087, 2),</span><br><span class="line">            &quot;t&quot; : NumberLong(1)</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;majorityWriteDate&quot; : ISODate(&quot;2019-10-31T08:04:47Z&quot;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>命令会返回很多数据，这里我只列出了小部分，可以看到我们当前所在节点是master节点(primary),如果当前节点不是primary,也会通过primary属性告诉你当前primary节点是哪个,同时最后一次写入的时间戳是1572509087。</p><p>此时我们登录另一台secondary节点,切换到local数据库,执行命令<code>db.oplog.rs.find()</code>命令,会返回很多条数据，这里我们查看最后一条即可<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&#123; </span><br><span class="line">  &quot;ts&quot; : Timestamp(1572509087, 2), </span><br><span class="line">  &quot;t&quot; : NumberLong(1), </span><br><span class="line">  &quot;h&quot; : NumberLong(&quot;6139682004250579847&quot;), </span><br><span class="line">  &quot;v&quot; : 2, </span><br><span class="line">  &quot;op&quot; : &quot;i&quot;, </span><br><span class="line">  &quot;ns&quot; : &quot;test.coll&quot;, </span><br><span class="line">  &quot;ui&quot; : UUID(&quot;1be7f8d0-fde2-4d68-89ea-808f14b326da&quot;), </span><br><span class="line">  &quot;wall&quot; : ISODate(&quot;2019-10-31T08:04:47.925Z&quot;), </span><br><span class="line">  &quot;o&quot; : &#123; </span><br><span class="line">    &quot;_id&quot; : ObjectId(&quot;5dba959fcf287dfd8727a1bf&quot;), </span><br><span class="line">    &quot;count&quot; : 1 </span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>可以看到oplog的ts和isMater()命令返回的lastTime.opTime.ts的值是一致的,证明我们的数据是最新的，如果你这个时候访问其他节点查看oplog.rs的数据，会发现数据是一模一样的。<br>在来解释下字段含义</p><ul><li>ts : 操作时间，当前timestamp + 计数器，计数器每秒都被重置</li><li>h：操作的全局唯一标识</li><li>v：oplog版本信息</li><li>op：操作类型<ul><li>i：插入操作</li><li>u：更新操作</li><li>d：删除操作</li><li>c：执行命令（如createDatabase，dropDatabase）</li></ul></li><li>n：空操作，特殊用途</li><li>ns：操作针对的集合</li><li>o：操作内容，可以看到我这里插入的字段是count，值是1</li><li>o2：操作查询条件，仅update操作包含该字段</li></ul><h4 id="初始化同步"><a href="#初始化同步" class="headerlink" title="初始化同步"></a>初始化同步</h4><p>副本集中的成员启动之后，就会检查自身状态，确定是否可以从某个成员那里进行同步。如果不行的话，它会尝试从副本的另一个成员那里进行完整的数据复制。这个过程就是初始化同步(initial syncing)。</p><p>init sync过程包含如下步骤</p><ol><li>准备工作删除所有已存在的数据库，以一个全新的状态开始同步</li><li>将同步源的所有记录全部复制到本地(除了local)</li><li>oplog同步第一步,克隆过程中的所有操作都会被记录到oplog中，如果有文档在克隆过程中被移动了，就有可能会被遗漏，导致没有被克隆，对于这样的文档，可能需要重新克隆</li><li>oplog同步过程第二步，将第一个oplog同步中的操作记录下来</li><li>创建索引</li><li>如果当前节点的数据仍然远远落后于同步源，那么oplog同步过程的最后一步就是将创建索引期间的所有操作全部同步过来，防止该成员成为备份节点。</li><li>完成初始化同步之后，切换到普通同步状态，这时当前成员就可以称为备份节点了。</li></ol><p>总结起来就是从其他节点同步全量数据，然后不过从Primary的local.oplog.rs集合里查询最新的oplog并应用到自身。</p><blockquote><p>查询固定集合使用的tailable cursor(<a href="https://docs.mongodb.com/manual/core/tailable-cursors/" target="_blank" rel="noopener">https://docs.mongodb.com/manual/core/tailable-cursors/</a>)</p></blockquote><h3 id="Primary选举"><a href="#Primary选举" class="headerlink" title="Primary选举"></a>Primary选举</h3><p>Primary选举除了在复制集初始化时发生，还有如下场景</p><ol><li>复制集reconfig</li><li>Secondary节点检测到Primary宕机时，会触发新Primary的选举</li><li>当有Primary节点主动stepDown（主动降级为Secondary）时，也会触发新的Primary选举</li></ol><p>Primary的选举受节点间心跳、优先级、最新的oplog时间等多种因素影响。</p><h4 id="节点间心跳"><a href="#节点间心跳" class="headerlink" title="节点间心跳"></a>节点间心跳</h4><p>复制集成员间默认每2s会发送一次心跳信息，如果10s未收到某个节点的心跳，则认为该节点已宕机；如果宕机的节点为Primary，Secondary（前提是可被选为Primary）会发起新的Primary选举。</p><blockquote><p>心跳是为了知道其他成员状态,哪个是主节点，哪个可以作为同步源，哪个挂掉了等等信息</p></blockquote><p>成员状态:</p><ol><li>STARTUP : 刚启动时处于这个状态，加载副本集成功后就进入STARTUP2状态</li><li>STARTUP2 : 整个初始化同步都处于这个状态，这个状态下，MongDB会创建几个线程，用于处理复制和选举，然后就会切换到RECOVERING状态</li><li>RECOVERING : 表示运行正常，当暂时不能处理读取请求。如果有成员处于这个状态，可能会造成轻微系统过载</li><li>ARBITER : 仲裁者处于这个状态</li><li>DOWN : 一个正常运行的成员不可达，就处于DOWN状态。这个状态有可能是网络问题</li><li>UNKNOWN : 成员无法到达其他任何成员，其他成员就知道无法它处于什么状态，就会处于UNKNOWN。表明这个未知状态的成员挂掉了。或者两个成员间存在网络访问问题。</li><li>REMOVED : 被移除副本集时处于的状态,添加回来后，就会回到正常状态</li><li>ROLLBACK : 处于数据回滚时就处于ROLLBACK状态。回滚结束后，会换为RECOVERING状态，然后成为备份节点。</li><li>FATAL : 发生不可挽回错误，也不再尝试恢复，就处于这个状态。这个时候通常应该重启服务器</li></ol><h4 id="节点优先级"><a href="#节点优先级" class="headerlink" title="节点优先级"></a>节点优先级</h4><ol><li>每个节点都倾向于投票给优先级最高的节点</li><li>优先级为0的节点不会主动发起Primary选举</li><li>当Primary发现有优先级更高Secondary，并且该Secondary的数据落后在10s内，则Primary会主动降级，让优先级更高的Secondary有成为Primary的机会。</li></ol><h4 id="OpTime"><a href="#OpTime" class="headerlink" title="OpTime"></a>OpTime</h4><p>最新optime（最近一条oplog的时间戳）的节点才能被选为主,请看上面对oplog.rs的分析。</p><h4 id="网络分区"><a href="#网络分区" class="headerlink" title="网络分区"></a>网络分区</h4><p>只有大多数投票节点间保持网络连通，才有机会被选Primary；如果Primary与大多数的节点断开连接，Primary会主动降级为Secondary。当发生网络分区时，可能在短时间内出现多个Primary，故Driver在写入时，最好设置<strong>大多数成功</strong>的策略，这样即使出现多个Primary，也只有一个Primary能成功写入大多数。</p><h3 id="复制集的读写设置"><a href="#复制集的读写设置" class="headerlink" title="复制集的读写设置"></a>复制集的读写设置</h3><h4 id="Read-Preference"><a href="#Read-Preference" class="headerlink" title="Read Preference"></a>Read Preference</h4><p>默认情况下，复制集的所有读请求都发到Primary，Driver可通过设置Read Preference来将读请求路由到其他的节点。</p><ol><li>primary： 默认规则，所有读请求发到Primary</li><li>primaryPreferred： Primary优先，如果Primary不可达，请求Secondary</li><li>secondary： 所有的读请求都发到secondary</li><li>secondaryPreferred：Secondary优先，当所有Secondary不可达时，请求Primary</li><li>nearest：读请求发送到最近的可达节点上（通过ping探测得出最近的节点）</li></ol><h4 id="Write-Concern"><a href="#Write-Concern" class="headerlink" title="Write Concern"></a>Write Concern</h4><p>默认情况下，Primary完成写操作即返回，Driver可通过设置<a href="https://docs.mongodb.org/manual/core/write-concern/" target="_blank" rel="noopener">Write Concern</a>来设置写成功的规则。</p><p>如下的write concern规则设置写必须在大多数节点上成功，超时时间为5s。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">db.products.insert(</span><br><span class="line">  &#123; item: &quot;envelopes&quot;, qty : 100, type: &quot;Clasp&quot; &#125;,</span><br><span class="line">  &#123; writeConcern: &#123; w: majority, wtimeout: 5000 &#125; &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure><p>上面的设置方式是针对单个请求的，也可以修改副本集默认的write concern，这样就不用每个请求单独设置。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">cfg = rs.conf()</span><br><span class="line">cfg.settings = &#123;&#125;</span><br><span class="line">cfg.settings.getLastErrorDefaults = &#123; w: &quot;majority&quot;, wtimeout: 5000 &#125;</span><br><span class="line">rs.reconfig(cfg)</span><br></pre></td></tr></table></figure><h3 id="回滚（rollback）"><a href="#回滚（rollback）" class="headerlink" title="回滚（rollback）"></a>回滚（rollback）</h3><p>Primary执行了一个写请求之后挂了，但是备份节点还没有来得及复制这次操作。新选举出来的主节点结就会漏掉这次写操作。当旧Primary恢复之后，就要回滚部分操作。</p><p>比如一个复制集存在两个数据中心,DC1中存在A(primary),B两个节点,DC2中存在C,D,E这三个节点。<br>如果DC1出现了故障。其中DC1这个数据中心的最后的操作是126,但是126没有被复制到另外的数据中心。所以DC2中服务器最新的操作是125</p><p>DC2的数据中心仍然满足副本集大多数的要求(5台，DC2有3台),因此其中一个会被选举成为新的主节点，这个节点会继续处理后续的写入操作。<br>当网络恢复之后，DC1中心的服务器就会从其他服务器同步126之后的操作，但是无法找到。这种时候DC1中的A,B就会进入回滚过程。</p><p>回滚回将失败之前未复制的操作撤销。拥有126操作的服务器会在DC2的服务器的oplog寻找共同的操作点。这里会定位125，这是两个数据中心相匹配的最后一个操作。</p><p>这时，服务器会查看这些没有被复制的操作，将受这些操作影响的文档写入一个.bson文件，保存在数据目录下的rollback目录中。</p><p>如果126是一个更新操作，服务器回将126更新的文档写入collectionName.bson文件。如果想要恢复被回滚的操作，可以使用mongorestore命令。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;何以高可用&quot;&gt;&lt;a href=&quot;#何以高可用&quot; class=&quot;headerlink&quot; title=&quot;何以高可用?&quot;&gt;&lt;/a&gt;何以高可用?&lt;/h3&gt;&lt;p&gt;我们以前用Mysql的时候，经常是一台服务器走天下，如果只是用于学习，是没有问题的，但是在生产环境中，这样的风险是很大的，如果服务器因为网络原因或者崩溃了，就会导致数据库一段时间了不可用，这样的体验很不好。&lt;/p&gt;
&lt;p&gt;那么应该怎么办呢？既然一台机器不行，我就多上几台机器总可以了吧，比如我上个两台，让他们互为主备，相互同步数据。想到这里我就只想说一个字，稳。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;其实redis,mongodb,kafka等分布式应用基本上都是这样的思想&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;MongoDB也差不多是这样的思想。它通过复制集来解决这个问题,MongoDB复制集由一组Mongod进程组成，包含一个Primary节点和多个Secondary节点，Mongodb Driver(客户端)的所有数据都写入Primary，Secondary从Primary同步写入的数据，以保持复制集内所有成员存储相同的数据集，提供数据的高可用。&lt;/p&gt;
&lt;p&gt;要想成为primary节点，你必须保证大多数节点都同意才行,大多数的节点就是副本中一半以上的成员。&lt;/p&gt;
    
    </summary>
    
    
      <category term="MongoDB" scheme="http://yoursite.com/tags/MongoDB/"/>
    
  </entry>
  
  <entry>
    <title>让你的SpringBoot一次build,到处运行</title>
    <link href="http://yoursite.com/2019/10/25/springboot-with-k8s/"/>
    <id>http://yoursite.com/2019/10/25/springboot-with-k8s/</id>
    <published>2019-10-25T05:54:53.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>开发web项目的时候，我们一般会有多个环境(dev,beta,rc,production),然后每次使用maven打包的时候都要指定profile</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mven clean package -P beta</span><br></pre></td></tr></table></figure><p>这样就可以将application-{profile}.properties打入jar包中。这也就意味着对于不同的环境我们要打不同的包，虽然他们只有一些配置信息不同而已。</p><p>那么有没有办法在不同的环境中使用相同的应用程序代码呢? 当然有,<a href="https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-features.html#boot-features-external-config" target="_blank" rel="noopener">springboot提供了这样的机会</a>,它允许我们通过很多种方式来决定配置文件是哪一个,当然啦本篇文章并不是介绍有哪些加载外部配置文件的方式。</p><a id="more"></a><p>就比如祸水三千，我只取一瓢。所以这次我们直接指定property文件的位置即可</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -jar myproject.jar --spring.config.location=classpath:/default.properties,/myconfig/application.properties</span><br></pre></td></tr></table></figure><p>道理是这么个道理,但是现在大多的项目都是部署在k8s上的,那么应该如何做呢？</p><p>这里我借助了k8s configMap来实现加载外部配置文件达到我们的目的。</p><p>首先这里生成了configMap<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl create configmap application-configmap --from-file=application.properties=../smcp-web/src/main/resources/application-beta.properties -o yaml -n smcp| kubectl replace -f -</span><br></pre></td></tr></table></figure></p><p>解释下上面命令的意思,每一次在打包镜像之后，都会重新生成configMap,生成的application-configmap中，key=application.properties,value则是项目中application-beta.properties的内容</p><blockquote><p>生成configmap有很多种方式，可以根据自己的需求来生成，可以查看官方文档 <a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-pod-configmap/" target="_blank" rel="noopener">https://kubernetes.io/docs/tasks/configure-pod-container/configure-pod-configmap/</a><br>-n 的意思是指定namespace,如果你使用的是默认的namespace,那么可以不用指定。 -n == –namespace</p></blockquote><p>我们生成的上面的configMap可以通过<code>kubectl get configmap application-configmap -o yaml -n smcp</code>查看</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">data:</span></span><br><span class="line">  <span class="string">application.properties:</span> <span class="string">|</span></span><br><span class="line"><span class="string">    logging.level.com.project.smcp=INFO</span></span><br><span class="line"><span class="string">    logging.level.org.apache.shiro=INFO</span></span><br><span class="line"><span class="string">    logging.level.okhttp3.OkHttpClient=ERROR</span></span><br><span class="line"><span class="string"></span><span class="attr">kind:</span> <span class="string">ConfigMap</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  creationTimestamp:</span> <span class="number">2019</span><span class="bullet">-10</span><span class="bullet">-24</span><span class="attr">T11:41:57Z</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">application-configmap</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">smcp</span></span><br><span class="line"><span class="attr">  resourceVersion:</span> <span class="string">"62755048"</span></span><br><span class="line"><span class="attr">  uid:</span> <span class="number">48</span><span class="string">c8ab36-f653-11e9-9eab-fa163fea9021</span></span><br></pre></td></tr></table></figure><p>可以看到其实是将application-beta.properties文件中的内容全部拷贝到了data中，然后这些所有值的key为application.properties.再来看看在我们deployment.yml文件中如何使用</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1beta2</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Deployment</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">smcp</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  selector:</span></span><br><span class="line"><span class="attr">    matchLabels:</span></span><br><span class="line"><span class="attr">      app:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">  replicas:</span> <span class="number">1</span></span><br><span class="line"><span class="attr">  template:</span></span><br><span class="line"><span class="attr">    metadata:</span></span><br><span class="line"><span class="attr">      labels:</span></span><br><span class="line"><span class="attr">        app:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">    spec:</span></span><br><span class="line"><span class="attr">      containers:</span></span><br><span class="line"><span class="attr">      - name:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">        image:</span> <span class="string">docker-registry.xxx.com/xxx/smcp-web:latest</span></span><br><span class="line"><span class="attr">        args:</span> <span class="string">["--spring.config.location=/smcp-config/application.properties"]</span></span><br><span class="line"><span class="attr">        ports:</span></span><br><span class="line"><span class="attr">        - containerPort:</span> <span class="number">9000</span></span><br><span class="line"><span class="attr">        volumeMounts:</span></span><br><span class="line"><span class="attr">        - name:</span> <span class="string">application-config</span></span><br><span class="line"><span class="attr">          mountPath:</span> <span class="string">smcp-config</span></span><br><span class="line"><span class="attr">        envFrom:</span></span><br><span class="line"><span class="attr">        - secretRef:</span></span><br><span class="line"><span class="attr">            name:</span> <span class="string">my-secret</span></span><br><span class="line"><span class="attr">      volumes:</span></span><br><span class="line"><span class="attr">      - name:</span> <span class="string">application-config</span></span><br><span class="line"><span class="attr">        configMap:</span></span><br><span class="line"><span class="attr">          name:</span> <span class="string">application-configmap</span></span><br><span class="line"><span class="attr">          items:</span></span><br><span class="line"><span class="attr">            - key:</span> <span class="string">application.properties</span></span><br><span class="line"><span class="attr">              path:</span> <span class="string">application.properties</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Service</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line"><span class="attr">  name:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">  namespace:</span> <span class="string">smcp</span></span><br><span class="line"><span class="attr">  labels:</span></span><br><span class="line"><span class="attr">    app:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line"><span class="attr">  ports:</span></span><br><span class="line"><span class="attr">  - targetPort:</span> <span class="number">9000</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">9000</span></span><br><span class="line"><span class="attr">    protocol:</span> <span class="string">TCP</span></span><br><span class="line"><span class="attr">  selector:</span></span><br><span class="line"><span class="attr">    app:</span> <span class="string">smcp-service</span></span><br><span class="line"><span class="attr">  type:</span> <span class="string">NodePort</span></span><br></pre></td></tr></table></figure><p>经过上面的配置，现在application.properties文件就处于/smcp-config下了。看到这里可能会有人有疑问,为什么我的configMap要单独用命令生成，而不是在demployment.yaml文件中单独声明configMap类型的配置呢?<br>其实也是可以的，但是如果这样做你每次添加了环境变量你都要去demployment.yml中添加配置,你这样可能就要配置两次(application-{profile}.properties中还要配置一次)，所以我就干脆用命令生成，在开发的时候不要考虑k8s的存在。</p><p>还要注意上面的args参数，通过它我们指定了外部环境变量的路径。而对于生成镜像的dockerfile，其实也很简单<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">FROM java:8</span><br><span class="line">EXPOSE 9090</span><br><span class="line">ADD target/smcp-web.jar /smcp-web.jar</span><br><span class="line">ENTRYPOINT [&quot;java&quot;, &quot;-jar&quot;,&quot;/smcp-web.jar&quot;]</span><br></pre></td></tr></table></figure></p><p>最后当我们执行了下面的命令之后，就可以部署我们的容器到k8s中了</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl apply -f deployment.yml</span><br></pre></td></tr></table></figure><p>最后部署后，进入Pod中，可以看到目录结构如下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">smcp-web.jar</span><br><span class="line">smcp-config</span><br><span class="line">  -- application.properties</span><br></pre></td></tr></table></figure><p>而启动命令也变成了下面这样，可以通过<code>ps -ef</code>查看</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -jar /smcp-web.jar --spring.config.location=/smcp-config/application.properties</span><br></pre></td></tr></table></figure><p>上面使用configMap的方式处理,当然还可以直接在环境变量中使用configMap(参考上面secretRef的方式)，当使用环境变量的方式注入configMap的时候，你需要使用下面这样的命令生成数据。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl create configmap application-configmap --fron-env-file=src/main/resources/application-beta.properties</span><br></pre></td></tr></table></figure><p>同时yaml文件中关于configMap的修改成下面这样,使用环境变量的方式记得把volumes去掉。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">envFrom:</span><br><span class="line">- configMapRef:</span><br><span class="line">  name: application-configmap</span><br></pre></td></tr></table></figure></p><p>但是使用环境变量注入的方式有一点需要注意,当你更新了configMap而不重新部署的时候,容器中的变量是不会更新的，而如果使用mountPath的方式,环境变量的值就会更新(大概10s左右)。</p><blockquote><p>如果你还使用mountPath的同时还使用了subpath同样不会更新</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;开发web项目的时候，我们一般会有多个环境(dev,beta,rc,production),然后每次使用maven打包的时候都要指定profile&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;mven clean package -P beta&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;这样就可以将application-{profile}.properties打入jar包中。这也就意味着对于不同的环境我们要打不同的包，虽然他们只有一些配置信息不同而已。&lt;/p&gt;
&lt;p&gt;那么有没有办法在不同的环境中使用相同的应用程序代码呢? 当然有,&lt;a href=&quot;https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-features.html#boot-features-external-config&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;springboot提供了这样的机会&lt;/a&gt;,它允许我们通过很多种方式来决定配置文件是哪一个,当然啦本篇文章并不是介绍有哪些加载外部配置文件的方式。&lt;/p&gt;
    
    </summary>
    
    
      <category term="spring" scheme="http://yoursite.com/tags/spring/"/>
    
      <category term="k8s configMap" scheme="http://yoursite.com/tags/k8s-configMap/"/>
    
  </entry>
  
  <entry>
    <title>MongoDB设计技巧</title>
    <link href="http://yoursite.com/2019/10/18/mongodb-design-skill/"/>
    <id>http://yoursite.com/2019/10/18/mongodb-design-skill/</id>
    <published>2019-10-18T07:01:56.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<h3 id="范式化设计还是反范式"><a href="#范式化设计还是反范式" class="headerlink" title="范式化设计还是反范式"></a>范式化设计还是反范式</h3><p>考虑下这样的场景，我们的订单数据是这样的</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">商品：</span><br><span class="line">&#123;</span><br><span class="line">  &quot;_id&quot;: productId,</span><br><span class="line">  &quot;name&quot;: name,</span><br><span class="line">  &quot;price&quot;: price,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">订单:</span><br><span class="line">&#123;</span><br><span class="line">  &quot;_id&quot;: orderId,</span><br><span class="line">  &quot;user&quot;: userId,</span><br><span class="line">  &quot;items&quot;: [</span><br><span class="line">    productId1,</span><br><span class="line">    productId2,</span><br><span class="line">    productId3</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><a id="more"></a><p>当我们查询订单内容的时候，先通过orderId查询订单，然后在通过订单信息中的productId查询到对应的商品信息。这种设计下一次查询无法获取完整的订单。</p><p>范式化结果就是读取速度比较忙，当所有订单的一致性会有保证。</p><p>在来看看反范式化设计</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">订单:</span><br><span class="line">&#123;</span><br><span class="line">  &quot;_id&quot;: orderId,</span><br><span class="line">  &quot;user&quot;: userId,</span><br><span class="line">  &quot;items&quot;: [</span><br><span class="line">   &#123;</span><br><span class="line">    &quot;_id&quot;: productId1,</span><br><span class="line">    &quot;name&quot;: name,</span><br><span class="line">    &quot;price&quot;: price,</span><br><span class="line">   &#125;,</span><br><span class="line">   &#123;</span><br><span class="line">    &quot;_id&quot;: productId2,</span><br><span class="line">    &quot;name&quot;: name,</span><br><span class="line">    &quot;price&quot;: price,</span><br><span class="line">   &#125;,</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里将商品信息作为内嵌文档存在订单数据中，这样当显示的时候就只需要一次查询就可以了。</p><p>反范式读取速度快，一致性稍弱，商品信息的变更不能原子性地更新到多个文档。</p><p>那么我们一般使用哪一个呢？我们在设计的时候要考虑以下问题</p><ol><li>读写比是怎样的？</li></ol><p>可能读取了商品信息一万次才修改一次它的详细信息，为了那一次写入快一点或者保证一致性，搭上一万次的读取消耗值得吗？还有你认为引用的数据多久会更新一次？更新越少，越适合反范式化。有些极少变化的数据几乎根本不值得引用。比如名字，性别，地址等。</p><ol start="2"><li>一致性重要吗？</li></ol><p>如果是肯定的，则应该范式化。</p><ol start="3"><li>要不要快速的读取？<br>如果想要读取尽可能快，则要反范式化。在这个引用中就无所谓了，所以不能算考量因素，实时的应用要尽可能地反范式化。</li></ol><p>订单文档非常适合反范式化，因为其中的商品信息不经常变化。就算变了也不必更新到所有订单。范式化再次就没有什么优势可言了。</p><p>所以本例中就是将订单反范式化。</p><h3 id="嵌入时间点数据"><a href="#嵌入时间点数据" class="headerlink" title="嵌入时间点数据"></a>嵌入时间点数据</h3><p>当一个商品打折或者换了图片，并不需要更改原来的订单中的信息。类似这种特定于某一时刻的时间点数据，都应该做嵌入处理。</p><p>在我们上面提到的订单文档中有一处也是这样，地址就属于时间点数据。若某人更新了个人信息，那么并不需要改变其以往的订单内容。</p><h3 id="千万不要嵌入不断增加的数据"><a href="#千万不要嵌入不断增加的数据" class="headerlink" title="千万不要嵌入不断增加的数据"></a>千万不要嵌入不断增加的数据</h3><p>MongoDB存储数据的机制决定了对数组不断追加数据是很低效的。在正常使用中数组和对象大小应该相对固定。</p><p>嵌入20，100，或者100000个子文档都不是问题，关键是提前这么做，之后基本保持不变。否则放任文档增长会使得系统慢的你受不了。</p><p>对于那些不断增加的内容，必须评论这个时候应该将其作为单独的文档处理比较合适。</p><h3 id="尽可能预先分配空间"><a href="#尽可能预先分配空间" class="headerlink" title="尽可能预先分配空间"></a>尽可能预先分配空间</h3><p>只要知道文档开始比较小，后来会变为确定的大小就可以使用这种优化方法，一开始插入文档的时候，就用和最终数据大小一样的垃圾数据填充，比如添加一个garbage字段(其中包含一个字符串，串大小与文档最终大小相同)，然后马上重置字段<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">db.collection.insert(&#123;&quot;_id&quot; : 1,/* other fields */, &quot;garbase&quot;: longString&#125;);</span><br><span class="line">db.collection.update(&#123;&quot;_id&quot; : 1, &#125;);</span><br></pre></td></tr></table></figure></p><p>这样，MongDB就会为文档今后的增长分配足够的空间</p><blockquote><p>mongodb中存储文档是预留了空间的，允许文档扩容，但是当文档增大到一定地步的时候，就会超过原本分配的空间，此时文档就会进行移动</p></blockquote><h3 id="用数组存放要匿名访问的内嵌数据"><a href="#用数组存放要匿名访问的内嵌数据" class="headerlink" title="用数组存放要匿名访问的内嵌数据"></a>用数组存放要匿名访问的内嵌数据</h3><p>一个常见的问题就是内嵌的信息到底是用数组还是用子文档存。如果确切知道要查询的内容，就要用子文档。如果有时候不太清楚查询的具体内容，就要用数组。当知道一些条目的查询条件时，通常该使用数组。</p><p>假设我想记录下游戏中某些物品的属性。我们可以这样建模</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;_id&quot;: 1,</span><br><span class="line">  &quot;items&quot; : &#123;</span><br><span class="line"></span><br><span class="line">    &quot;slingshot&quot;: &#123;</span><br><span class="line">      &quot;type&quot; : &quot;weapon&quot;,</span><br><span class="line">      &quot;damage&quot; : 30,</span><br><span class="line">      &quot;ranged&quot; : true</span><br><span class="line">    &#125;,</span><br><span class="line"></span><br><span class="line">    &quot;jar&quot; : &#123;</span><br><span class="line">      &quot;type&quot;: &quot;container&quot;,</span><br><span class="line">      &quot;contains&quot;: &quot;fairy&quot;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>假设要找出所有damage大于20的武器，子文档不支持这种查找方式，你只能知晓具体某种物品的信息才能查找，比如{“items.jar.damage”: {“$gt”:20}}.<br>如果无需标识符，就要用数组<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;_id&quot;: 1,</span><br><span class="line">  &quot;items&quot; : [</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">      &quot;id&quot; : &quot;slingshot&quot;</span><br><span class="line">      &quot;type&quot; : &quot;weapon&quot;,</span><br><span class="line">      &quot;damage&quot; : 30,</span><br><span class="line">      &quot;ranged&quot; : true</span><br><span class="line">    &#125;,</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">      &quot;id&quot; : &quot;jar&quot;,</span><br><span class="line">      &quot;type&quot;: &quot;container&quot;,</span><br><span class="line">      &quot;contains&quot;: &quot;fairy&quot;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>比如{”items.damage”:{“$gt”:20}}就行了。如果还需要多条件查询，可以使用$elemMatch.</p><h3 id="如何使用自增id代替ObjectId"><a href="#如何使用自增id代替ObjectId" class="headerlink" title="如何使用自增id代替ObjectId"></a>如何使用自增id代替ObjectId</h3><p>有时候在使用过程中受限于业务或者其他情况，并不想使用ObjectId,而是想要使用自动Id来代替。但是MongoDB本身并没有提供这个功能，那么如何实现呢？</p><p>可以新建一个collection来保存自增id</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;_id&quot; : ObjectId(&quot;59ed8d3df772d09a67eb25f6&quot;),</span><br><span class="line">    &quot;fieldName&quot; : &quot;user&quot;,</span><br><span class="line">    &quot;seq&quot; : NumberLong(100064)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>fieldName表示哪个集合,那么下次要使用的时候只用取出这个值加1就可以了。代码如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">public</span> Long <span class="title">getNextSequence</span><span class="params">(String fieldName, <span class="keyword">long</span> gap)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Query query = <span class="keyword">new</span> Query();</span><br><span class="line">        query.addCriteria(Criteria.where(<span class="string">"fieldName"</span>).is(fieldName));</span><br><span class="line"></span><br><span class="line">        Update update = <span class="keyword">new</span> Update();</span><br><span class="line">        update.inc(<span class="string">"seq"</span>, gap);</span><br><span class="line"></span><br><span class="line">        FindAndModifyOptions options = FindAndModifyOptions.options();</span><br><span class="line">        options.upsert(<span class="keyword">true</span>);</span><br><span class="line">        options.returnNew(<span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">        Counter counter = mongoTemplate.findAndModify(query, update, options, Counter.class);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (counter != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> counter.getSeq();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">        log.error(<span class="string">"Exception when getNextSequence from mongodb"</span>, t);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> gap;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="不要到处使用索引"><a href="#不要到处使用索引" class="headerlink" title="不要到处使用索引"></a>不要到处使用索引</h3><p>索引是很强大，但是要提醒你的是，不是所有查询都可以用索引的。比如你要返回集合中90%的文档而非获取一些记录，就不应该使用索引。</p><p>如果对这种查询用了索引，结果就是几乎遍历整个索引树，把其中一部分，比方说40GB的索引都加载到内存。然后按照索引中的指针加载集合中200GB的文档数据，最终将加载<br>200GB + 40GB = 240GB的数据，比不用索引还多。</p><p>所以索引一般用在返回结果只是总体数据的小部分的时候。根据经验，一旦要大约返回集合一般的数据就不要使用索引了。</p><p>若是已经对某个字段建立了索引，又想在大规模查询时不使用它(因为使用索引可能会较低效),可以使用自然排序来强制MongoDB禁用索引。自然排序就是“按照磁盘上的存储顺序返回数据”,这样MongDB就不会使用索引了。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">db.students.find().sort(&#123;&quot;$natural&quot; : 1&#125;);</span><br></pre></td></tr></table></figure><p>如果某个查询不用索引，MongoDB就会做全表扫描。</p><h3 id="索引覆盖查询"><a href="#索引覆盖查询" class="headerlink" title="索引覆盖查询"></a>索引覆盖查询</h3><p>如果你想要返回某些字段且这些字段都可以放到索引中，那么MongoDB可以做索引覆盖查询，这种查询不会访问指针指向的文档，而是直接用索引的数据返回结果，比如有如下索引</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">db.students.ensureIndex(x : 1, y : 1, z : 1);</span><br></pre></td></tr></table></figure><p>现在查询被索引的字段，并要求只返回这些字段，MongoDB就没有必要加载整个文档</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">db.students.find(&#123;&quot;x&quot; : &quot;xxx&quot;, &quot;y&quot; : &quot;xxx&quot;&#125;,&#123;x : 1, y : 1, z : 1, &quot;_id&quot; : 0&#125;);</span><br></pre></td></tr></table></figure><p>注意由于_id是默认返回的，而它又不是索引的一部分，所以MongoDB就需要到文档中获取_id,去掉它，就可以仅根据索引返回结果了。</p><p>若是查询值返回几个字段，则考虑将其放到索引中，即使不对他们执行查询，也能做索引覆盖查询。比如上面的字段z。</p><h3 id="AND查询要点"><a href="#AND查询要点" class="headerlink" title="AND查询要点"></a>AND查询要点</h3><p>假设要查询满足条件A、B、C的文档。满足A的文档有40000，满足B的有9000，满足C的有200，要是让MongoDB按照这个顺序查询，效率可不高。</p><p>如果把C放到最前，然后是B，然后是A，则针对B,C只需要查询最多200个文档。</p><p>这样工作量显著减少了。要是已知某个查询条件更加苛刻，则要将其放置到最前面。</p><h3 id="OR型查询要点"><a href="#OR型查询要点" class="headerlink" title="OR型查询要点"></a>OR型查询要点</h3><p>OR与AND查询相反，匹配最多的查询语句应该放到最前面，因为MongDB每次都要匹配不在结果集中的文档。</p><h3 id="单表查询尽量使用Respostory"><a href="#单表查询尽量使用Respostory" class="headerlink" title="单表查询尽量使用Respostory"></a>单表查询尽量使用Respostory</h3><p>开发中，对于简单的查询我一般使用MongoRepository来实现功能，如果有复杂的结合MongoTemplate，注意这两者是可以混合使用的。</p><h3 id="converter的建议"><a href="#converter的建议" class="headerlink" title="converter的建议"></a>converter的建议</h3><p>开发中我们要写对于一个collection,其中有些特殊的类型(比如枚举)需要我们写converter,大多时候是双向的，比如db–&gt;collection和collection–&gt;db<br>如果只有一个类型需要转换，我们可以针对这一个属性进行转换，比如下面的例子</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@WritingConverter</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserStatusToIntConverter</span> <span class="keyword">implements</span> <span class="title">Converter</span>&lt;<span class="title">UserStatus</span>, <span class="title">Integer</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">convert</span><span class="params">(UserStatus userStatus)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> userStatus.getStatus();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@ReadingConverter</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserStatusFromIntConverter</span> <span class="keyword">implements</span> <span class="title">Converter</span>&lt;<span class="title">Integer</span>, <span class="title">UserStatus</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> UserStatus <span class="title">convert</span><span class="params">(Integer source)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> UserStatus.findStatus(source);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一个字段还好，如果一个类中有很多个字段都需要做转换的话，就会产生很多个converter,这个时候我们可以写一个类级别的转换器</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@ReadingConverter</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OperateLogFromDbConverter</span> <span class="keyword">extends</span> <span class="title">AbstractReadingConverter</span>&lt;<span class="title">Document</span>, <span class="title">OperateLog</span>&gt; </span>&#123;</span><br><span class="line">  <span class="meta">@Override</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> OperateLog <span class="title">convert</span><span class="params">(Document source)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">      OperateLog opLog = convertBasicField(source);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (source.containsKey(<span class="string">"_id"</span>)) &#123;</span><br><span class="line">          opLog.setId(source.getLong(<span class="string">"_id"</span>));</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (source.containsKey(<span class="string">"module"</span>)) &#123;</span><br><span class="line"></span><br><span class="line">          opLog.setModule(ModuleEnum.findModule(source.getInteger(<span class="string">"module"</span>)));</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (source.containsKey(<span class="string">"opType"</span>)) &#123;</span><br><span class="line">          opLog.setOpType(OpTypeEnum.findOpType(source.getInteger(<span class="string">"opType"</span>)));</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">if</span> (source.containsKey(<span class="string">"level"</span>)) &#123;</span><br><span class="line">          opLog.setLevel(OpLevelEnum.findOpLevel(source.getInteger(<span class="string">"level"</span>)));</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">return</span> opLog;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">private</span> OperateLog <span class="title">convertBasicField</span><span class="params">(Document source)</span> </span>&#123;</span><br><span class="line">      Gson gson = <span class="keyword">new</span> Gson();</span><br><span class="line">      <span class="keyword">return</span> gson.fromJson(source.toJson(), OperateLog.class);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面代码我用了GSON做common field的转换，如果你不这样写，就需要判断每个字段，然后进行填充。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;范式化设计还是反范式&quot;&gt;&lt;a href=&quot;#范式化设计还是反范式&quot; class=&quot;headerlink&quot; title=&quot;范式化设计还是反范式&quot;&gt;&lt;/a&gt;范式化设计还是反范式&lt;/h3&gt;&lt;p&gt;考虑下这样的场景，我们的订单数据是这样的&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;8&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;9&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;10&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;11&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;12&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;13&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;14&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;15&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;16&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;17&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;商品：&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;_id&amp;quot;: productId,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;name&amp;quot;: name,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;price&amp;quot;: price,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;订单:&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;_id&amp;quot;: orderId,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;user&amp;quot;: userId,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &amp;quot;items&amp;quot;: [&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    productId1,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    productId2,&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    productId3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  ]&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="MongoDB" scheme="http://yoursite.com/tags/MongoDB/"/>
    
  </entry>
  
  <entry>
    <title>分析Java Object中的wait/notify</title>
    <link href="http://yoursite.com/2019/10/10/analysis-java-object-wait-notify/"/>
    <id>http://yoursite.com/2019/10/10/analysis-java-object-wait-notify/</id>
    <published>2019-10-10T03:11:59.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>在Java的Object类中有2个我们不怎么常用(框架中用的更多)的方法：wait()与notify()或notfiyAll()，这两个方法主要用于多线程间的协同处理，即控制线程之间的等待、通知、切换及唤醒。</p><p>首先了解下线程有哪几种状态,Java的<a href="https://docs.oracle.com/javase/7/docs/api/java/lang/Thread.State.html" target="_blank" rel="noopener">Thread.State</a>中定义了线程的6种状态,分别如下：</p><ol><li>NEW    未启动的,不会出现在dump文件中(可以通过jstack命令查看线程堆栈)</li><li>RUNNABLE    正在JVM中执行的</li><li>BLOCKED    被阻塞,等待获取监视器锁进入synchronized代码块或者在调用Object.wait之后重新进入synchronized代码块</li><li>WAITING    无限期等待另一个线程执行特定动作后唤醒它,也就是调用Object.wait后会等待拥有同一个监视器锁的线程调用notify/notifyAll来进行唤醒</li><li>TIMED_WAITING    有时限的等待另一个线程执行特定动作</li><li>TERMINATED    已经完成了执行</li></ol><a id="more"></a><blockquote><p>从操作系统层面上来讲，一个进程从创建到消亡期间，最常见的进程状态有以下几种<br>新建态 ： 从程序映像到进程映像的转变，还没有加入到就绪队列中<br>就绪态 : 进程运行已万事俱备，正等待调度执行<br>运行态 : 进程指令正在被执行<br>阻塞态 : 进程正在等待一个时间操作完成，例如I/O操作<br>完成态 : 进程运行结束，它的资源已经被释放，供其他活动进程使用</p></blockquote><p>接下来我们分析下面的代码</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WaitNotify</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    Object lock = <span class="keyword">new</span> Object();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// thread1</span></span><br><span class="line">    <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">"thread1 is ready"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">            lock.notify();</span><br><span class="line">            System.out.println(<span class="string">"thread1 is notify,but not exit synchronized"</span>);</span><br><span class="line">            System.out.println(<span class="string">"thread1 is exit synchronized"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;).start();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// thread2</span></span><br><span class="line">    <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">        System.out.println(<span class="string">"thread2 is ready"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"thread2 is waiting"</span>);</span><br><span class="line">                lock.wait();</span><br><span class="line">                System.out.println(<span class="string">"thread2 is awake"</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;).start();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面的代码运行结果如下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">thread1 is ready</span><br><span class="line">thread2 is ready</span><br><span class="line">thread2 is waiting</span><br><span class="line">thread1 is notify,but not exit synchronized</span><br><span class="line">thread 1 is exit synchronized</span><br><span class="line">thread2 is awake</span><br></pre></td></tr></table></figure><p>看到这里你会发现平平无奇，好像也没什么特殊的事情,但是如果深入分析下，就会发现以下的问题</p><ol><li>为何调用wait或者notify一定要加synchronized，不加行不行？</li><li>thread2中调用了wait后，当前线程还未退出同步代码块，其他线程(thread1)能进入同步块吗？</li><li>为何调用wait()有可能抛出InterruptedException异常</li><li>调用notify/notifyAll后等待中的线程会立刻唤醒吗？</li><li>调用notify/notifyAll是随机从等待线程队列中取一个或者按某种规律取一个来执行？</li><li>wait的线程是否会影响系统性能？</li></ol><p>针对上面的问题，我们逐个分析下</p><h4 id="为何调用wait或者notify一定要加synchronized，不加行不行？"><a href="#为何调用wait或者notify一定要加synchronized，不加行不行？" class="headerlink" title="为何调用wait或者notify一定要加synchronized，不加行不行？"></a>为何调用wait或者notify一定要加synchronized，不加行不行？</h4><p>如果你不加，你会得到下面的异常</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Exception in thread <span class="string">"main"</span> java.lang.IllegalMonitorStateException</span><br></pre></td></tr></table></figure><blockquote><p>在<a href="https://github.com/unofficial-openjdk/openjdk/blob/jdk8u%2Fjdk8u/hotspot/src/share/vm/runtime/objectMonitor.cpp#L1425" target="_blank" rel="noopener">JVM源代码中</a>首先会检查当前线程是否持有锁,如果没有持有则抛出异常</p></blockquote><p>其次为什么要加,也有比较广泛的讨论，首先wait/notify是为了线程间通信的，为了这个通信过程不被打断，需要保证wait/notify这个整体代码块的原子性，所以需要通过synchronized来加锁。</p><h4 id="thread2中调用了wait后，当前线程还未退出同步代码块，其他线程-thread1-能进入同步块吗？"><a href="#thread2中调用了wait后，当前线程还未退出同步代码块，其他线程-thread1-能进入同步块吗？" class="headerlink" title="thread2中调用了wait后，当前线程还未退出同步代码块，其他线程(thread1)能进入同步块吗？"></a>thread2中调用了wait后，当前线程还未退出同步代码块，其他线程(thread1)能进入同步块吗？</h4><p>wait在处理过程中会临时释放同步锁(如果不释放其他线程没有机会抢)，不过需要注意的是当其他线程调用notify唤起这个线程的时候，在wait方法退出之前会重新获取这把锁，只有获取了这把锁才会继续执行,这也和我们的结果相符合,输出了<code>thread2 is awake</code>,<br>其实想想也容易理解,synchronized的代码实际上是被被monitorenter和monitorexit包围起来的。当我们调用wait的时候，会释放锁,调用monitorexit的时候也会释放锁,那么当thread2被唤醒的时候必然重新获取到了锁(objectMonitor::enter)。</p><p>其实从jdk源代码的<a href="https://github.com/unofficial-openjdk/openjdk/blob/jdk8u%2Fjdk8u/hotspot/src/share/vm/runtime/objectMonitor.cpp#L1463" target="_blank" rel="noopener">ObjectMonitor::wait方法</a>可以一窥究竟，首先会放弃已经抢到的锁(exit(self)),而放弃锁的前提是获取到锁</p><p>而在notify方法中会选取一个线程获得cpu执行权，在去竞争锁，如果没有竞争到则会进入休眠。</p><p>如果调用的wait(200)这种代码,那么会在200ms后将线程从waiting set中移除并允许其重新竞争锁，需要注意的是notify方法并不会释放所持有的monitor</p><h4 id="为何调用wait-有可能抛出InterruptedException异常"><a href="#为何调用wait-有可能抛出InterruptedException异常" class="headerlink" title="为何调用wait()有可能抛出InterruptedException异常"></a>为何调用wait()有可能抛出InterruptedException异常</h4><p>当我们调用了某个线程的interrupt方法，对应的线程会抛出这个异常，wait方法也不希望去破坏这种规则，因此就算当前线程因为wait一直在阻塞。当某个线程希望它起来继续执行的时候，它还是得从阻塞态恢复过来，因此wait方法被唤醒起来的时候会去检测这个状态，当有线程interrupt了它的时候，它就会抛出这个异常从阻塞状态恢复过来。</p><p>这里有两点要注意：</p><ol><li>如果被interrupt的线程只是创建了，并没有start，那等他start之后进入wait态之后也是不能会恢复的</li><li>如果被interrupt的线程已经start了，在进入wait之前，如果有线程调用了其interrupt方法，那这个wait等于什么都没做，会直接跳出来，不会阻塞,下面的代码演示了这种情况</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">Thread thread2 = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">    System.out.println(<span class="string">"thread2 is ready"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">"thread2 is waiting"</span>);</span><br><span class="line">            lock.wait();</span><br><span class="line">            System.out.println(<span class="string">"thread2 is awake"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            System.out.println(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// main</span></span><br><span class="line">  thread2.start();</span><br><span class="line"></span><br><span class="line">  Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">  thread2.interrupt();</span><br></pre></td></tr></table></figure><p>上面的代码输出结果如下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">thread2 is ready</span><br><span class="line">thread2 is waiting</span><br><span class="line">java.lang.InterruptedException</span><br></pre></td></tr></table></figure><h4 id="调用notify-notifyAll后等待中的线程会立刻唤醒吗？"><a href="#调用notify-notifyAll后等待中的线程会立刻唤醒吗？" class="headerlink" title="调用notify/notifyAll后等待中的线程会立刻唤醒吗？"></a>调用notify/notifyAll后等待中的线程会立刻唤醒吗？</h4><p>hotspot真正的实现是退出同步代码块的时候才会去真正唤醒对应的线程，不过这个也是个默认策略，也可以改的，在notify之后立马唤醒相关线程。<br>这个也可从jdk源代码的objectMonitor类objectMonitor::notify方法中看到.在调用notify时的默认策略是Policy == 2（这个值是源码中的初值，可以通过-XX:SyncKnobs来设置）</p><p>其实对于Policy(1、2、3、4)都是将objectMonitor的ObjectWaiter集合中取出一个等待线程，放入到_EntryList（blocked线程集合，可以参与下次抢锁），只是放入_EntryList的策略不一样，体现为唤醒wait线程的规则不一样。</p><p>对于默认策略notify在将一个等待线程放入阻塞线程集合之后就退出，因为同步块还没有执行完monitorexit，锁其实还未释放，所以在打印出“thread1 is exit synchronized!”的时候，thread2线程还是blocked状态（因为thread1还没有退出同步块）。</p><p>这里可以发现，对于不在Policy中的情况，会直接将一个ObjectWaiter进行unpark唤醒操作，但是被唤醒的线程是否立即获取到了锁呢？答案是否定的。</p><h4 id="调用notify-notifyAll是随机从等待线程队列中取一个或者按某种规律取一个来执行？"><a href="#调用notify-notifyAll是随机从等待线程队列中取一个或者按某种规律取一个来执行？" class="headerlink" title="调用notify/notifyAll是随机从等待线程队列中取一个或者按某种规律取一个来执行？"></a>调用notify/notifyAll是随机从等待线程队列中取一个或者按某种规律取一个来执行？</h4><p>我们自己实现可能一个for循环就搞定了，不过在jvm里实现没这么简单，而是借助了monitor_exit，上面我提到了当某个线程从wait状态恢复出来的时候，要先获取锁，然后再退出同步块，所以notifyAll的实现是调用notify的线程在退出其同步块的时候唤醒起最后一个进入wait状态的线程，然后这个线程退出同步块的时候继续唤醒其倒数第二个进入wait状态的线程，依次类推，同样这是一个策略的问题，jvm里提供了挨个直接唤醒线程的参数,这里要分情况：</p><ol><li>如果是通过notify来唤起的线程，那先进入wait的线程会先被唤起来</li><li>如果是通过nootifyAll唤起的线程，默认情况是最后进入的会先被唤起来，即LIFO的策略</li></ol><h4 id="wait的线程是否会影响系统性能？"><a href="#wait的线程是否会影响系统性能？" class="headerlink" title="wait的线程是否会影响系统性能？"></a>wait的线程是否会影响系统性能？</h4><p>这个或许是大家比较关心的话题，因为关乎系统性能问题，wait/nofity是通过jvm里的park/unpark机制来实现的，在linux下这种机制又是通过pthread_cond_wait/pthread_cond_signal来玩的，因此当线程进入到wait状态的时候其实是会放弃cpu的，也就是说这类线程是不会占用cpu资源，也不会影响系统加载。</p><h4 id="什么是监视器-monitor"><a href="#什么是监视器-monitor" class="headerlink" title="什么是监视器(monitor)"></a>什么是监视器(monitor)</h4><p>Java中每一个对象都可以成为一个监视器（Monitor）, 该Monitor由一个锁（lock）, 一个等待队列（waiting queue ）, 一个入口队列( entry queue)组成.<br>对于一个对象的方法， 如果没有synchonized关键字， 该方法可以被任意数量的线程，在任意时刻调用。<br>对于添加了synchronized关键字的方法，任意时刻只能被唯一的一个获得了对象实例锁的线程调用。</p><p><img src="/images/java_monitor.bmp" alt="java监视器"></p><p><strong>进入区(Entry Set):</strong> 表示线程通过 synchronized要求获得对象锁，如果获取到了，则成为拥有者，如果没有获取到在在进入区等待，直到其他线程释放锁之后再去竞争(谁获取到则根据)</p><p><strong>拥有者(Owner):</strong> 表示线程获取到了对象锁，可以执行synchronized包围的代码了</p><p><strong>等待区(Wait Set):</strong> 表示线程调用了wait方法,此时释放了持有的对象锁，等待被唤醒(谁被唤醒取得监视器锁由jvm决定)</p><h4 id="关于sleep"><a href="#关于sleep" class="headerlink" title="关于sleep"></a>关于sleep</h4><p>它是一个静态方法，一般的调用方式是Thread.sleep(2000),表示让当前线程休眠2000ms,并不会让出监视器，这一点需要注意。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在Java的Object类中有2个我们不怎么常用(框架中用的更多)的方法：wait()与notify()或notfiyAll()，这两个方法主要用于多线程间的协同处理，即控制线程之间的等待、通知、切换及唤醒。&lt;/p&gt;
&lt;p&gt;首先了解下线程有哪几种状态,Java的&lt;a href=&quot;https://docs.oracle.com/javase/7/docs/api/java/lang/Thread.State.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Thread.State&lt;/a&gt;中定义了线程的6种状态,分别如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;NEW    未启动的,不会出现在dump文件中(可以通过jstack命令查看线程堆栈)&lt;/li&gt;
&lt;li&gt;RUNNABLE    正在JVM中执行的&lt;/li&gt;
&lt;li&gt;BLOCKED    被阻塞,等待获取监视器锁进入synchronized代码块或者在调用Object.wait之后重新进入synchronized代码块&lt;/li&gt;
&lt;li&gt;WAITING    无限期等待另一个线程执行特定动作后唤醒它,也就是调用Object.wait后会等待拥有同一个监视器锁的线程调用notify/notifyAll来进行唤醒&lt;/li&gt;
&lt;li&gt;TIMED_WAITING    有时限的等待另一个线程执行特定动作&lt;/li&gt;
&lt;li&gt;TERMINATED    已经完成了执行&lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
    
      <category term="Java" scheme="http://yoursite.com/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>MongoDB初学者最常用的10个命令</title>
    <link href="http://yoursite.com/2019/09/26/top-10-common-mongodb-commands-for-beginners/"/>
    <id>http://yoursite.com/2019/09/26/top-10-common-mongodb-commands-for-beginners/</id>
    <published>2019-09-26T06:23:03.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<h4 id="1-登录mongodb"><a href="#1-登录mongodb" class="headerlink" title="1. 登录mongodb"></a>1. 登录mongodb</h4><p>以下命令可以用于登录mongodb数据库，但是需要保证用户你声明的数据库中存在对应的用户和密码<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mongo --host &lt;hostName&gt; --port &lt;port&gt; -u &lt;username&gt; -p &lt;password&gt; --authenticationDatabase &lt;dbname&gt;</span><br><span class="line"></span><br><span class="line">mongo --host 192.168.140.11 -u test -p 123456 --authenticationDatabase test_db</span><br></pre></td></tr></table></figure></p><a id="more"></a><h4 id="2-列出所有的数据库"><a href="#2-列出所有的数据库" class="headerlink" title="2. 列出所有的数据库"></a>2. 列出所有的数据库</h4><p>  当你以适当角色的用户身份登录后，可以使用以下命令查看所有数据库</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">show dbs</span><br></pre></td></tr></table></figure><h4 id="3-选择要使用的数据库"><a href="#3-选择要使用的数据库" class="headerlink" title="3. 选择要使用的数据库"></a>3. 选择要使用的数据库</h4><p>要开始使用特定的数据库，可以用以下命令</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">use &lt;databaseName&gt;</span><br></pre></td></tr></table></figure><h4 id="4-创建用户"><a href="#4-创建用户" class="headerlink" title="4. 创建用户"></a>4. 创建用户</h4><p>  当你想让不同的用户拥有不同的权限的时候可以使用以下命令</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">use &lt;databaseName&gt;</span><br><span class="line">db.createUser(&#123; user: &apos;&lt;username&gt;&apos;, pwd: &apos;&lt;password&gt;&apos;, roles: [ &#123; role: &quot;readWrite&quot;, db: &quot;&lt;databaseName&gt;&quot; &#125; ] &#125;);</span><br><span class="line"></span><br><span class="line">例子:</span><br><span class="line">use admin</span><br><span class="line">db.createUser(&#123; user: &apos;admin&apos;, pwd: &apos;123456&apos;, roles: [ &#123; role: &quot;userAdminAnyDatabase&quot;, db: &quot;admin&quot; &#125; ] &#125;);</span><br></pre></td></tr></table></figure><h4 id="5-列出所有的集合，用户以及角色"><a href="#5-列出所有的集合，用户以及角色" class="headerlink" title="5. 列出所有的集合，用户以及角色"></a>5. 列出所有的集合，用户以及角色</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">// 列出当前database下所有的集合:</span><br><span class="line">show collections;</span><br><span class="line">db.getCollectionNames();</span><br><span class="line"></span><br><span class="line">// 列出当前database下所有的用户</span><br><span class="line">show users;</span><br><span class="line">db.getUsers();</span><br><span class="line"></span><br><span class="line">// 列出当前dababase下所有角色</span><br><span class="line">show roles;</span><br></pre></td></tr></table></figure><p>不同的角色对应的权限，最直接之处就在于没有权限有些命令就无法执行</p><h4 id="6-创建集合"><a href="#6-创建集合" class="headerlink" title="6. 创建集合"></a>6. 创建集合</h4><p>  下面的命令用户创建集合，更详细命令可以查看<a href="https://docs.mongodb.com/manual/reference/method/db.createCollection/" target="_blank" rel="noopener">官方文档</a></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">db.createCollection(&quot;collectionName&quot;);</span><br></pre></td></tr></table></figure><h4 id="7-将文档插入到集合中"><a href="#7-将文档插入到集合中" class="headerlink" title="7. 将文档插入到集合中"></a>7. 将文档插入到集合中</h4><p>  集合一旦创建之后，下一步就是创建一个或多个文档插入到集合中</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 插入单个文档</span><br><span class="line">db.&lt;collectionName&gt;.insert(&#123;field1: &quot;value&quot;, field2: &quot;value&quot;&#125;)</span><br><span class="line"></span><br><span class="line">// 插入多个文档</span><br><span class="line">db.&lt;collectionName&gt;.insert([&#123;field1: &quot;value1&quot;&#125;, &#123;field1: &quot;value2&quot;&#125;])</span><br><span class="line">db.&lt;collectionName&gt;.insertMany([&#123;field1: &quot;value1&quot;&#125;, &#123;field1: &quot;value2&quot;&#125;])</span><br></pre></td></tr></table></figure><h4 id="8-保存或者更新文档"><a href="#8-保存或者更新文档" class="headerlink" title="8. 保存或者更新文档"></a>8. 保存或者更新文档</h4><p>  保存命令可用于更新现有文档或根据传递给它的文档参数插入新文档。如果传递的_id与现有文档匹配，则文档将更新。否则，将创建一个新文档。在内部，保存方法使用插入或更新命令。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">db.&lt;collectionName&gt;.save(&#123;&quot;_id&quot;: new ObjectId(&quot;123456&quot;), field1: &quot;value&quot;, field2: &quot;value&quot;&#125;);</span><br></pre></td></tr></table></figure><h4 id="9-显示集合记录"><a href="#9-显示集合记录" class="headerlink" title="9. 显示集合记录"></a>9. 显示集合记录</h4><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">// 获取所有记录</span><br><span class="line">db.&lt;collectionName&gt;.find();</span><br><span class="line"></span><br><span class="line">// 获取指定数量的记录</span><br><span class="line">db.&lt;collectionName&gt;.find().limit(10);</span><br><span class="line"></span><br><span class="line">// 根据id获取记录</span><br><span class="line">db.&lt;collectionName&gt;.find(&#123;&quot;_id&quot;: yourId&#125;)</span><br><span class="line"></span><br><span class="line">// 返回记录中特定field的值</span><br><span class="line">// 类似返回select field1,field2 from table</span><br><span class="line">db.&lt;collectionName&gt;.find(&#123;&quot;_id&quot;: ObjectId(&quot;someid&quot;)&#125;, &#123;field1: 1, field2: 1&#125;);</span><br><span class="line">// 不返回field1的数据</span><br><span class="line">db.&lt;collectionName&gt;.find(&#123;&quot;_id&quot;: ObjectId(&quot;someid&quot;)&#125;, &#123;field1: 0&#125;);</span><br><span class="line"></span><br><span class="line">// 文档记录数</span><br><span class="line">db.&lt;collectionName&gt;.count();</span><br></pre></td></tr></table></figure><h4 id="10-管理命令"><a href="#10-管理命令" class="headerlink" title="10. 管理命令"></a>10. 管理命令</h4><p>以下是一些管理命令，这些命令可能有助于查找集合详细信息，例如存储大小，总大小和总体统计信息</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">// 获取集合的统计信息,比如空间占用，总大小，引擎信息等</span><br><span class="line">db.&lt;collectionName&gt;.stats()</span><br><span class="line">db.printCollectionStats()</span><br><span class="line"></span><br><span class="line">//获取集合的延迟统计信息,比如读写的次数,时间等等</span><br><span class="line">db.&lt;collectionName&gt;.latencyStats()</span><br><span class="line"></span><br><span class="line">// 获取数据和索引的集合大小</span><br><span class="line">// 集合的大小</span><br><span class="line">db.&lt;collectionName&gt;.dataSize()</span><br><span class="line">// 集合中存储文档的总大小</span><br><span class="line">db.&lt;collectionName&gt;.storageSize()</span><br><span class="line">// 集合数据和索引的总大小(以字节为单位)</span><br><span class="line">db.&lt;collectionName&gt;.totalSize()</span><br><span class="line">// 集合中所有索引的总大小</span><br><span class="line">db.&lt;collectionName&gt;.totalIndexSize()</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;1-登录mongodb&quot;&gt;&lt;a href=&quot;#1-登录mongodb&quot; class=&quot;headerlink&quot; title=&quot;1. 登录mongodb&quot;&gt;&lt;/a&gt;1. 登录mongodb&lt;/h4&gt;&lt;p&gt;以下命令可以用于登录mongodb数据库，但是需要保证用户你声明的数据库中存在对应的用户和密码&lt;br&gt;&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;mongo --host &amp;lt;hostName&amp;gt; --port &amp;lt;port&amp;gt; -u &amp;lt;username&amp;gt; -p &amp;lt;password&amp;gt; --authenticationDatabase &amp;lt;dbname&amp;gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;mongo --host 192.168.140.11 -u test -p 123456 --authenticationDatabase test_db&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="mongodb" scheme="http://yoursite.com/categories/mongodb/"/>
    
    
      <category term="mongodb" scheme="http://yoursite.com/tags/mongodb/"/>
    
  </entry>
  
  <entry>
    <title>RESTful资源命名最佳实践</title>
    <link href="http://yoursite.com/2019/09/20/restful-resource-naming-practise/"/>
    <id>http://yoursite.com/2019/09/20/restful-resource-naming-practise/</id>
    <published>2019-09-20T04:08:37.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>在Rest中，数据的呈现方式叫做资源(Resource)。拥有强大而一致的REST资源命名策略,是最好的设计决策。</p><p>一个资源可以是单个的也可以是一个集合。比如customers是一个集合，而customer是单个资源。我们可以定义customers这个集合的资源的URI是<code>/customers</code>,而单个customer资源的URI是<code>/customers/{customerId}</code>。</p><p>资源也可以包含子集合的资源。比如，使用<code>/customers/{customerId}/accounts</code> 来表示某个customer下的account集合资源。同样的，对于account集合资源下的单个account我们可以定义成这样:<code>/customers/{customerId}/accounts/{accountId}</code></p><p>REST API使用统一资源标识符（URI）来寻址资源。REST API设计者应该创建URI，将REST API的资源模型传达给潜在的客户端开发人员。当资源命名良好时，API直观且易于使用。如果做得不好，那么相同的API会感觉难以使用和理解。</p><a id="more"></a><h3 id="使用名词来表示资源"><a href="#使用名词来表示资源" class="headerlink" title="使用名词来表示资源"></a>使用名词来表示资源</h3><p>RESTful URI应该引用作为事物（名词）的资源而不是引用动作（动词），因为名词具有动词不具有的属性 - 类似于具有属性的资源。资源的一些示例是：</p><ol><li>系统的用户</li><li>用户账户(银行的场景): </li><li>网络设备</li></ol><p>它们的资源URI可以被设计成下面这样:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/device-management/managed-devices </span><br><span class="line">http://api.example.com/device-management/managed-devices/&#123;device-id&#125; </span><br><span class="line">http://api.example.com/user-management/users/</span><br><span class="line">http://api.example.com/user-management/users/&#123;id&#125;</span><br></pre></td></tr></table></figure><p>为了更好的说明我们把资源原型分为四个种类(document,collection,store 以及 controller),你应该总是把资源放到其中一个原型中，并且遵守它的统一命名。</p><h4 id="document"><a href="#document" class="headerlink" title="document"></a>document</h4><p>文档资源是一种类似于对象实例或数据库记录的单一概念(比如mysql中的一行记录,Mongodb中的document),在REST中，你可以将其视为资源集合中的单个资源。文档的状态表示通常包括具有值的字段和指向其他相关资源的链接。</p><p>使用单数名称表示文档资源原型</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/device-management/managed-devices/&#123;device-id&#125;</span><br><span class="line">http://api.example.com/user-management/users/&#123;id&#125;</span><br><span class="line">http://api.example.com/user-management/users/admin</span><br></pre></td></tr></table></figure><h4 id="collection"><a href="#collection" class="headerlink" title="collection"></a>collection</h4><p>集合资源是服务端管理的资源目录。客户可以建议将新资源添加到集合中。但是，要由集合选择是否创建新资源。集合资源选择它想要包含的内容，并决定每个包含的资源的URI。</p><p>使用复数名称表示集合资源原型</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/device-management/managed-devices</span><br><span class="line">http://api.example.com/user-management/users</span><br><span class="line">http://api.example.com/user-management/users/&#123;id&#125;/accounts</span><br></pre></td></tr></table></figure><h4 id="store"><a href="#store" class="headerlink" title="store"></a>store</h4><p>store是<strong>客户端管理的资源库</strong>,store资源允许API客户端放入资源，取出资源，并决定何时删除它们。store永远不会生成新的URI。相反，每个存储的资源都有一个客户端在最初放入存储时选择的URI。</p><p>使用复数名称表示store资源原型</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/cart-management/users/&#123;id&#125;/carts</span><br><span class="line">http://api.example.com/song-management/users/&#123;id&#125;/playlists</span><br></pre></td></tr></table></figure><h4 id="controller"><a href="#controller" class="headerlink" title="controller"></a>controller</h4><p>controller资源有点像程序的概念,controller资源就像可执行函数，带有参数和返回值;输入和输出。</p><p>使用动词表示controller原型</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 查看用户的信用卡</span><br><span class="line">http://api.example.com/cart-management/users/&#123;id&#125;/cart/checkout</span><br><span class="line"></span><br><span class="line">// 播放整个播放列表</span><br><span class="line">http://api.example.com/song-management/users/&#123;id&#125;/playlist/play</span><br></pre></td></tr></table></figure><p>这里的controller为什么要用动词呢?其实大家可以想象下Spring中Controller做了什么事情,它调用了service组合成各个业务逻辑,将数据组合起来之后进行返回.</p><h3 id="一致性"><a href="#一致性" class="headerlink" title="一致性"></a>一致性</h3><p>使用一致的资源命名约定和URI格式，以最小化和最大可读性和可维护性。你可以实现以下设计提示以实现一致性：</p><h4 id="使用正斜杠（-）表示层次关系"><a href="#使用正斜杠（-）表示层次关系" class="headerlink" title="使用正斜杠（/）表示层次关系"></a>使用正斜杠（/）表示层次关系</h4><p>正斜杠（/）字符用于URI的路径部分，以指示资源之间的层次关系</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/device-management</span><br><span class="line">http://api.example.com/device-management/managed-devices</span><br><span class="line">http://api.example.com/device-management/managed-devices/&#123;id&#125;</span><br><span class="line">http://api.example.com/device-management/managed-devices/&#123;id&#125;/scripts</span><br><span class="line">http://api.example.com/device-management/managed-devices/&#123;id&#125;/scripts/&#123;id&#125;</span><br></pre></td></tr></table></figure><h4 id="不要在URI中使用尾部正斜杠（-）"><a href="#不要在URI中使用尾部正斜杠（-）" class="headerlink" title="不要在URI中使用尾部正斜杠（/）"></a>不要在URI中使用尾部正斜杠（/）</h4><p>作为URI路径中的最后一个字符，正斜杠（/）不会添加语义值，并可能导致混淆。最好完全放弃它们</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/device-management/managed-devices/</span><br><span class="line"></span><br><span class="line">/*这个版本更好*/</span><br><span class="line">http://api.example.com/device-management/managed-devices</span><br></pre></td></tr></table></figure><h4 id="使用连字符（-）来提高URI的可读性"><a href="#使用连字符（-）来提高URI的可读性" class="headerlink" title="使用连字符（ - ）来提高URI的可读性"></a>使用连字符（ - ）来提高URI的可读性</h4><p>要使你的URI易于扫描和解释，请使用连字符（ - ）字符来提高长路径段中名称的可读性。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 更好可读性</span><br><span class="line">http://api.example.com/inventory-management/managed-entities/&#123;id&#125;/install-script-location</span><br><span class="line"></span><br><span class="line">// 可读性不够高</span><br><span class="line">http://api.example.com/inventory-management/managedEntities/&#123;id&#125;/installScriptLocation</span><br></pre></td></tr></table></figure><h4 id="不用使用下滑线"><a href="#不用使用下滑线" class="headerlink" title="不用使用下滑线 _"></a>不用使用下滑线 _</h4><p>可以使用下划线代替连字符作为分隔符 - 但是根据应用程序的字体，下划线 _ 字符可能会在某些浏览器或屏幕中被部分遮挡或完全隐藏。为避免这种混淆，请使用连字符 - 而不是下划线 _。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 更具可读性</span><br><span class="line">http://api.example.com/inventory-management/managed-entities/&#123;id&#125;/install-script-location</span><br><span class="line"></span><br><span class="line">// 更容易出错</span><br><span class="line">http://api.example.com/inventory_management/managed_entities/&#123;id&#125;/install_script_location</span><br></pre></td></tr></table></figure><h4 id="在URI中使用小写字母"><a href="#在URI中使用小写字母" class="headerlink" title="在URI中使用小写字母"></a>在URI中使用小写字母</h4><p>方便时，URI路径中应始终首选小写字母。</p><p><a href="http://www.rfc-base.org/txt/rfc-3986.txt" target="_blank" rel="noopener">RFC 3986</a>将URI定义为区分大小写，但协议和host除外</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 1</span><br><span class="line">http://api.example.org/my-folder/my-doc</span><br><span class="line">// 2</span><br><span class="line">HTTP://API.EXAMPLE.ORG/my-folder/my-doc</span><br><span class="line">// 3</span><br><span class="line">http://api.example.org/My-Folder/my-doc</span><br></pre></td></tr></table></figure><p>在上面的例子中，1和2是相同的，但3不是,因为它使用大写字母的My-Folder。</p><h4 id="不要使用文件扩展名"><a href="#不要使用文件扩展名" class="headerlink" title="不要使用文件扩展名"></a>不要使用文件扩展名</h4><p>文件扩展名看起来很糟糕，不会增加任何优势。删除它们也会减少URI的长度。没理由保留它们。除了上述原因，如果你想使用文件扩展突出显示API的媒体类型，那么你应该依赖于通过Content-Type标头传达的媒体类型来确定如何处理正文的内容。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 不要这样用</span><br><span class="line">http://api.example.com/device-management/managed-devices.xml</span><br><span class="line"></span><br><span class="line">// 正确的URI</span><br><span class="line">http://api.example.com/device-management/managed-devices</span><br></pre></td></tr></table></figure><h3 id="切勿在URI中使用CRUD函数名称"><a href="#切勿在URI中使用CRUD函数名称" class="headerlink" title="切勿在URI中使用CRUD函数名称"></a>切勿在URI中使用CRUD函数名称</h3><p>URI不应用于指示执行CRUD功能。URI应该用于唯一标识资源，而不是对它们的任何操作。应使用HTTP请求方法来指示执行哪个CRUD功能。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">// 获取所有设备</span><br><span class="line">HTTP GET http://api.example.com/device-management/managed-devices</span><br><span class="line">// 创建新设备</span><br><span class="line">HTTP POST http://api.example.com/device-management/managed-devices</span><br><span class="line"></span><br><span class="line">// 根据给定id获取设备</span><br><span class="line">HTTP GET http://api.example.com/device-management/managed-devices/&#123;id&#125;</span><br><span class="line"></span><br><span class="line">// 根据给定id更新设备</span><br><span class="line">HTTP PUT http://api.example.com/device-management/managed-devices/&#123;id&#125;</span><br><span class="line"></span><br><span class="line">// 根据给定id删除设备</span><br><span class="line">HTTP DELETE http://api.example.com/device-management/managed-devices/&#123;id&#125;</span><br></pre></td></tr></table></figure><h3 id="使用查询组件过滤URI集合"><a href="#使用查询组件过滤URI集合" class="headerlink" title="使用查询组件过滤URI集合"></a>使用查询组件过滤URI集合</h3><p>很多时候，你会遇到需要根据某些特定资源属性对需要排序，过滤或限制的资源集合的要求。为此，不要创建新的API  - 而是在资源集合API中启用排序，过滤和分页功能，并将输入参数作为查询参数传递</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">http://api.example.com/device-management/managed-devices</span><br><span class="line">http://api.example.com/device-management/managed-devices?region=USA</span><br><span class="line">http://api.example.com/device-management/managed-devices?region=USA&amp;brand=XYZ</span><br><span class="line">http://api.example.com/device-management/managed-devices?region=USA&amp;brand=XYZ&amp;sort=installation-date</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在Rest中，数据的呈现方式叫做资源(Resource)。拥有强大而一致的REST资源命名策略,是最好的设计决策。&lt;/p&gt;
&lt;p&gt;一个资源可以是单个的也可以是一个集合。比如customers是一个集合，而customer是单个资源。我们可以定义customers这个集合的资源的URI是&lt;code&gt;/customers&lt;/code&gt;,而单个customer资源的URI是&lt;code&gt;/customers/{customerId}&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;资源也可以包含子集合的资源。比如，使用&lt;code&gt;/customers/{customerId}/accounts&lt;/code&gt; 来表示某个customer下的account集合资源。同样的，对于account集合资源下的单个account我们可以定义成这样:&lt;code&gt;/customers/{customerId}/accounts/{accountId}&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;REST API使用统一资源标识符（URI）来寻址资源。REST API设计者应该创建URI，将REST API的资源模型传达给潜在的客户端开发人员。当资源命名良好时，API直观且易于使用。如果做得不好，那么相同的API会感觉难以使用和理解。&lt;/p&gt;
    
    </summary>
    
    
      <category term="RESTful" scheme="http://yoursite.com/tags/RESTful/"/>
    
  </entry>
  
  <entry>
    <title>揭秘,门面日志如何自动发现日志组件</title>
    <link href="http://yoursite.com/2019/09/06/log-framework-search-princeple/"/>
    <id>http://yoursite.com/2019/09/06/log-framework-search-princeple/</id>
    <published>2019-09-06T08:20:00.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<h3 id="commons-logging"><a href="#commons-logging" class="headerlink" title="commons-logging"></a>commons-logging</h3><p>commons-logging是apache提供的一个通用的日志接口，是为了避免和具体的日志方案直接耦合的一种实现。通过commons-logging用户可以自己选择log4j或者jdk自带的logging作为具体实现。</p><p>使用commons-logging的代码如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.apache.commons.logging.Log;</span><br><span class="line"><span class="keyword">import</span> org.apache.commons.logging.LogFactory;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyApplication</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> Log log = LogFactory.getLog(<span class="keyword">this</span>.getClass());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><a id="more"></a><p>Log是一个接口，LogFactory的内部会去装载具体的日志系统，并获得实现该Log接口的实现类，其具体流程如下</p><ol><li>通过系统属性org.apache.commons.logging.LogFactory加载LogFactory的实现</li><li>如果未找到，则通过服务发现机制(SPI)在META-INF/services/org.apache.commons.logging.LogFactory寻找实现类进行加载</li><li>如果未找到，则从classpath下寻找commons-logging.properties文件，根据文件中org.apache.commons.logging.LogFactory配置进行加载</li><li>如果仍未找到,则使用默认配置：如果找到Log4j 则默认使用log4j 实现，如果仍没有则使用JDK14Logger 实现，再没有则使用commons-logging 内部提供的SimpleLog 实现</li></ol><p>所以只要你引入了log4j的jar包以及对其进行了配置底层就会直接使用log4j来进行日志输出了,其实质就是在org.apache.commons.logging.impl.Log4JLogger(commons-logging包)的getLogger方法调用了log4j的Logger.getLogger来返回底层的Logger,当记录日志的时候就会通过这个Logger写日志。</p><h3 id="Slf4j"><a href="#Slf4j" class="headerlink" title="Slf4j"></a>Slf4j</h3><p>slf4j全称为Simple Logging Facade for JAVA，java简单日志门面。其使用方式如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.slf4j.Logger;</span><br><span class="line"><span class="keyword">import</span> org.slf4j.LoggerFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyApplication</span> </span>&#123;</span><br><span class="line">  <span class="keyword">private</span> Logger logger = LoggerFactory.getLogger(<span class="keyword">this</span>.getClass());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>slf4j和commons-logging不一样的是，如果你没有具体的日志组件(logback,log4j等)，它是无法打印日志的，如果你在一个maven项目里面只引入slf4j的jar包，然后记录日志</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">public</span> <span class="keyword">static</span> Logger logger = LoggerFactory.getLogger(Main.class);</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">      logger.info(<span class="string">"slf4j"</span>);</span><br><span class="line">      System.out.println(<span class="string">"done"</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>就会出现下面的信息<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">SLF4J: Failed to load class &quot;org.slf4j.impl.StaticLoggerBinder&quot;.</span><br><span class="line">SLF4J: Defaulting to no-operation (NOP) logger implementation</span><br><span class="line">SLF4J: See http://www.slf4j.org/codes.html#StaticLoggerBinder for further details.</span><br></pre></td></tr></table></figure></p><p>如果你此时引入log4j或者logback的jar包就会打印出日志。</p><p>通过LoggerFactory.getLogger()查看其实现原理，其加载Logger核心源码如下:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> Set&lt;URL&gt; <span class="title">findPossibleStaticLoggerBinderPathSet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">  Set&lt;URL&gt; staticLoggerBinderPathSet = <span class="keyword">new</span> LinkedHashSet&lt;URL&gt;();</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">      ClassLoader loggerFactoryClassLoader = LoggerFactory.class.getClassLoader();</span><br><span class="line">      Enumeration&lt;URL&gt; paths;</span><br><span class="line">      <span class="keyword">if</span> (loggerFactoryClassLoader == <span class="keyword">null</span>) &#123;</span><br><span class="line">          paths = ClassLoader.getSystemResources(<span class="string">"org/slf4j/impl/StaticLoggerBinder.class"</span>);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          paths = loggerFactoryClassLoader.getResources(<span class="string">"org/slf4j/impl/StaticLoggerBinder.class"</span>);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">while</span> (paths.hasMoreElements()) &#123;</span><br><span class="line">          URL path = paths.nextElement();</span><br><span class="line">          staticLoggerBinderPathSet.add(path);</span><br><span class="line">      &#125;</span><br><span class="line">  &#125; <span class="keyword">catch</span> (IOException ioe) &#123;</span><br><span class="line">      Util.report(<span class="string">"Error getting resources from path"</span>, ioe);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> staticLoggerBinderPathSet;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其主要思想就是去classpath下找org/slf4j/impl/StaticLoggerBinder.class,即所有slf4j的实现,无论是log4j还是logback都有实现类，同学们可以看下自己项目中的jar包。</p><p>如果引入了多个实现，编译器会在编译的时候选择其中一个实现类进行绑定,也会将具体绑定的哪个日志框架告诉你</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">reportActualBinding</span><span class="params">(Set&lt;URL&gt; binderPathSet)</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">    <span class="keyword">if</span> (binderPathSet != <span class="keyword">null</span> &amp;&amp; binderPathSet.size() &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        Util.report(<span class="string">"Actual binding is of type ["</span> + StaticLoggerBinder.getSingleton().getLoggerFactoryClassStr() + <span class="string">"]"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/images/java/logback-slf4j.png" alt="具体实现"></p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;commons-logging&quot;&gt;&lt;a href=&quot;#commons-logging&quot; class=&quot;headerlink&quot; title=&quot;commons-logging&quot;&gt;&lt;/a&gt;commons-logging&lt;/h3&gt;&lt;p&gt;commons-logging是apache提供的一个通用的日志接口，是为了避免和具体的日志方案直接耦合的一种实现。通过commons-logging用户可以自己选择log4j或者jdk自带的logging作为具体实现。&lt;/p&gt;
&lt;p&gt;使用commons-logging的代码如下&lt;/p&gt;
&lt;figure class=&quot;highlight java&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;6&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;7&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;import&lt;/span&gt; org.apache.commons.logging.Log;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;import&lt;/span&gt; org.apache.commons.logging.LogFactory;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;class&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;MyApplication&lt;/span&gt; &lt;/span&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;  &lt;span class=&quot;keyword&quot;&gt;private&lt;/span&gt; Log log = LogFactory.getLog(&lt;span class=&quot;keyword&quot;&gt;this&lt;/span&gt;.getClass());&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="log" scheme="http://yoursite.com/tags/log/"/>
    
  </entry>
  
  <entry>
    <title>写给新手的 github 设置指南</title>
    <link href="http://yoursite.com/2019/08/30/github-setting-manual/"/>
    <id>http://yoursite.com/2019/08/30/github-setting-manual/</id>
    <published>2019-08-30T04:14:47.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>如果你的团队决定在github上管理项目代码,那么下面这些设置你一定要会。</p><p>首先进入到仓库的Settings页面,左边会有菜单选项</p><h3 id="设置谁能提交代码"><a href="#设置谁能提交代码" class="headerlink" title="设置谁能提交代码"></a>设置谁能提交代码</h3><p><img src="/images/github/github_setting_member.png" alt="设置成员"></p><p>添加你的team成员账号,这样他们才能向这个仓库提交代码</p><a id="more"></a><h3 id="设置默认分支"><a href="#设置默认分支" class="headerlink" title="设置默认分支"></a>设置默认分支</h3><p>开发过程中,我们并不直接向master提交分支,而是会新建一个开发分支develop/1.0,所有人都只能向这个分支提交代码</p><p><img src="/images/github/github_setting_default_branch.png" alt="设置默认分支"></p><h3 id="设置提交规则"><a href="#设置提交规则" class="headerlink" title="设置提交规则"></a>设置提交规则</h3><p>有了分支之后，我们可以针对不同的分支设置不同的提交规则(Settings–&gt;Branches–&gt;add rule)</p><p><img src="/images/github/github_setting_branch_rule.png" alt="设置分支规则"></p><p>设置状态检查我们一般会集成jekins，它是CI/CD的主要工具，当我们提交代码的时候会通过WebHooks和Jekins通信，然后在jekins上编译代码，运行其他任务等等。</p><h3 id="不允许merge代码-只能rebase或者squash"><a href="#不允许merge代码-只能rebase或者squash" class="headerlink" title="不允许merge代码,只能rebase或者squash"></a>不允许merge代码,只能rebase或者squash</h3><p><img src="/images/github/github_setting_forbidden_merge.png" alt="禁止merge"></p><p>一般在实践中，merge后的分支会使得git tree很难看，所以我们一般不做merge，直接squash或者rebase</p><p><img src="/images/github/github_no_merge.png" alt="不允许merge"></p><h3 id="设置pull-request模板"><a href="#设置pull-request模板" class="headerlink" title="设置pull request模板"></a>设置pull request模板</h3><p>在当前project下新建.github文件夹,然后再文件夹下放置pull_request_template.md文件,文件内容如下:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">## Description: </span><br><span class="line">-</span><br><span class="line">## Changes:</span><br><span class="line">-</span><br><span class="line">## Test Scope:</span><br><span class="line">-</span><br><span class="line">## Screenshots (optional)</span><br></pre></td></tr></table></figure><p>然后将此次修改推送到远端并合并此次请求。这样当我们提交pr的时候就出现了下面的模板</p><p><img src="/images/github/github_setting_pr_template.png" alt="提交pr"></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;如果你的团队决定在github上管理项目代码,那么下面这些设置你一定要会。&lt;/p&gt;
&lt;p&gt;首先进入到仓库的Settings页面,左边会有菜单选项&lt;/p&gt;
&lt;h3 id=&quot;设置谁能提交代码&quot;&gt;&lt;a href=&quot;#设置谁能提交代码&quot; class=&quot;headerlink&quot; title=&quot;设置谁能提交代码&quot;&gt;&lt;/a&gt;设置谁能提交代码&lt;/h3&gt;&lt;p&gt;&lt;img src=&quot;/images/github/github_setting_member.png&quot; alt=&quot;设置成员&quot;&gt;&lt;/p&gt;
&lt;p&gt;添加你的team成员账号,这样他们才能向这个仓库提交代码&lt;/p&gt;
    
    </summary>
    
    
      <category term="github" scheme="http://yoursite.com/tags/github/"/>
    
  </entry>
  
  <entry>
    <title>老大喊我用AOP记录下日志</title>
    <link href="http://yoursite.com/2019/08/22/use-aop-record-log/"/>
    <id>http://yoursite.com/2019/08/22/use-aop-record-log/</id>
    <published>2019-08-22T02:42:32.000Z</published>
    <updated>2019-12-20T03:26:24.679Z</updated>
    
    <content type="html"><![CDATA[<p>老大喊我记录下API的操作日志,免得前端甩锅,主要记录新增,修改,删除等操作。我想了下就决定用AOP来实现这个功能。</p><p>由于使用的是SpringBoot，所以首先应该在依赖中引入AOP包。<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"> <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-aop<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure></p><p>一般引入了AOP之后，一般不用做其他特殊配置,也不用加上@EnableAspectJAutoProxy注解。但是它仍有两个属性需要我们注意<br><a id="more"></a></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"># 相当于@EnableAspectJAutoProxy</span><br><span class="line">spring.aop.auto=true</span><br><span class="line"></span><br><span class="line"># 默认使用jdk来实现AOP,如果想要使用CGLIB的话，这里要改成true</span><br><span class="line">spring.aop.proxy-target-class=false</span><br></pre></td></tr></table></figure><h3 id="实现日志记录功能"><a href="#实现日志记录功能" class="headerlink" title="实现日志记录功能"></a>实现日志记录功能</h3><p>我想要记录某个API对模块进行了什么操作，操作的key,对于修改，删除来说我们记录id,对于新增来说，最开始没有id,我们记录name即可(也可以是其他属性),所以OpLog注解是这样的</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(&#123;ElementType.METHOD&#125;)</span><br><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> OpLog &#123;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 模块</span></span><br><span class="line">  <span class="function">String <span class="title">module</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="comment">// 类型</span></span><br><span class="line">  <span class="function">String <span class="title">opType</span><span class="params">()</span></span>;</span><br><span class="line">  <span class="comment">// 级别，默认一般</span></span><br><span class="line">  <span class="function">String <span class="title">level</span><span class="params">()</span> <span class="keyword">default</span> OpLevel.COMMON</span>;</span><br><span class="line">  <span class="comment">// 需要记录的key</span></span><br><span class="line">  <span class="function">String <span class="title">key</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>为了让这个功能更加易用,所以我要求其他开发者必须在Controller上加上注解@OpLog,为什么是Controller呢?因为API是在Controller上,我并不关心具体业务，我只要记录对应的操作即可。在Controller层你只需要这要做就行了</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/user"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CpGroupController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 注意这里的userParam和@RequestBody的userParam名字必需要要一致</span></span><br><span class="line">  <span class="meta">@OpLog</span>(<span class="keyword">module</span> = <span class="string">"用户管理"</span>, opType = <span class="string">"新增"</span>, key = <span class="string">"userParam.name"</span>)</span><br><span class="line">  <span class="meta">@PostMapping</span>(<span class="string">"/add"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> BaseResponse&lt;Boolean&gt; <span class="title">add</span><span class="params">(@RequestBody UserParam userParam)</span> </span>&#123;</span><br><span class="line">      </span><br><span class="line">      <span class="keyword">return</span> ResponseUtil.success(userService.add(userParam));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来就是切面代码的编写了，其中我们要记录访问的url以及必要的操作信息。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Aspect</span></span><br><span class="line"><span class="meta">@Slf</span>4j</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">OpLogAspect</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Autowired</span></span><br><span class="line">  <span class="keyword">private</span> OperateLogService operateLogService;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">int</span> paramNameIndex = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">int</span> propertyIndexFrom = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Pointcut</span>(<span class="string">"execution(public * com.generalthink.springboot.web.controller..*.*(..))"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">webLogPointcut</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Around</span>(value = <span class="string">"webLogPointcut() &amp;&amp; @annotation(opLog)"</span>, argNames = <span class="string">"joinPoint,opLog"</span>)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> Object <span class="title">around</span><span class="params">(ProceedingJoinPoint joinPoint, OpLog opLog)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line"></span><br><span class="line">    Object objReturn = joinPoint.proceed();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        OperateLog operationLog = generateOperateLog(joinPoint, opLog);</span><br><span class="line"></span><br><span class="line">        operateLogService.save(operationLog);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">        log.error(<span class="string">"operateLog record error"</span>, e);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> objReturn;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">private</span> OperateLog <span class="title">generateOperateLog</span><span class="params">(ProceedingJoinPoint joinPoint, OpLog opLog)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    String requestUrl = extractRequestUrl();</span><br><span class="line"></span><br><span class="line">    Object recordKey = getOpLogRecordKey(joinPoint, opLog);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> OperateLog.builder()</span><br><span class="line">            .<span class="keyword">module</span>(opLog.<span class="keyword">module</span>())</span><br><span class="line">            .opType(opLog.opType())</span><br><span class="line">            .level(opLog.level())</span><br><span class="line">            .operateTimeUnix(CommonUtils.getNowTimeUnix())</span><br><span class="line">            .recordKey(recordKey != <span class="keyword">null</span> ? recordKey.toString() : <span class="keyword">null</span>)</span><br><span class="line">            .url(requestUrl)</span><br><span class="line">            .operator(getCurrentUser())</span><br><span class="line">            .build();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 获取当前用户</span></span><br><span class="line">  <span class="function"><span class="keyword">private</span> String <span class="title">getCurrentUser</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Subject subject = SecurityUtils.getSubject();</span><br><span class="line">    <span class="keyword">return</span> (String) subject.getPrincipal();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 获取想要记录的key</span></span><br><span class="line">  <span class="function"><span class="keyword">private</span> Object <span class="title">getOpLogRecordKey</span><span class="params">(ProceedingJoinPoint joinPoint, OpLog opLog)</span> </span>&#123;</span><br><span class="line">    String key = opLog.key();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(StringUtils.isEmpty(key)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String[] keys = key.split(<span class="string">"\\."</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//入参  value</span></span><br><span class="line">    Object[] args = joinPoint.getArgs();</span><br><span class="line"></span><br><span class="line">    CodeSignature codeSignature = (CodeSignature) joinPoint.getSignature();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取Controller方法上的参数名称</span></span><br><span class="line">    String[] paramNames = codeSignature.getParameterNames();</span><br><span class="line"></span><br><span class="line">    Object paramArg = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; paramNames.length; i++) &#123;</span><br><span class="line">        String paramName = paramNames[i];</span><br><span class="line">        <span class="keyword">if</span> (paramName.equals(keys[paramNameIndex])) &#123;</span><br><span class="line">            paramArg = args[i];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(keys.length == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> paramArg;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取参数的值</span></span><br><span class="line">    Object paramValue = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">if</span>(<span class="keyword">null</span> != paramArg) &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            paramValue = getParamValue(paramArg, keys, propertyIndexFrom);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IllegalAccessException e) &#123;</span><br><span class="line">            log.error(<span class="string">"parse field error"</span>, e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> paramValue;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> Object <span class="title">getParamValue</span><span class="params">(Object param, String[] keys, <span class="keyword">int</span> idx)</span></span></span><br><span class="line"><span class="function">          <span class="keyword">throws</span> IllegalAccessException </span>&#123;</span><br><span class="line">    Optional&lt;Field&gt; fieldOptional = getAllFields(<span class="keyword">new</span> ArrayList&lt;&gt;(), param.getClass())</span><br><span class="line">            .stream()</span><br><span class="line">            .filter(field -&gt; field.getName().equalsIgnoreCase(keys[idx]))</span><br><span class="line">            .findFirst();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(!fieldOptional.isPresent()) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置属性可访问,因为bean当中属性一般都是private</span></span><br><span class="line">    Field field = fieldOptional.get();</span><br><span class="line">    field.setAccessible(<span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(idx + <span class="number">1</span> == keys.length) &#123;</span><br><span class="line">        <span class="keyword">return</span> field.get(param);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> getParamValue(field.get(param), keys, idx + <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 递归获取所有Field</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> List&lt;Field&gt; <span class="title">getAllFields</span><span class="params">(List&lt;Field&gt; fieldList, Class&lt;?&gt; type)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取当前类的所有Field</span></span><br><span class="line">    fieldList.addAll(Arrays.asList(type.getDeclaredFields()));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 获取父类的所有Field</span></span><br><span class="line">    Class&lt;?&gt; superClass = type.getSuperclass();</span><br><span class="line">    <span class="keyword">if</span>(superClass != <span class="keyword">null</span> &amp;&amp; superClass != Object.class) &#123;</span><br><span class="line">        getAllFields(fieldList,superClass);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> fieldList;</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="comment">// 获取访问URL</span></span><br><span class="line">  <span class="function"><span class="keyword">private</span> String <span class="title">extractRequestUrl</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ServletRequestAttributes attributes = (ServletRequestAttributes) RequestContextHolder</span><br><span class="line">            .getRequestAttributes();</span><br><span class="line">    HttpServletRequest request = attributes.getRequest();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> request.getRequestURL().toString();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>主要就是记录了操作了什么模块,操作内容等等,对于修改和删除操作我们可以记录id,但是对于save操作,我们没有办法记录id,只能记录其他属性,比如说name,就可以记录保存的数据了</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  name: &quot;zhangsan&quot;,</span><br><span class="line">  age: 20</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于上面的数据就会把”zhangsan”这个值保存下来,后面就可以通过查询日志表知道是谁操作的,修改和删除也是同样的.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;老大喊我记录下API的操作日志,免得前端甩锅,主要记录新增,修改,删除等操作。我想了下就决定用AOP来实现这个功能。&lt;/p&gt;
&lt;p&gt;由于使用的是SpringBoot，所以首先应该在依赖中引入AOP包。&lt;br&gt;&lt;figure class=&quot;highlight xml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt; &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;dependency&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;groupId&lt;/span&gt;&amp;gt;&lt;/span&gt;org.springframework.boot&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;groupId&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;artifactId&lt;/span&gt;&amp;gt;&lt;/span&gt;spring-boot-starter-aop&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;artifactId&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;dependency&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;&lt;/p&gt;
&lt;p&gt;一般引入了AOP之后，一般不用做其他特殊配置,也不用加上@EnableAspectJAutoProxy注解。但是它仍有两个属性需要我们注意&lt;br&gt;
    
    </summary>
    
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
      <category term="Spring" scheme="http://yoursite.com/tags/Spring/"/>
    
      <category term="AOP" scheme="http://yoursite.com/tags/AOP/"/>
    
  </entry>
  
  <entry>
    <title>LinkedHashMap源码解析</title>
    <link href="http://yoursite.com/2019/08/16/LinkedHashMap-analysis/"/>
    <id>http://yoursite.com/2019/08/16/LinkedHashMap-analysis/</id>
    <published>2019-08-16T08:13:36.000Z</published>
    <updated>2019-12-20T03:26:24.671Z</updated>
    
    <content type="html"><![CDATA[<p>上一篇文章分析了HashMap的原理,有网友留言想看LinkedHashMap分析，今天它来了。</p><p>LinkedHashMap是HashMap的子类,在原有HashMap数据结构的基础上,它还维护着一个双向链表链接所有entry,这个链表定义了迭代顺序，通常是数据插入的顺序。</p><p><img src="/images/java/LinkedHashMap_structure.png" alt="LinkedHashMap结构"></p><a id="more"></a><p>上图我只画了链表，其实红黑树节点也是一样的，只是节点类型不一样而已</p><p>也就是说我们遍历LinkedHashMap的时候,是从head指针指向的节点开始遍历,一直到tail指向的节点。</p><h3 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LinkedHashMap</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">HashMap</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt;</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">  <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">HashMap</span>.<span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    Entry&lt;K,V&gt; before, after;</span><br><span class="line">    Entry(<span class="keyword">int</span> hash, K key, V value, Node&lt;K,V&gt; next) &#123;</span><br><span class="line">        <span class="keyword">super</span>(hash, key, value, next);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 双向链表头节点</span></span><br><span class="line">    <span class="keyword">transient</span> LinkedHashMap.Entry&lt;K,V&gt; head;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 双向链表尾节点</span></span><br><span class="line">    <span class="keyword">transient</span> LinkedHashMap.Entry&lt;K,V&gt; tail;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 指定遍历LinkedHashMap的顺序,true表示按照访问顺序,false表示按照插入顺序，默认为false</span></span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">boolean</span> accessOrder;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从LinkedHashMap的定义里面可以看到它单独维护了一个双向链表，用于记录元素插入的顺序。这也是为什么我们打印LinkedHashMap的时候可以按照插入顺序打印的支撑。而accessOrder属性则指明了进行遍历时是按照什么顺序进行访问,我们可以通过它的构造方法自己指定顺序。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">LinkedHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity,<span class="keyword">float</span> loadFactor,</span></span></span><br><span class="line"><span class="function"><span class="params">                   <span class="keyword">boolean</span> accessOrder)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">super</span>(initialCapacity, loadFactor);</span><br><span class="line">  <span class="keyword">this</span>.accessOrder = accessOrder;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>当accessOrder=true,访问顺序的输出是什么意思呢？来看下下面的一段代码<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">LinkedHashMap&lt;Integer,Integer&gt; map = <span class="keyword">new</span> LinkedHashMap&lt;&gt;(<span class="number">8</span>, <span class="number">0.75f</span>, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">map.put(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">map.put(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line">map.put(<span class="number">3</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">map.get(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(map);</span><br></pre></td></tr></table></figure></p><p>输出结果是<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;1=1, 3=3, 2=2&#125;</span><br></pre></td></tr></table></figure></p><p>可以看到get了的数据被放到了双向链表尾部，也就是按照了访问时间进行排序,这就是访问顺序的含义。</p><p>在插入的时候LinkedHashMap复写了HashMap的newNode以及newTreeNode方法,并且在方法内部更新了双向链表的指向关系。</p><p>同时插入的时候调用了afterNodeAccess()方法以及afterNodeInsertion()方法,在HashMap中这两个方法是空实现,而在LinkedHashMap中则有具体实现,这两个方法也是专门给LinkedHashMap进行回调处理的。</p><p>afterNodeAccess()方法中如果accessOrder=true时会移动节点到双向链表尾部。当我们在调用map.get()方法的时候如果accessOrder=true也会调用这个方法,这就是为什么访问顺序输出时访问到的元素移动到链表尾部的原因。</p><p>接下来来看看afterNodeInsertion()的实现</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// evict如果为false，则表处于创建模式,当我们new HashMap(Map map)的时候就处于创建模式</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">afterNodeInsertion</span><span class="params">(<span class="keyword">boolean</span> evict)</span> </span>&#123; <span class="comment">// possibly remove eldest</span></span><br><span class="line">  LinkedHashMap.Entry&lt;K,V&gt; first;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// removeEldestEntry 总是返回false,所以下面的代码不会执行。</span></span><br><span class="line">  <span class="keyword">if</span> (evict &amp;&amp; (first = head) != <span class="keyword">null</span> &amp;&amp; removeEldestEntry(first)) &#123;</span><br><span class="line">      K key = first.key;</span><br><span class="line">      removeNode(hash(key), key, <span class="keyword">null</span>, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>看到这里我有一个想法,可以通过LinkedHashMap来实现LRU(Least Recently Used,即近期最少使用),只要满足条件,就删除head节点。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">public class LRUCache&lt;K,V&gt; extends LinkedHashMap&lt;K,V&gt; &#123;</span><br><span class="line">    </span><br><span class="line">  private int cacheSize;</span><br><span class="line">  </span><br><span class="line">  public LRUCache(int cacheSize) &#123;</span><br><span class="line">      super(16,0.75f,true);</span><br><span class="line">      this.cacheSize = cacheSize;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  /**</span><br><span class="line">   * 判断元素个数是否超过缓存容量</span><br><span class="line">   */</span><br><span class="line">  @Override</span><br><span class="line">  protected boolean removeEldestEntry(Map.Entry&lt;K, V&gt; eldest) &#123;</span><br><span class="line">      return size() &gt; cacheSize;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>就这样一个简单的LRU Cache就实现了,以后面试官如果喊你给它实现一个LRU,你就这样写给他,如果他让你换一种方式,你就用链表使用同样的思维给他实现一个,然后你就可以收割offer了。</p><p>对于删除,LinkedHashMap也同样是在HashMap的删除逻辑完成后，调用了afterNodeRemoval这个回调方法来更正链表指向关系。</p><p>其实你只要看了上一篇文章再也不怕面试官问我JDK8 HashMap,再记得LinkedHashMap只是多维护了一个双向链表之后,再看LinkedHashMap中关于链表操作的代码就非常简单了。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;上一篇文章分析了HashMap的原理,有网友留言想看LinkedHashMap分析，今天它来了。&lt;/p&gt;
&lt;p&gt;LinkedHashMap是HashMap的子类,在原有HashMap数据结构的基础上,它还维护着一个双向链表链接所有entry,这个链表定义了迭代顺序，通常是数据插入的顺序。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/java/LinkedHashMap_structure.png&quot; alt=&quot;LinkedHashMap结构&quot;&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>请别再问我Jdk8 HashMap了，好吗？</title>
    <link href="http://yoursite.com/2019/07/29/jdk8-hashmap-sourcecode/"/>
    <id>http://yoursite.com/2019/07/29/jdk8-hashmap-sourcecode/</id>
    <published>2019-07-29T10:08:35.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>上一篇文章中提到了ThreadLocalMap是使用开放地址法来解决冲突问题的，而我们今天的主角HashMap是采用了链表法来处理冲突的,什么是链表法呢?</p><p><img src="/images/java/hashmap-structure.png" alt="jdk8链表法"></p><p>在散列表中，每个 “ 桶(bucket)” 或者 “ 槽(slot)” 会对应一条链表，所有散列值相同的元素我们都放到相同槽位对应的链表中。</p><p>jdk8和jdk7不一样，jdk7中没有红黑树,数组中只挂载链表。而jdk8中在桶容量大于等于64且链表节点数大于等于8的时候转换为红黑树。当红黑树节点数量小于6时又会转换为链表。</p><a id="more"></a><h3 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h3><p>但插入的时候,我们只需要通过散列函数计算出对应的槽位,将其插入到对应链表或者红黑树即可。如果此时元素数量超过了一定值则会进行扩容，同时进行rehash.</p><h3 id="查找或者删除"><a href="#查找或者删除" class="headerlink" title="查找或者删除"></a>查找或者删除</h3><p>通过散列函数计算出对应的槽，然后遍历链表或者删除</p><h3 id="链表为什么会转为红黑树"><a href="#链表为什么会转为红黑树" class="headerlink" title="链表为什么会转为红黑树?"></a>链表为什么会转为红黑树?</h3><p>上一篇文章有提到过通过装载因子来判定空闲槽位还有多少，如果超过装载因子的值就会动态扩容,HashMap会扩容为原来的两倍大小(初始容量为16,即槽(数组)的大小为16)。但是无论负载因子和散列函数设得再合理，也避免不了链表过长的情况，一旦链表过长查找和删除元素就比较耗时，影响HashMap性能,所以JDK8中对其进行了优化，当链表长度大于等于8的时候将链表转换为红黑树，利用红黑树的特点(查找、插入、删除的时间复杂度最坏为O(logn))，可以提高HashMap的性能。当节点个数少于6个的时候，又会将红黑树转化为链表。因为在数据量较小的情况下，红黑树要维持平衡，比起链表来，性能上的优势并不明显，而且编码难度比链表要大上不少。</p><h3 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h3><h4 id="构造方法以及重要属性"><a href="#构造方法以及重要属性" class="headerlink" title="构造方法以及重要属性"></a>构造方法以及重要属性</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><p>HashMap的构造方法中可以分别指定初始化容量(bucket大小)以及负载因子，如果不指定默认值分别是16和0.75.它几个重要属性如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 初始化容量，必须要2的n次幂</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_INITIAL_CAPACITY = <span class="number">1</span> &lt;&lt; <span class="number">4</span>; <span class="comment">// aka 16</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 负载因子默认值</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> DEFAULT_LOAD_FACTOR = <span class="number">0.75f</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 需要从链表转换为红黑树时,链表节点的最小长度</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TREEIFY_THRESHOLD = <span class="number">8</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转换为红黑树时数组的最小容量</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MIN_TREEIFY_CAPACITY = <span class="number">64</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// resize操作时,红黑树节点个数小于6则转换为链表。</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> UNTREEIFY_THRESHOLD = <span class="number">6</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// HashMap阈值，用于判断是否需要扩容(threshold = 容量*loadFactor)</span></span><br><span class="line"><span class="keyword">int</span> threshold;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 负载因子</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">float</span> loadFactor;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 链表节点</span></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> <span class="keyword">int</span> hash;</span><br><span class="line">  <span class="keyword">final</span> K key;</span><br><span class="line">  V value;</span><br><span class="line">  Node&lt;K,V&gt; next;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 保存数据的数组</span></span><br><span class="line"><span class="keyword">transient</span> Node&lt;K,V&gt;[] table;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 红黑树节点</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeNode</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">LinkedHashMap</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">  TreeNode&lt;K,V&gt; parent;  <span class="comment">// red-black tree links</span></span><br><span class="line">  TreeNode&lt;K,V&gt; left;</span><br><span class="line">  TreeNode&lt;K,V&gt; right;</span><br><span class="line">  TreeNode&lt;K,V&gt; prev;    <span class="comment">// needed to unlink next upon deletion</span></span><br><span class="line">  <span class="keyword">boolean</span> red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面的table就是存储数据的数组(可以叫做桶或者槽),数组挂载的是链表或者红黑树。值得一提的是构造HashMap的时候并没有初始化数组容量，而是在第一次put元素的时候才进行初始化的。</p><h3 id="hash函数的设计"><a href="#hash函数的设计" class="headerlink" title="hash函数的设计"></a>hash函数的设计</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> hash = (key == <span class="keyword">null</span>) ? <span class="number">0</span> : (h = key.hashCode()) ^ (h &gt;&gt;&gt; <span class="number">16</span>);</span><br><span class="line"><span class="keyword">int</span> index = hash &amp; (tab.length-<span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>从上面可以看出,key为null是时候放到数组中的第一个位置的,我们一般定位key应当存放在数组哪个位置的时候一般是这样做的 <code>key.hashCode() % tab.length</code>。但是当tab.length是2的n次幂的时候，就可以转换为 <code>A % B = A &amp; (B-1)</code>;所以 <code>index = hash &amp; (tab.length-1)</code>就可以理解了。</p><blockquote><p>这里是使用了除留余数法的理念来设计的,可以可能减少hash冲突<br>除留余数法 : 用关键字K除以某个不大于hash表长度m的数p,将所得余数作为hash表地址<br>比如x/8=x&gt;&gt;3,即把x右移3位，得到了x/8的商，被移掉的部分(后三位)，则是x%8，也就是余数。</p></blockquote><p>而对于hash值的运算为什么是<code>(h = key.hashCode()) ^ (h &gt;&gt;&gt; 16)</code>呢？也就是为什么要向右移16位呢?直接使用 <code>key.hashCode() &amp; (tab.length -1)</code>不好吗？<br>如果这样做，由于tab.length肯定是远远小于hash值的,所以位运算的时候只有低位才参与运算，而高位毫无作为，会带来hash冲突的风险。</p><p><strong>而hashcode本身是一个32位整形值，向右移位16位之后再进行异或运行计算出来的整形将具有高位和低位的性质，就可以得到一个非常随机的hash值，在通过除留余数法，得到的index就更低概率的减少了冲突。</strong></p><h3 id="插入数据"><a href="#插入数据" class="headerlink" title="插入数据"></a>插入数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, <span class="keyword">boolean</span> onlyIfAbsent,</span></span></span><br><span class="line"><span class="function"><span class="params">                  <span class="keyword">boolean</span> evict)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"> Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="keyword">int</span> n, i;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 1. 如果数组未初始化,则初始化数组</span></span><br><span class="line"> <span class="keyword">if</span> ((tab = table) == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">    n = (tab = resize()).length;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 2. 如果当前节点未被插入数据(未碰撞),则直接new一个节点进行插入</span></span><br><span class="line"> <span class="keyword">if</span> ((p = tab[i = (n - <span class="number">1</span>) &amp; hash]) == <span class="keyword">null</span>)</span><br><span class="line">    tab[i] = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line"> <span class="keyword">else</span> &#123;</span><br><span class="line">    Node&lt;K,V&gt; e; K k;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 碰撞了,已存在相同的key,则进行覆盖</span></span><br><span class="line">   <span class="keyword">if</span> (p.hash == hash &amp;&amp;</span><br><span class="line">       ((k = p.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">       e = p;</span><br><span class="line">   <span class="keyword">else</span> <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">        <span class="comment">// 4. 碰撞后发现为树结构，则挂载在树上</span></span><br><span class="line">       e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="keyword">this</span>, tab, hash, key, value);</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">       <span class="keyword">for</span> (<span class="keyword">int</span> binCount = <span class="number">0</span>; ; ++binCount) &#123;</span><br><span class="line">            <span class="comment">// 5. 进行尾插入,如果链表节点数达到上线则转换为红黑树</span></span><br><span class="line">           <span class="keyword">if</span> ((e = p.next) == <span class="keyword">null</span>) &#123;</span><br><span class="line">               p.next = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line">               <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="number">1</span>) <span class="comment">// -1 for 1st</span></span><br><span class="line">                   treeifyBin(tab, hash);</span><br><span class="line">               <span class="keyword">break</span>;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">// 6. 链表中碰撞了</span></span><br><span class="line">           <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">               ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">               <span class="keyword">break</span>;</span><br><span class="line">           p = e;</span><br><span class="line">       &#125;</span><br><span class="line">     &#125;</span><br><span class="line">     <span class="comment">// 7. 用新value替换旧的value</span></span><br><span class="line">     <span class="keyword">if</span> (e != <span class="keyword">null</span>) &#123; <span class="comment">// existing mapping for key</span></span><br><span class="line">       V oldValue = e.value;</span><br><span class="line">       <span class="keyword">if</span> (!onlyIfAbsent || oldValue == <span class="keyword">null</span>)</span><br><span class="line">           e.value = value;</span><br><span class="line">       afterNodeAccess(e);</span><br><span class="line">       <span class="keyword">return</span> oldValue;</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br><span class="line"> ++modCount;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 8. 操作阈值则进行扩容</span></span><br><span class="line"> <span class="keyword">if</span> (++size &gt; threshold)</span><br><span class="line">     resize();</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 给LinkedHashMap实现</span></span><br><span class="line"> afterNodeInsertion(evict);</span><br><span class="line"> <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>简述下put的逻辑，它主要分为以下几个步骤:</p><ol><li>首先判断是否初始化，如果未初始化则初始化数组,初始容量为16</li><li>通过hash&amp;(n-1)获取数组下标，如果该位置为空，表示未碰撞，直接插入数据</li><li>发生碰撞且存在相同的key，则在后面处理中直接进行覆盖</li><li>碰撞后发现为树结构，则直接挂载到红黑树上</li><li>碰撞后发现为链表结构，则进行尾插入，当链表容量大于等于8的时候转换为树节点</li><li>发现在链表中进行碰撞了，则在后面处理直接覆盖</li><li>发现之前存在相同的key,只直接用新值替换旧值</li><li>map的容量(存储元素的数量)大于阈值则进行扩容，扩容为之前容量的2倍</li></ol><h3 id="扩容"><a href="#扩容" class="headerlink" title="扩容"></a>扩容</h3><p>resize()方法中，如果发现当前数组未初始化，则会初始化数组。如果已经初始化，则会将数组容量扩容为之前的两倍，同时进行rehash(将旧数组的数据移动到新的数组).JDK8的rehash过程很有趣，相比JDK7做了不少优化，我们来看下这里的rehash过程。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// 数组扩容为之前2倍大小的代码省略，这里主要分析rehash过程。</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (oldTab != <span class="keyword">null</span>) &#123;</span><br><span class="line"> <span class="comment">// 遍历旧数组</span></span><br><span class="line"> <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; oldCap; ++j) &#123;</span><br><span class="line">   Node&lt;K,V&gt; e;</span><br><span class="line">   <span class="keyword">if</span> ((e = oldTab[j]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">     oldTab[j] = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">     <span class="comment">// 1. 如果旧数组中不存在碰撞,则直接移动到新数组的位置</span></span><br><span class="line">     <span class="keyword">if</span> (e.next == <span class="keyword">null</span>)</span><br><span class="line">        newTab[e.hash &amp; (newCap - <span class="number">1</span>)] = e;</span><br><span class="line">     <span class="keyword">else</span> <span class="keyword">if</span> (e <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">        <span class="comment">// 2. 如果存在碰撞，且节点类型是树节点，则进行树节点拆分(挂载到扩容后的数组中或者转为链表)</span></span><br><span class="line">        ((TreeNode&lt;K,V&gt;)e).split(<span class="keyword">this</span>, newTab, j, oldCap);</span><br><span class="line">     <span class="keyword">else</span> &#123; <span class="comment">// preserve order</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 处理冲突是链表的情况,会保留原有节点的顺序</span></span><br><span class="line"></span><br><span class="line">       Node&lt;K,V&gt; loHead = <span class="keyword">null</span>, loTail = <span class="keyword">null</span>;</span><br><span class="line">       Node&lt;K,V&gt; hiHead = <span class="keyword">null</span>, hiTail = <span class="keyword">null</span>;</span><br><span class="line">       Node&lt;K,V&gt; next;</span><br><span class="line">       <span class="keyword">do</span> &#123;</span><br><span class="line">         next = e.next;</span><br><span class="line">         <span class="comment">// 4. 判断扩容后元素是否在原有的位置(这里非常巧妙,下面会分析)</span></span><br><span class="line">         <span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">           <span class="keyword">if</span> (loTail == <span class="keyword">null</span>)</span><br><span class="line">               loHead = e;</span><br><span class="line">           <span class="keyword">else</span></span><br><span class="line">               loTail.next = e;</span><br><span class="line">           loTail = e;</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="comment">// 5. 元素不是在原有位置</span></span><br><span class="line">         <span class="keyword">else</span> &#123;</span><br><span class="line">           <span class="keyword">if</span> (hiTail == <span class="keyword">null</span>)</span><br><span class="line">               hiHead = e;</span><br><span class="line">           <span class="keyword">else</span></span><br><span class="line">               hiTail.next = e;</span><br><span class="line">           hiTail = e;</span><br><span class="line">         &#125;</span><br><span class="line">       &#125; <span class="keyword">while</span> ((e = next) != <span class="keyword">null</span>);</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 6. 将扩容后未改变index的元素复制到新数组</span></span><br><span class="line">       <span class="keyword">if</span> (loTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">         loTail.next = <span class="keyword">null</span>;</span><br><span class="line">         newTab[j] = loHead;</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 7. 将扩容后改变了index位置的元素复制到新数组</span></span><br><span class="line">       <span class="keyword">if</span> (hiTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">         hiTail.next = <span class="keyword">null</span>;</span><br><span class="line">         <span class="comment">// 8. index改变后,新的下标是j+oldCap,这里也很巧妙，下面会分析</span></span><br><span class="line">         newTab[j + oldCap] = hiHead;</span><br><span class="line">       &#125;</span><br><span class="line">     &#125;</span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面的代码中展现了整个rehash的过程，先遍历旧数组中的元素，接着做下面的事情</p><ol><li>如果旧数组中不存在数据碰撞(未挂载链表或者红黑树),那么直接将元素赋值到新数组中，其中<code>index=e.hash &amp; (newCap - 1)</code>。</li><li>如果存在碰撞，且节点类型是树节点，则进行树节点拆分(挂载到扩容后的数组中或者转为链表)</li><li>如果存在碰撞，且节点是链表，则处理链表的情况,<strong>rehash过程会保留节点原始顺序(JDK7中不会保留，这也是导致jdk7中多线程出现死循环的原因)</strong></li><li>判断元素在扩容后是否还处于原有的位置，这里通过<code>(e.hash &amp; oldCap) == 0</code>判断,oldCap表示扩容前数组的大小。</li><li>发现元素不是在原有位置，更新hiTail和hiHead的指向关系</li><li>将扩容后未改变index的元素复制到新数组</li><li>将扩容后改变了index位置的元素复制到新数组，新数组的下标是 <code>j + oldCap</code>。</li></ol><p>其中第4点和第5点中将链表的元素分为两部分(do..while部分)，一部分是rehash后index未改变的元素，一部分是index被改变的元素。分别用两个指针来指向头尾节点。</p><blockquote><p>比如当oldCap=8时,1–&gt;9–&gt;17都挂载在tab[1]上,而扩容后，1–&gt;17挂载在tab[1]上,9挂载在tab[9]上。</p></blockquote><p>那么是如何确定rehash后index是否被改变呢？改变之后的index又变成了多少呢？</p><p>这里的设计很是巧妙，还记得HashMap中数组大小是2的n次幂吗?当我们计算索引位置的时候，使用的是 e.hash &amp; (tab.length -1)。</p><p>这里我们讨论数组大小从8扩容到16的过程。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">tab.length -1 = 7   0 0 1 1 1</span><br><span class="line">e.hashCode = x      0 x x x x</span><br><span class="line">==============================</span><br><span class="line">                    0 0 y y y</span><br></pre></td></tr></table></figure></p><p>可以发现在扩容前index的位置由hashCode的低三位来决定。那么扩容后呢？</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">tab.length -1 = 15   0 1 1 1 1</span><br><span class="line">e.hashCode = x       x x x x x</span><br><span class="line">==============================</span><br><span class="line">                     0 z y y y</span><br></pre></td></tr></table></figure><p>扩容后，index的位置由低四位来决定,而低三位和扩容前一致。也就是说扩容后index的位置是否改变是由高字节来决定的,也就是说我们只需要将hashCode和高位进行运算即可得到index是否改变。</p><p>而刚好扩容之后的高位和oldCap的高位一样。如上面的15二进制是1111,而8的二进制是1000,他们的高位都是一样的。所以我们通过e.hash &amp; oldCap运算的结果即可判断index是否改变。</p><p>同理，如果扩容后index该变了。新的index和旧的index的值也是高位不同，其新值刚好是 oldIndex + oldCap的值。所以当index改变后,新的index是 j + oldCap。</p><p>至此,resize方法结束,元素被插入到了该有的位置。</p><h3 id="get"><a href="#get" class="headerlink" title="get()"></a>get()</h3><p>get()的方法就相对来说要简单一些了，它最重要的就是找到key是存放在哪个位置</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title">getNode</span><span class="params">(<span class="keyword">int</span> hash, Object key)</span> </span>&#123;</span><br><span class="line">  Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="keyword">int</span> n; K k;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 1. 首先(n-1) &amp; hash确定元素位置</span></span><br><span class="line">  <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">      (first = tab[(n - <span class="number">1</span>) &amp; hash]) != <span class="keyword">null</span>) &#123;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 2. 判断第一个元素是否是我们需要找的元素</span></span><br><span class="line">      <span class="keyword">if</span> (first.hash == hash &amp;&amp;</span><br><span class="line">          ((k = first.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">          <span class="keyword">return</span> first;</span><br><span class="line">      <span class="keyword">if</span> ((e = first.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 3. 节点如果是树节点,则在红黑树中寻找元素</span></span><br><span class="line">        <span class="keyword">if</span> (first <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">            <span class="keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);</span><br><span class="line">        <span class="number">4</span>. 在链表中寻找对应的节点</span><br><span class="line">        <span class="keyword">do</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                <span class="keyword">return</span> e;</span><br><span class="line">        &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="remove"><a href="#remove" class="headerlink" title="remove"></a>remove</h3><p>remove方法寻找节点的过程和get()方法寻找节点的过程是一样的，这里我们主要分析寻找到节点后是如何处理的</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (node != <span class="keyword">null</span> &amp;&amp; (!matchValue || (v = node.value) == value ||</span><br><span class="line">    (value != <span class="keyword">null</span> &amp;&amp; value.equals(v)))) &#123;</span><br><span class="line">    <span class="comment">// 1. 删除树节点,删除时如果不平衡会重新移动节点位置</span></span><br><span class="line">    <span class="keyword">if</span> (node <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">        ((TreeNode&lt;K,V&gt;)node).removeTreeNode(<span class="keyword">this</span>, tab, movable);</span><br><span class="line">    <span class="comment">// 删除的节点是链表第一个节点,则直接将第二个节点赋值为第一个节点</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (node == p)</span><br><span class="line">        tab[index] = node.next;</span><br><span class="line">    <span class="comment">// 删除的节点是链表的中间节点，这里的p为node的prev节点</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        p.next = node.next;</span><br><span class="line">    ++modCount;</span><br><span class="line">    --size;</span><br><span class="line">    afterNodeRemoval(node);</span><br><span class="line">    <span class="keyword">return</span> node;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>remove方法中，最为复杂的部分应该是removeTreeNode部分，因为删除红黑树节点后，可能需要退化为链表节点，还可能由于不满足红黑树特点，需要移动节点位置。<br>代码也比较多，这里就不贴上来了。但也因此佐证了为什么不全部使用红黑树来代替链表。</p><h3 id="JDK7扩容时导致的死循环问题"><a href="#JDK7扩容时导致的死循环问题" class="headerlink" title="JDK7扩容时导致的死循环问题"></a>JDK7扩容时导致的死循环问题</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* Transfers all entries from current table to newTable.</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">transfer</span><span class="params">(Entry[] newTable)</span> </span>&#123;</span><br><span class="line"> Entry[] src = table;</span><br><span class="line"> <span class="keyword">int</span> newCapacity = newTable.length;</span><br><span class="line"> <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; src.length; j++) &#123;</span><br><span class="line">   Entry&lt;K,V&gt; e = src[j];</span><br><span class="line">   <span class="keyword">if</span> (e != <span class="keyword">null</span>) &#123;</span><br><span class="line">       src[j] = <span class="keyword">null</span>;</span><br><span class="line">       <span class="keyword">do</span> &#123;</span><br><span class="line">           <span class="comment">// B线程执行到这里之后就暂停了</span></span><br><span class="line">           Entry&lt;K,V&gt; next = e.next;</span><br><span class="line">           <span class="keyword">int</span> i = indexFor(e.hash, newCapacity);</span><br><span class="line">           e.next = newTable[i];</span><br><span class="line">           newTable[i] = e;</span><br><span class="line">           e = next;</span><br><span class="line">       &#125; <span class="keyword">while</span> (e != <span class="keyword">null</span>);</span><br><span class="line">   &#125;</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>扩容时上面的代码容易导致死循环,是怎样导致的呢？假设有两个线程A和B都在执行这一段代码，数组大小由2扩容到4,在扩容前tab[1]=1–&gt;5–&gt;9。<br><img src="/images/java/hashmap-unresize.png" alt="扩容前"></p><p>当B线程执行到 next = e.next时让出时间片,A线程执行完整段代码但是还没有将内部的table设置为新的newTable时，线程B继续执行。</p><p>此时A线程执行完成之后，挂载在tab[1]的元素是9–&gt;5–&gt;1,注意这里的<strong>顺序被颠倒了</strong>。此时e = 1, next = 5;</p><blockquote><p>tab[i]的按照循环次数变更顺序, 1. tab[i]=1, 2. tab[i]=5–&gt;1, 3. tab[i]=9–&gt;5–&gt;1</p></blockquote><p><img src="/images/java/hashmap-resize-threadA.png" alt="线程A执行完成后"></p><p>同样B线程我们也按照循环次数来分析</p><ol><li>第一次循环执行完成后,newTable[i]=1, e = 5</li><li>第二次循环完成后: newTable[i]=5–&gt;1, e = 1。 </li><li>第三次循环,e没有next,所以next指向null。当执行e.next = newTable[i](1–&gt;5)的时候,就形成了 1–&gt;5–&gt;1的环,再执行newTable[i]=e,此时newTable[i] = 1–&gt;5–&gt;1。</li></ol><p>当在数组该位置get寻找对应的key的时候，就发生了死循环,引起CPU 100%问题。</p><p><img src="/images/java/hashmap-resize.png" alt="线程B执行扩容过程"></p><p>而JDK8就不会出现这个问题,它在这里就有一个优化，它使用了两个指针来分别指向头节点和尾节点，而且还保证了元素原本的顺序。<br>当然HashMap仍然是不安全的,所以在多线程并发条件下推荐使用ConcurrentHashMap。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;上一篇文章中提到了ThreadLocalMap是使用开放地址法来解决冲突问题的，而我们今天的主角HashMap是采用了链表法来处理冲突的,什么是链表法呢?&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/java/hashmap-structure.png&quot; alt=&quot;jdk8链表法&quot;&gt;&lt;/p&gt;
&lt;p&gt;在散列表中，每个 “ 桶(bucket)” 或者 “ 槽(slot)” 会对应一条链表，所有散列值相同的元素我们都放到相同槽位对应的链表中。&lt;/p&gt;
&lt;p&gt;jdk8和jdk7不一样，jdk7中没有红黑树,数组中只挂载链表。而jdk8中在桶容量大于等于64且链表节点数大于等于8的时候转换为红黑树。当红黑树节点数量小于6时又会转换为链表。&lt;/p&gt;
    
    </summary>
    
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>开放寻址的ThreadLocalMap分析</title>
    <link href="http://yoursite.com/2019/07/25/ThreadLocalMap-sourcecode/"/>
    <id>http://yoursite.com/2019/07/25/ThreadLocalMap-sourcecode/</id>
    <published>2019-07-25T03:38:26.000Z</published>
    <updated>2019-12-20T03:26:24.671Z</updated>
    
    <content type="html"><![CDATA[<h3 id="什么是散列"><a href="#什么是散列" class="headerlink" title="什么是散列"></a>什么是散列</h3><p>散列表(hash table)我们平时也叫它哈希表或者Hash表，它用的是数组支持按照下标随机访问数据的特性，所以散列表其实就是数组的一种扩展，由数组演化而来。可以说，没有数组就没有散列表。</p><p>比如我们有100件商品,编号没有规律的4位数字,现在我们想要通过编号快速获取商品信息，如何做呢?我们可以将这100件商品信息放到数组里，通过 商品编号%100这样的方式得到一个值,值为1的商品放到数组中下标为1的位置，值为2的商品，我们放到数组中下标为2的位置。以此类推，编号为K的选手放到数组中下标为K的位置。因为商品编号通过散列函数(编号%100)跟数据下标一一对应，所以但我们需要查询编号为x的商品信息的时候,我们使用同样的方式，将编号转换为数组下标，就可以从对应的数组下标的位置取出数据。<br>这就是散列的典型思想。</p><a id="more"></a><p>我们通过上面的例子可以得出这样规律：散列表用的就是数组支持按照下标随机访问的时候，时间复杂度是 O(1) 的特性。通过散列函数(商品编号%100)把元素的键值映<br>射为下标，然后将数据存储在数组中对应下标的位置。当我们按照键值查询元素时，我们用同样的散列函数，将键值转化数组下标，从对应的数组下标的位置取<br>数据。</p><h3 id="开放寻址"><a href="#开放寻址" class="headerlink" title="开放寻址"></a>开放寻址</h3><p>一说到散列(或者叫做hash表),大家更熟悉的是HashMap或者LinkedHashMap,而今天的主角是ThreadLocalMap,它是ThreadLocal中的一个内部类。分析ThreadLocal源码的时候不可能绕过它。</p><p>由于哈希表使用了数组,无论hash函数如何设计都无可避免存在hash冲突。上面的例子如果两件商品的id分别是1001和1101,那么他们的数据就会就会被放到数组的同一个位置，出现了冲突</p><blockquote><p>鸽巢原理，又名狄利克雷抽屉原理、鸽笼原理。其中一种简单的表述法为：若有n个笼子和n+1只鸽子，所有的鸽子都被关在鸽笼里，那么至少有一个笼子有至少2只鸽子</p></blockquote><p>ThreadLocalMap作为hash表的一种实现方式,它是使用什么方式来解决冲突的呢?它使用了开放寻址法来解决这个问题。</p><h4 id="元素插入"><a href="#元素插入" class="headerlink" title="元素插入"></a>元素插入</h4><p>开放寻址法的核心是如果出现了散列冲突，就重新探测一个空闲位置，将其插入。当我们往散列表中插入数据时，如果某个数据经过散列函数散列之后，存储位置已经被占用了，我们就从当前位置开始，依次往后查找，看是否有空闲位置，直到找到为止。</p><p><img src="/images/java/hash-element-find.png" alt="元素查找过程"></p><p>从图中可以看出，散列表的大小为 10 ，在元素 x 插入散列表之前，已经 6 个元素插入到散列表中。 x 经过 Hash 算法之后，被散列到位置下标为 7 的位置,但是这个位置已经有数据了，所以就产生了冲突。于是我们就顺序地往后一个一个找，看有没有空闲的位置，遍历到尾部都没有找到空闲的位置，于是我们再从表头开始找，直到找到空闲位置 2 ，于是将其插入到这个位置。</p><h4 id="元素查找"><a href="#元素查找" class="headerlink" title="元素查找"></a>元素查找</h4><p>在散列表中查找元素的过程有点儿类似插入过程。我们通过散列函数求出要查找元素的键值对应的散列值，然后比较数组中下标为散列值的元素和要查找的元素。如果相等，则说明就是我们要找的元素；否则就顺序往后依次查找。如果遍历到数组中的空闲位置，还没有找到，就说明要查找的元素并没有在散列表中。</p><h4 id="元素删除"><a href="#元素删除" class="headerlink" title="元素删除"></a>元素删除</h4><p>ThreadLocalMap跟数组一样，不仅支持插入、查找操作，还支持删除操作。对于使用线性探测法解决冲突的散列表，删除操作稍微有些特别。我们不能单纯地把要删除的元素设置为空。</p><p>还记得我们刚讲的查找操作吗？在查找的时候，一旦我们通过线性探测方法，找到一个空闲位置，我们就可以认定散列表中不存在这个数据。但是，如果这个空闲位置是我们后来删除的，就会导致原来的查找算法失效。本来存在的数据，会被认定为不存在。这个问题如何解决呢？</p><p>我们可以在删除元素之后,将之后不为null的数据rehash，这样就不会影响查询的逻辑</p><blockquote><p>另一种方法是:可以将删除的元素，特殊标记为 deleted 。当线性探测查找的时候，遇到标记为 deleted 的空间，并不是停下来，而是继续往下探测</p></blockquote><p><img src="/images/java/hash-rehash.png" alt="rehash过程"></p><p>这里解释下rehash的过程:当删除元素8的时候，先把下标为8的值设置为null,然后将其后面不为空的数组元素rehash。比如8后面的元素是9,其原本应该的位置(9%10=9)就在9所以不移动。下一个元素是19,应该在下标为9的位置,但是已经被占用了,所以就找下一个空闲的位置，下标为3的位置是空闲的，放入tab[3]。接着下一个元素1就在tab[1]不移动，<br>元素7的位置在tab[7]，因为已经被占用，放入下一个空闲位置tab[8]。下一个元素仍然是19,这里由于tab[9]已经被占用，所以放入下一个空闲位置tab[0]。接着最后一个元素4位置就在tab[4],所以不移动。元素4的下一个位置为空，整个rehash过程结束。</p><h3 id="装载因子"><a href="#装载因子" class="headerlink" title="装载因子"></a>装载因子</h3><p>你可能已经发现了，线性探测法其实存在很大问题。当散列表中插入的数据越来越多时，散列冲突发生的可能性就会越来越大，空闲位置会越来越少，线性探测的时间就会越来越久。极端情况下，我们可能需要探测整个散列表，所以最坏情况下的时间复杂度为 O(n) 。同理，在删除和查找时，也有可能会线性探测整张散列表，才能找到要查找或者删除的数据。</p><p>不管采用哪种探测方法，hash函数设计得在合理,当散列表中空闲位置不多的时候，散列冲突的概率就会大大提高。为了尽可能保证散列表的操作效率，一般情况下，我们会尽可能保证散列表中有一定比例的空闲槽位。我们用装载因子(load factor)来表示空位的多少。</p><p>装载因子的计算公式是：<strong>散列表的装载因子=填入表中的元素个数/散列表的长度</strong> 装载因子越大，说明空闲位置越少，冲突越多，散列表的性能会下降。</p><h3 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h3><h4 id="ThreadLocalMap定义"><a href="#ThreadLocalMap定义" class="headerlink" title="ThreadLocalMap定义"></a>ThreadLocalMap定义</h4><p>ThreadLocal的核心数据结构是ThreadLocalMap,它的数据结构如下：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">static class ThreadLocalMap &#123;</span><br><span class="line">  </span><br><span class="line">  // 这里的entry继承WeakReference了</span><br><span class="line">  static class Entry extends WeakReference&lt;ThreadLocal&lt;?&gt;&gt; &#123;</span><br><span class="line">      Object value;</span><br><span class="line">      Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;</span><br><span class="line">          super(k);</span><br><span class="line">          value = v;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  // 初始化容量,必须是2的n次方</span><br><span class="line">  private static final int INITIAL_CAPACITY = 16;</span><br><span class="line"></span><br><span class="line">  // entry数组,用于存储数据</span><br><span class="line">  private Entry[] table;</span><br><span class="line"></span><br><span class="line">  // map的容量</span><br><span class="line">  private int size = 0;</span><br><span class="line"></span><br><span class="line">  // 数据量达到多少进行扩容,默认是 table.length * 2 / 3</span><br><span class="line">  private int threshold;</span><br></pre></td></tr></table></figure><p>从ThreadLocalMap的定义可以看出Entry的key就是ThreadLocal，而value就是值。同时，Entry也继承WeakReference，所以说Entry所对应key（ThreadLocal实例）的引用为一个弱引用。而且定义了装载因子为数组长度的三分之二。</p><h4 id="set-方法"><a href="#set-方法" class="headerlink" title="set()方法"></a>set()方法</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(ThreadLocal&lt;?&gt; key, Object value)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  Entry[] tab = table;</span><br><span class="line">  <span class="keyword">int</span> len = tab.length;</span><br><span class="line">  <span class="keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 采用线性探测,寻找合适的插入位置</span></span><br><span class="line">  <span class="keyword">for</span> (Entry e = tab[i]; e != <span class="keyword">null</span>; e = tab[i = nextIndex(i, len)]) &#123;</span><br><span class="line">      ThreadLocal&lt;?&gt; k = e.get();</span><br><span class="line">      <span class="comment">// key存在则直接覆盖</span></span><br><span class="line">      <span class="keyword">if</span> (k == key) &#123;</span><br><span class="line">          e.value = value;</span><br><span class="line">          <span class="keyword">return</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="comment">// key不存在,说明之前的ThreadLocal对象被回收了</span></span><br><span class="line">      <span class="keyword">if</span> (k == <span class="keyword">null</span>) &#123;</span><br><span class="line">          replaceStaleEntry(key, value, i);</span><br><span class="line">          <span class="keyword">return</span>;</span><br><span class="line">      &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 不存在也没有旧元素,就创建一个</span></span><br><span class="line">  tab[i] = <span class="keyword">new</span> Entry(key, value);</span><br><span class="line">  <span class="keyword">int</span> sz = ++size;</span><br><span class="line">  <span class="comment">// 清除旧的槽(entry不为空，但是ThreadLocal为空)，并且当数组中元素大于阈值就rehash</span></span><br><span class="line">  <span class="keyword">if</span> (!cleanSomeSlots(i, sz) &amp;&amp; sz &gt;= threshold)</span><br><span class="line">    expungeStaleEntries();</span><br><span class="line">    <span class="comment">// 扩容</span></span><br><span class="line">    <span class="keyword">if</span> (size &gt;= threshold - threshold / <span class="number">4</span>)</span><br><span class="line">      resize();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面源码的主要步骤如下:</p><ol><li>采用线性探测法,寻找合适的插入位置。首先判断key是否存在,存在则直接覆盖。如果key不存在证明被垃圾回收了此时需要用新的元素替换旧的元素</li><li>不存在对应的元素,需要创建一个新的元素</li><li>清除entry不为空，但是ThreadLocal(entry的key被回收了)的元素，防止内存泄露</li><li>如果满足条件:size &gt;= threshold - threshold / 4就将数组扩大为之前的两倍，同时会重新计算数组元素所处的位置并进行移动(rehash)。比如最开始数组初始大小为16,当size &gt;= (16*2/3=10) - (10/4) = 8的时候就会扩容,将数组大小扩容至32.</li></ol><p>无论是replaceStaleEntry()方法还是cleanSomeSlots()方法，最重要的方法调用是expungeStaleEntry(),你可以在ThreadLocalMap中的get,set,remove都能发现调用它的身影。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">expungeStaleEntry</span><span class="params">(<span class="keyword">int</span> staleSlot)</span> </span>&#123;</span><br><span class="line">  Entry[] tab = table;</span><br><span class="line">  <span class="keyword">int</span> len = tab.length;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 删除对应位置的entry</span></span><br><span class="line">  tab[staleSlot].value = <span class="keyword">null</span>;</span><br><span class="line">  tab[staleSlot] = <span class="keyword">null</span>;</span><br><span class="line">  size--;</span><br><span class="line"></span><br><span class="line">  Entry e;</span><br><span class="line">  <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// rehash过程,直到entry为null</span></span><br><span class="line">  <span class="keyword">for</span> (i = nextIndex(staleSlot, len);(e = tab[i]) != <span class="keyword">null</span>; i = nextIndex(i, len)) &#123;</span><br><span class="line">    ThreadLocal&lt;?&gt; k = e.get();</span><br><span class="line">    <span class="comment">// k为空,证明已经被垃圾回收了</span></span><br><span class="line">    <span class="keyword">if</span> (k == <span class="keyword">null</span>) &#123;</span><br><span class="line">        e.value = <span class="keyword">null</span>;</span><br><span class="line">        tab[i] = <span class="keyword">null</span>;</span><br><span class="line">        size--;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">int</span> h = k.threadLocalHashCode &amp; (len - <span class="number">1</span>);</span><br><span class="line">        <span class="comment">// 判断当前元素是否处于"真正"应该待的位置</span></span><br><span class="line">        <span class="keyword">if</span> (h != i) &#123;</span><br><span class="line">            tab[i] = <span class="keyword">null</span>;</span><br><span class="line">            <span class="comment">// 线性探测</span></span><br><span class="line">            <span class="keyword">while</span> (tab[h] != <span class="keyword">null</span>)</span><br><span class="line">                h = nextIndex(h, len);</span><br><span class="line">            tab[h] = e;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面rehash的代码结合文章开头的说明理解起来更是容易,当从ThreadLocalMap新增,获取,删除的时候都会根据条件进行rehash，条件如下</p><ol><li>ThreadLocal对象被回收,此时Entry中key为null,value不为null。这时会触发rehash</li><li>当阈值达到ThreadLocalMap容量的三分之二的时候</li></ol><h4 id="get-方法"><a href="#get-方法" class="headerlink" title="get()方法"></a>get()方法</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> Entry <span class="title">getEntry</span><span class="params">(ThreadLocal&lt;?&gt; key)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> i = key.threadLocalHashCode &amp; (table.length - <span class="number">1</span>);</span><br><span class="line">  Entry e = table[i];</span><br><span class="line">  <span class="comment">// 现在数据中进行查找</span></span><br><span class="line">  <span class="keyword">if</span> (e != <span class="keyword">null</span> &amp;&amp; e.get() == key)</span><br><span class="line">      <span class="keyword">return</span> e;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">      <span class="keyword">return</span> getEntryAfterMiss(key, i, e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 采用线性探测找到对应元素</span></span><br><span class="line"><span class="function"><span class="keyword">private</span> Entry <span class="title">getEntryAfterMiss</span><span class="params">(ThreadLocal&lt;?&gt; key, <span class="keyword">int</span> i, Entry e)</span> </span>&#123;</span><br><span class="line">  Entry[] tab = table;</span><br><span class="line">  <span class="keyword">int</span> len = tab.length;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (e != <span class="keyword">null</span>) &#123;</span><br><span class="line">      ThreadLocal&lt;?&gt; k = e.get();</span><br><span class="line">      <span class="comment">// 找到元素</span></span><br><span class="line">      <span class="keyword">if</span> (k == key)</span><br><span class="line">          <span class="keyword">return</span> e;</span><br><span class="line">      <span class="comment">// ThreadLocal为空,需要删除过期元素，同时进行rehash</span></span><br><span class="line">      <span class="keyword">if</span> (k == <span class="keyword">null</span>)</span><br><span class="line">          expungeStaleEntry(i);</span><br><span class="line">      <span class="keyword">else</span></span><br><span class="line">          i = nextIndex(i, len);</span><br><span class="line">      e = tab[i];</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>线性探测法贯穿了get,set的所有流程，理解了原理在看代码就很简单了。</p><h4 id="remove-方法"><a href="#remove-方法" class="headerlink" title="remove()方法"></a>remove()方法</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">remove</span><span class="params">(ThreadLocal&lt;?&gt; key)</span> </span>&#123;</span><br><span class="line">   Entry[] tab = table;</span><br><span class="line">   <span class="keyword">int</span> len = tab.length;</span><br><span class="line">   <span class="keyword">int</span> i = key.threadLocalHashCode &amp; (len-<span class="number">1</span>);</span><br><span class="line">   <span class="keyword">for</span> (Entry e = tab[i];</span><br><span class="line">        e != <span class="keyword">null</span>;</span><br><span class="line">        e = tab[i = nextIndex(i, len)]) &#123;</span><br><span class="line">       <span class="keyword">if</span> (e.get() == key) &#123;</span><br><span class="line">           e.clear();</span><br><span class="line">           expungeStaleEntry(i);</span><br><span class="line">           <span class="keyword">return</span>;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>remove的时候回删除旧的entry,然后进行rehash.</p><h3 id="ThreadLocal的使用"><a href="#ThreadLocal的使用" class="headerlink" title="ThreadLocal的使用"></a>ThreadLocal的使用</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Counter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> ThreadLocal&lt;Integer&gt; seqCount = <span class="keyword">new</span> ThreadLocal&lt;Integer&gt;()&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">initialValue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">nextInt</span><span class="params">()</span></span>&#123;</span><br><span class="line">    seqCount.set(seqCount.get() + <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> seqCount.get();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">    Counter seqCount = <span class="keyword">new</span> Counter();</span><br><span class="line"></span><br><span class="line">    CountThread thread1 = <span class="keyword">new</span> CountThread(seqCount);</span><br><span class="line">    CountThread thread2 = <span class="keyword">new</span> CountThread(seqCount);</span><br><span class="line">    CountThread thread3 = <span class="keyword">new</span> CountThread(seqCount);</span><br><span class="line">    CountThread thread4 = <span class="keyword">new</span> CountThread(seqCount);</span><br><span class="line"></span><br><span class="line">    thread1.start();</span><br><span class="line">    thread2.start();</span><br><span class="line">    thread3.start();</span><br><span class="line">    thread4.start();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CountThread</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Counter counter;</span><br><span class="line"></span><br><span class="line">    CountThread(Counter counter)&#123;</span><br><span class="line">        <span class="keyword">this</span>.counter = counter;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span> ; i &lt; <span class="number">3</span> ; i++)&#123;</span><br><span class="line">            System.out.println(Thread.currentThread().getName() + <span class="string">" seqCount :"</span> + counter.nextInt());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行效果如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Thread-3 seqCount :1</span><br><span class="line">Thread-0 seqCount :1</span><br><span class="line">Thread-3 seqCount :2</span><br><span class="line">Thread-0 seqCount :2</span><br><span class="line">Thread-0 seqCount :3</span><br><span class="line">Thread-2 seqCount :1</span><br><span class="line">Thread-2 seqCount :2</span><br><span class="line">Thread-1 seqCount :1</span><br><span class="line">Thread-3 seqCount :3</span><br><span class="line">Thread-1 seqCount :2</span><br><span class="line">Thread-1 seqCount :3</span><br><span class="line">Thread-2 seqCount :3</span><br></pre></td></tr></table></figure></p><p>ThreadLocal 其实是为每个线程都提供一份变量的副本, 从而实现同时访问而不受影响。从这里也看出来了它和synchronized之间的应用场景不同, synchronized是为了让每个线程对变量的修改都对其他线程可见, 而 ThreadLocal 是为了线程对象的数据不受其他线程影响, 它最适合的场景应该是在同一线程的不同开发层次中共享数据。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;什么是散列&quot;&gt;&lt;a href=&quot;#什么是散列&quot; class=&quot;headerlink&quot; title=&quot;什么是散列&quot;&gt;&lt;/a&gt;什么是散列&lt;/h3&gt;&lt;p&gt;散列表(hash table)我们平时也叫它哈希表或者Hash表，它用的是数组支持按照下标随机访问数据的特性，所以散列表其实就是数组的一种扩展，由数组演化而来。可以说，没有数组就没有散列表。&lt;/p&gt;
&lt;p&gt;比如我们有100件商品,编号没有规律的4位数字,现在我们想要通过编号快速获取商品信息，如何做呢?我们可以将这100件商品信息放到数组里，通过 商品编号%100这样的方式得到一个值,值为1的商品放到数组中下标为1的位置，值为2的商品，我们放到数组中下标为2的位置。以此类推，编号为K的选手放到数组中下标为K的位置。因为商品编号通过散列函数(编号%100)跟数据下标一一对应，所以但我们需要查询编号为x的商品信息的时候,我们使用同样的方式，将编号转换为数组下标，就可以从对应的数组下标的位置取出数据。&lt;br&gt;这就是散列的典型思想。&lt;/p&gt;
    
    </summary>
    
    
      <category term="Java" scheme="http://yoursite.com/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>为什么零拷贝可以提升Kafka性能?</title>
    <link href="http://yoursite.com/2019/07/19/kafka-zerocopy/"/>
    <id>http://yoursite.com/2019/07/19/kafka-zerocopy/</id>
    <published>2019-07-19T06:15:33.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<h3 id="从操作系统说起"><a href="#从操作系统说起" class="headerlink" title="从操作系统说起"></a>从操作系统说起</h3><p>计算机系统是由“硬件”和“软件”两大部分组成，计算机硬件包括一个或多个处理器(CPU)、内存、键盘、显示器、磁盘、I/O接口以及其他一些外围设备比如打印机，绘图仪等等。<br>总之，计算机硬件部分是一个由多种电子和机械设备组成的硬件系统。</p><p>为了让人方便正确使用这些设备，就需要编写若干程序来管理这些设备，正是这些程序组成了计算机的软件系统。软件也可以分为两大类：系统软件和应用软件。人们首先直接在硬件上加载一层程序，用它来管理整个计算机硬件设备以及一些软件信息资源，同时还为用户提供开发应用程序的环境，这就是操作系统软件和实用软件。应用软件是在操作系统支持下，为实现用户要求而编制的各种应用程序。</p><p><img src="/images/introduction-kafka/computer-layer.png" alt="操作系统层次结构图"></p><a id="more"></a><p>CPU、内存和I/O接口组成的主设备通常称为主机，把没有加载操作系统的主机叫做裸机。裸机与操作系统软件的接口是由CPU的指令系统和厂商提供的系统BIOS组成。</p><p>由于操作系统向用户隐藏了系统使用的硬件设备，因此操作系统要为它上面的应用系统软件提供一组命令或系统调用接口供用户程序使用。比如我们需要使用磁盘，可以通过系统命名或系统调用来间接完成，而不需要亲自手动编写一个磁盘设备驱动程序。因此对于用户来说，当计算机加载操作系统后，用户不直接与计算机硬件打交道，而是利用操作系统提供的命令和功能区使用计算机。</p><p>由于操作系统处于硬件和软件的中央位置，因此很早就有人把操作系统成为计算机系统软件的核心，简称<strong>核心</strong>或<strong>内核</strong>。</p><h3 id="内核态和用户态"><a href="#内核态和用户态" class="headerlink" title="内核态和用户态"></a>内核态和用户态</h3><p>从系统安全和保护的角度出发，在进行计算机体系结构设计时，处理机的执行模式一般设定为两种：分别称为内核模式(内核态)和用户模式(用户态)。当处理机处于内核模式执行时，意味着系统除了可以执行一般指令外，还可以执行特权指令，即可以执行访问各种控制寄存器的指令、I/O指令以及程序状态字。</p><p>当处理机处于用户模式执行时，只能执行一般指令，而不允许执行特权指令。这样做可以保护核心代码不受用户程序有意和无意的攻击。<br>显然，处理机在运行期间需要在内核模式和用户模式之前进行切换。</p><h3 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a>零拷贝</h3><p>Kafka使用零拷贝(Zero-Copy)技术来提供它的性能，所谓的零拷贝是指将数据直接从磁盘文件复制到网卡设备中，而不需要经由应用程序之手，减少了内核和用户模式之间的上下文切换，零拷贝技术通过DMA技术实现。</p><blockquote><p>直接存储器存取方式(Direct Memory Access, DMA)<br>DMA控制方式是以存储器为中心，在主存和I/O设备之间建立一条直接通路，在DMA控制器的控制下进行设备和主存之间的数据交换。这种方式只在传输开始和传输结束时才需要CPU的干预。它非常适用于高速设备与主存之间的成批数据传输。</p></blockquote><p>我们看下下面这样的这样一个场景：</p><p>客户端在游览器中发起请求获取内容，到看到具体内容经历了什么?</p><p><img src="/images/introduction-kafka/os-message-flow.png" alt="消息发送"></p><p>首先，该请求经过解析后，通过系统调用由用户态转为核心态执行，在核心态由操作系统中的TCP/IP协议代码和网卡驱动程序控制网卡把请求发送到相应的网络上，<br>等待Web服务器相应。当服务器返回时，由网卡接受，并通过内核传送给客户程序。</p><p>在服务器端，内核通过网卡从网络上接受Web请求，并通过系统调用传递给Web服务器。Web服务器根据此服务请求执行相应的服务进程，并由内核把结果发送到网络上传送给用户。</p><p>从上图中可以看到如果服务器从准备数据到发送数据经历了下面4个过程。</p><ol><li>调用read()时，将文件中的内容复制到内核模式下的Read Buffer中</li><li>CPU控制将内核模式数据复制到用户模式下</li><li>调用send()时，用用户模式下的内容复制到内核模式下的Socket Buffer中。</li><li>将内核模式下的Socket Buffer的数据复制到网卡设备中发送。</li></ol><p>从上面过程可以看出，数据是先从内核模式–&gt;用户模式–&gt;内核模式，浪费了2次复制过程：第一次是从内核模式复制到用户模式；第二次是从用户模式再复制回内核模式，而且在上面的过程中，内核和用户模式的上下文切换也是4次。</p><p>如果采用了零拷贝技术，那么应用程序就可以直接请求内核把磁盘中的数据传输给Socket.</p><p>零拷贝技术通过DMA技术将文件内容复制到内核模式下的Read Buffer中。不过没有数据被复制到Socket Buffer，只有包含数据的位置和长度的信息的文件描述符被加到Socket Buffer中。DMA引擎直接将数据从内核模式中传递到网卡设备。这里上下文切换变成了2次,也只经历了2次复制过程就从磁盘中传送出去了。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;从操作系统说起&quot;&gt;&lt;a href=&quot;#从操作系统说起&quot; class=&quot;headerlink&quot; title=&quot;从操作系统说起&quot;&gt;&lt;/a&gt;从操作系统说起&lt;/h3&gt;&lt;p&gt;计算机系统是由“硬件”和“软件”两大部分组成，计算机硬件包括一个或多个处理器(CPU)、内存、键盘、显示器、磁盘、I/O接口以及其他一些外围设备比如打印机，绘图仪等等。&lt;br&gt;总之，计算机硬件部分是一个由多种电子和机械设备组成的硬件系统。&lt;/p&gt;
&lt;p&gt;为了让人方便正确使用这些设备，就需要编写若干程序来管理这些设备，正是这些程序组成了计算机的软件系统。软件也可以分为两大类：系统软件和应用软件。人们首先直接在硬件上加载一层程序，用它来管理整个计算机硬件设备以及一些软件信息资源，同时还为用户提供开发应用程序的环境，这就是操作系统软件和实用软件。应用软件是在操作系统支持下，为实现用户要求而编制的各种应用程序。&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/images/introduction-kafka/computer-layer.png&quot; alt=&quot;操作系统层次结构图&quot;&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="Kafka" scheme="http://yoursite.com/tags/Kafka/"/>
    
  </entry>
  
  <entry>
    <title>MongoDB中的定时索引</title>
    <link href="http://yoursite.com/2019/07/15/mongodb-ttl-index/"/>
    <id>http://yoursite.com/2019/07/15/mongodb-ttl-index/</id>
    <published>2019-07-15T07:31:23.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>MongoDB中存在一种索引,叫做TTL索引(time-to-live index,具有生命周期的索引)，这种索引允许为每一个文档设置一个超时时间。一个文档达到预设置的老化程度后就会被删除。<br>数据到期对于某些类型的信息非常有用，例如机器生成的事件数据，日志和会话信息，这些信息只需要在数据库中保存有限的时间。</p><p>在createIndex中指定expireAfterSeconds选项就可以创建一个TTL索引：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">// 超时时间为24小时,默认是前台运行，可以通过background:true设置为后台模式</span><br><span class="line">db.user_session.createIndex(&#123;&quot;updated&quot;:1&#125;,&#123;expireAfterSeconds:60*60*24&#125;);</span><br></pre></td></tr></table></figure><a id="more"></a><p>这样在updated字段上创建了一个TTL索引。如果一个文档的updated字段存在并且它的值是日期类型，当服务器时间比文档的updated字段的时间晚expireAfterSeconds秒时，文档就会被删除。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">db.getCollection(&apos;user_session&apos;).insert(</span><br><span class="line">  &#123;</span><br><span class="line">    _id: NumberInt(1),</span><br><span class="line">    &quot;updated&quot;:new Date(),</span><br><span class="line">     username:&apos;lisi&apos;</span><br><span class="line">  &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure><blockquote><p>mongodb保存时间使用的UTC时间，在查询出来的结果的时候会转换为GMT时间，所以你看到保存的时间和电脑时间相差8个小时(GMT+8)<br>db.getCollection(‘user_session’).find({updated:{$gt: new Date(“2019-07-12 14:00:00”)}})  在查询的时候可以使用new Date()直接进行时间的比较，new Date传入的参数是GMT时间</p></blockquote><p>为了防止活跃的会话被删除，可以在会话上有活动发生时将updated字段的值更新为当前时间。只要updated的时间距离当前时间达到24小时。相应的文档就会被删除。</p><p>MongoDB的TTL功能依赖于mongodb中的后台线程，该线程读取索引中的日期类型值并从集合中删除过期的文档。<br>MongoDB每分钟对TTL索引进行一次清理，所以不应该依赖以秒为单位的时间保证索引的存活状态。而且TTL索引不保证在到期时立即删除过期数据。文档到期的时间与MongoDB从数据库中删除文档的时间之间可能存在延迟。由于删除过期文档的后台任务每60秒运行一次。所以，文档可能在文档到期和后台任务运行之间的期间保留在集合中。</p><blockquote><p>源码在 <a href="https://github.com/mongodb/mongo/blob/master/src/mongo/db/ttl.cpp" target="_blank" rel="noopener">https://github.com/mongodb/mongo/blob/master/src/mongo/db/ttl.cpp</a></p></blockquote><p>mongodb不支持使用createIndex来重新设置过期时间，只可以使用collMod命令修改expireAfterSeconds的值：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">db.runCommand(&#123;collMod:&quot;user_session&quot;,index: &#123;name:&quot;updated_1&quot;,expireAfterSeconds: 120&#125;&#125;);</span><br></pre></td></tr></table></figure><p>修改成功后，你会收到这样的消息(之前的过期时间是一分钟,现在修改为2分钟)</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;expireAfterSeconds_old&quot; : 60.0,</span><br><span class="line">    &quot;expireAfterSeconds_new&quot; : 120.0,</span><br><span class="line">    &quot;ok&quot; : 1.0</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在一个给定的集合上可以有多个TTL索引，你可以在created和updated字段分别建立ttl索引,但是不能同时使用两个字段建立复合ttl索引,也不能在同一个字段上又是创建TTL索引，又是创建普通索引，但是可以像“普通索引”一样用来优化排序和查询。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;MongoDB中存在一种索引,叫做TTL索引(time-to-live index,具有生命周期的索引)，这种索引允许为每一个文档设置一个超时时间。一个文档达到预设置的老化程度后就会被删除。&lt;br&gt;数据到期对于某些类型的信息非常有用，例如机器生成的事件数据，日志和会话信息，这些信息只需要在数据库中保存有限的时间。&lt;/p&gt;
&lt;p&gt;在createIndex中指定expireAfterSeconds选项就可以创建一个TTL索引：&lt;/p&gt;
&lt;figure class=&quot;highlight plain&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;// 超时时间为24小时,默认是前台运行，可以通过background:true设置为后台模式&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;db.user_session.createIndex(&amp;#123;&amp;quot;updated&amp;quot;:1&amp;#125;,&amp;#123;expireAfterSeconds:60*60*24&amp;#125;);&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
    
      <category term="MongoDB" scheme="http://yoursite.com/tags/MongoDB/"/>
    
  </entry>
  
  <entry>
    <title>MongoDB的锁事</title>
    <link href="http://yoursite.com/2019/07/05/mongodb-locks/"/>
    <id>http://yoursite.com/2019/07/05/mongodb-locks/</id>
    <published>2019-07-05T10:12:56.000Z</published>
    <updated>2019-12-20T03:26:24.675Z</updated>
    
    <content type="html"><![CDATA[<p>MongoDB有4个粒度级别的锁</p><ol><li>Global(MongoD 实例) – 所有的数据库上加锁</li><li>Database – 锁定某个数据库</li><li>Collection – 锁定某个集合</li><li>Document – 锁定某个文档</li></ol><p>MongoDB本身只提供Global,Database,Collection三个级别的锁,Document级别的锁是由存储引擎提供的(Wired Tiger提供了Document级别的锁)</p><a id="more"></a><p>MongoDB允许多个客户端读取和写入相同的数据。为了确保一致性，它使用锁和其他并发控制措施来防止多个客户端同时修改同一条数据。总之，这些机制保证对单个文档的所有写入完全或根本不发生，并且客户端永远不会看到数据的不一致视图。如果你想保证多个文档的并发性,就需要使用事务,MongoDB从4.0版本开始支持多文档事务(包括复制集),但是对分片集群的事务支持要从4.2版本才开始支持。</p><p>MongoDB使用<strong>多粒度锁</strong>，允许操作锁定全局，数据库或集合级别，并允许各个存储引擎在集合级别下实现自己的并发控制(例如WiredTiger提供了文档级别的锁)。MongoDB使用读写锁，并且允许并发读共享对资源(database或者collection)的访问。</p><p>除了用于读取的共享(S)锁(读锁)和用于写入操作的排它(X)锁(写锁)之外，意向共享(IS)和意向排它(IX)锁表示使用更精细的粒度锁定读取或写入资源的意图。但我们锁定某个资源的时候，使用意向锁来锁定比它更高级别的资源，这也是多粒度锁的核心。</p><p>对一个数据行或者document加意向锁的含义是:当前事务想要在这个节点或者这个节点的子节点加某种锁(共享或者排它)，有了意向锁以后，对任何数据加共享锁或者排它锁都要先加意向锁再加具体锁。</p><p><img src="/images/mongodb/node.png" alt="节点"></p><p>当我们对d1这个document进行写入的时候，需要给它加上X锁(排它锁),这个时候要先去它上层的资源Global,D1(database),C1(collection)加上IX(意向排它锁),加了之后才能在d1上加实际的排它锁。如果实际的锁加在C1等非叶子节点情况也是一样的。</p><p>加意向锁最主要的作用就是在加实际的锁的时候，通过检查父节点是否已经存在了意向锁来确定当前是否可以加上锁。</p><p>比如d1上持有了实际的排它锁，这个时候C1,D1,Global都有一个意向排它锁。此时如果有另外的请求想要在C1上加实际的共享锁(读),但是C1已经有了意向排它锁了,所以就加不成了，需要等待。</p><p>如果此时想要在d2上加共享锁。此时需要先去它的上层节点添加意向共享锁,虽然C1,D1已经存在了意向排它锁，但是仍然可以加上意向共享锁，因为它们是兼容的。</p><p>把意向锁的相容性和实际锁放进一个表格进行对别下：</p><table><thead><tr><th></th><th>IS(意向共享)</th><th>IX(意向排它)</th><th>S(共享)</th><th>X(排它)</th></tr></thead><tbody><tr><td>IS(意向共享)</td><td>true</td><td>true</td><td>true</td><td>false</td></tr><tr><td>IX(意向排它)</td><td>true</td><td>true</td><td>false</td><td>false</td></tr><tr><td>S(共享)</td><td>true</td><td>false</td><td>true</td><td>false</td></tr><tr><td>X(排它)</td><td>false</td><td>false</td><td>false</td><td>false</td></tr></tbody></table><p>对于大多数读写操作，WiredTiger存储引擎使用乐观并发控制。WiredTiger仅在全局，数据库和集合级别使用意向锁。当存储引擎检测到两个操作之间的冲突时，会发生写入冲突，导致MongoDB透明地重试该操作。一些全局操作(通常是涉及多个数据库的短期操作)仍然需要全局“实例范围”锁定。其他一些操作（例如删除集合）仍需要独占数据库锁。</p><p>我们MongoDB中使用db.serverStatus()或者db.currentOp()命令的输出中，锁模式表示如下：</p><table><thead><tr><th>锁模式</th><th>描述</th></tr></thead><tbody><tr><td>R</td><td>共享锁(S)</td></tr><tr><td>W</td><td>排它锁(X)</td></tr><tr><td>r</td><td>意向共享锁(IS)</td></tr><tr><td>w</td><td>意向排它锁(IX)</td></tr></tbody></table><p>所以我们要注意对应关系,不要弄错了。下面的表格列出了一些操作以及它们用于文档级锁定存储引擎的锁类型：</p><table><thead><tr><th>操作</th><th>Database</th><th>Collection</th></tr></thead><tbody><tr><td>query</td><td>r(意向共享锁)</td><td>r(意向共享锁)</td></tr><tr><td>insert</td><td>w(意向排它锁)</td><td>w(意向排它锁)</td></tr><tr><td>remove</td><td>w(意向排它锁)</td><td>w(意向排它锁)</td></tr><tr><td>update</td><td>w(意向排它锁)</td><td>w(意向排它锁)</td></tr><tr><td>aggregation(聚合)</td><td>r(意向共享锁)</td><td>r(意向共享锁)</td></tr><tr><td>create an index(前台)</td><td>W(排它锁)</td><td></td></tr><tr><td>create an index(后台)</td><td>w(意向排它锁)</td><td>w(意向排它锁)</td></tr><tr><td>list collections</td><td>r(意向共享锁)</td><td></td></tr><tr><td>map-reduce</td><td>W(排它锁) 以及 R(共享锁)</td><td>w(意向排它锁)以及r(意向共享锁)</td></tr></tbody></table>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;MongoDB有4个粒度级别的锁&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Global(MongoD 实例) – 所有的数据库上加锁&lt;/li&gt;
&lt;li&gt;Database – 锁定某个数据库&lt;/li&gt;
&lt;li&gt;Collection – 锁定某个集合&lt;/li&gt;
&lt;li&gt;Document – 锁定某个文档&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;MongoDB本身只提供Global,Database,Collection三个级别的锁,Document级别的锁是由存储引擎提供的(Wired Tiger提供了Document级别的锁)&lt;/p&gt;
    
    </summary>
    
    
      <category term="MongoDB" scheme="http://yoursite.com/tags/MongoDB/"/>
    
  </entry>
  
</feed>
